# AudioShelf — ROADMAP

> Source of truth for what to build next. A fresh session reads this, finds the topmost milestone that is **not ✅ Merged**, and acts. Follows the `/roadmap` workflow (Opus plans/researches · Sonnet implements · ping at every phase handoff).

**Legend:** ✅ Merged · 📝 Plan ready (execute next) · 🔬 Researching/Planning · [ ] Not started (plan first)

## Definition

Windows desktop app for listening to a large library of short-form spoken audio (audiobooks ~5min–1hr, a chapter at a time). Tauri 2 + React 18 + TypeScript + SQLite (rusqlite). **Read-only on audio files by default**; the rename tool is the sole sanctioned mutation.

- Repo: https://github.com/yovanmc/AudioShelf (default branch `main`)
- Design spec: [`docs/superpowers/specs/2026-06-11-audioshelf-design.md`](docs/superpowers/specs/2026-06-11-audioshelf-design.md)
- Detailed runbook (how-to, env, CI): [`docs/superpowers/WORKFLOW-execution.md`](docs/superpowers/WORKFLOW-execution.md)

## Conventions

- Cargo via `cmd /c "tools\dev-env.cmd cargo ..."` in the FOREGROUND (large timeout); `npm run build` before any `cargo tauri build`.
- Gates: `npx tsc --noEmit` · `npm test` · `cargo test` · `tools\verify.ps1 -Walkthrough <name>` (screenshot self-verification).
- Commits: use the repo's configured git identity (`yovanmc <yovanmc@users.noreply.github.com>`) — never pass `-c user.email=...` overrides. Follow the workspace `AGENTS.md`: substantive Codex-generated commits append `Co-authored-by: Codex <noreply@openai.com>` after a blank line.
- CI: `build-and-test` on windows-latest. Merge PRs `--merge --delete-branch` from main; FOREGROUND `gh pr checks <PR#> --watch`.
- App ships **no stylesheet** in v1 (intentionally unstyled).

## Milestones

| # | Title | Status | Plan | PR | Notes |
|---|-------|--------|------|----|----|
| 1 | Foundation & Library Browsing | ✅ Merged | [M1](docs/superpowers/plans/2026-06-11-audioshelf-foundation.md) | [#1](https://github.com/yovanmc/AudioShelf/pull/1) | Scan `Author/` dirs → filename grouping (`original_stem` for file lookup) → browse author→works→chapters + played toggle. Harness + WAV fixture + CI. Read-only verified. |
| 2 | Playback & Progress | ✅ Merged | [M2](docs/superpowers/plans/2026-06-11-audioshelf-m2-playback.md) | [#2](https://github.com/yovanmc/AudioShelf/pull/2) | Now-playing PlayerBar (play/pause, seek, skip ±15/30s, volume, sleep timer); auto-mark-played on finish + `play_events`; stop after each. Asset scope hardened to `[]` + runtime grant of library root. |
| 3 | Tags & Discovery | ✅ Merged | [M3](docs/superpowers/plans/2026-06-11-audioshelf-m3-discovery.md) | [#3](https://github.com/yovanmc/AudioShelf/pull/3) | Author TagEditor; Discover panel (For-you / pick-a-tag / more-from-author). No schema change. DiscoveryView `picked` lifted to App (controlled). |
| 4 | Opt-in Rename Tool | ✅ Merged | [M4](docs/superpowers/plans/2026-06-11-audioshelf-m4-rename.md) | [#4](https://github.com/yovanmc/AudioShelf/pull/4) | Defensive, crash-safe, reversible batch rename to canonical names (`rename.rs`: build_plan ok/noop/conflict; manifest-before-rename + TOCTOU; tolerant idempotent undo). The only audio-file mutation. |
| 5 | Grouping-Override Review UI | ✅ Merged | [M5](docs/superpowers/plans/2026-06-11-audioshelf-m5-grouping.md) | [#5](https://github.com/yovanmc/AudioShelf/pull/5) | Per-chapter Work/Ch# edit on Author Detail; DB-only `regroup_author` overlays `grouping_overrides` on the heuristic, re-applied on scan, never to disk. Merge/split/reassign/reset. 41 FE + 32 Rust tests (incl. `grouping_roundtrip`). Screenshot-verified merge→reset round-trip. |
| 6 | Settings & Library-Root Picker | ✅ Merged | [M6](docs/superpowers/plans/2026-06-11-audioshelf-m6-settings.md) | [#6](https://github.com/yovanmc/AudioShelf/pull/6) | App is now standalone-usable: generic `get_setting`/`set_setting` over the `settings` table + `tauri-plugin-dialog` folder picker. Bootstrap precedence `--library` flag → persisted `library_root` → first-run onboarding; a gone/unreadable saved root **fails safe** to Settings with the error. Prop-driven `SettingsView` (choose-folder/re-scan/onboarding), Settings button on LibraryView, `settings` walkthrough. 47 FE + 33 Rust tests. No new audio-file mutation (only the SQLite settings write). |
| 7 | Scale & Search Polish | ✅ Merged | [M7](docs/superpowers/plans/2026-06-11-audioshelf-m7-scale-search.md) | [#7](https://github.com/yovanmc/AudioShelf/pull/7) | Virtualized author list (`react-window` FixedSizeList, 300+/10k); backend `search_library` SQL search across authors/works/chapters (debounced 150ms, per-bucket cap 50, `LibraryView` fully controlled); inline color+initials cover placeholders; +40 `Zz Sample Author NN` filler fixtures (sort last) + `m7` walkthrough. **Finishes the v1 spec.** 53 FE + 34 Rust tests; m7 walkthrough verified (list+swatches+scroll, cool→works/chapters, sam→author) + player/grouping regression-verified (Jane Doe still first). |

### v2 — Library Experience

> Deepens browsing & organization (the user picked these three; multi-root explicitly deferred). Order: covers (visual, self-contained) → tags (schema, extends discovery) → browsing UI (frontend polish that builds on the prior two).

| # | Title | Status | Plan | PR | Notes |
|---|-------|--------|------|----|----|
| 8 | Embedded Cover Art | ✅ Merged | [M8](docs/superpowers/plans/2026-06-12-audioshelf-m8-cover-art.md) | [#8](https://github.com/yovanmc/AudioShelf/pull/8) | On-the-fly `get_work_cover` (embedded-first) / `get_author_cover` (folder-first) resolve a cover from audio-tag pictures (`lofty`) **or** a folder image (`cover/folder/front.{jpg,jpeg,png}`); thumbnailed (`image`, ≤256px) and cached under `<app_data>/covers/`, served via the existing asset protocol — **NO schema change**, no `base64`. Author dir = `parent(file_path)` (sidesteps harness-unset `library_root`). `<Cover>` lazy-loads with a module-level client cache and falls back to the shared color+initials `<Swatch>` (no layout shift). Folder-`cover.png` fixtures on Jane Doe (red) + Sam Smith (blue); `covers` walkthrough + `imagesSettled()` before cover shots. Read-only intact (only writes are app-private thumbnails). 6 unit + 2 integration Rust tests, 53 FE tests; covers/browse/m7 screenshot-verified. |
| 9 | Work & Chapter Tags | ✅ Merged | [M9](docs/superpowers/plans/2026-06-12-audioshelf-m9-work-chapter-tags.md) | [#10](https://github.com/yovanmc/AudioShelf/pull/10) | New idempotent `work_tags`/`chapter_tags` tables; generic injection-safe `replace_tags(table,key_col,…)` helper (+`set_work_tags`/`set_chapter_tags` commands); `WorkRow`/`ChapterRow` gain `tags`. Discovery refactored author-centric → **work-centric union** (author ∪ work tags; chapter tags excluded by design; backward-compatible). Search matches work/chapter tags via `EXISTS` subqueries; `get_all_tags` unions all 3 tables. UI: always-visible work TagEditor + per-chapter toggle (open-when-tagged). `tags` walkthrough. **No audio-file mutation; fixture counts stayed 43/44/47** (tags seeded at runtime); no `Cargo.lock` churn. 61 FE + 45 Rust tests; tags/discovery/m7 screenshot-verified. |
| 10 | Richer Browsing UI | ✅ Merged | [M10](docs/superpowers/plans/2026-06-12-audioshelf-m10-browsing-ui.md) | [#12](https://github.com/yovanmc/AudioShelf/pull/12) | Author list: sort A–Z/Length/Played% + filter by tag & played-status + enriched per-row counts (`… NN% played · <duration>`); author detail: sort works + per-work collapse/expand-all. Prefs persist as one `browse_prefs` JSON (M6 settings). `AuthorRow.{totalSecs,tags}` added to `query_authors` — **no schema change**. Sort/filter client-side on the in-memory virtualized list. Fixed a stale-closure pref bug (functional updaters). 104 FE + 47 Rust tests; browse walkthrough + m7/tags/covers screenshot-verified. Read-only; fixtures 43/44/47; no `Cargo.lock` churn. |

### v3 — Personal Home & Look

> Make AudioShelf feel finished and personal. Two milestones: a **Personal Home** landing surface (author-centric "continue listening" + your listening stats/history, all derived from the existing `play_events` log), then the app's **first design system** (theme tokens, light/dark, restyle every view) built **last** so it re-skins the new home in one pass. Order chosen by the user: home first, design last. Both preserve the M8→M10 **no-migration** streak (read-only-on-disk; the only writes remain the SQLite `settings`/`play_events` rows). Per-second mid-chapter resume, multi-root libraries, and playback speed/auto-advance remain explicitly deferred.

| # | Title | Status | Plan | PR | Notes |
|---|-------|--------|------|----|----|
| 11 | Personal Home (Continue + Stats) | ✅ Merged | [M11](docs/superpowers/plans/2026-06-12-audioshelf-m11-personal-home.md) | [#16](https://github.com/yovanmc/AudioShelf/pull/16) | New default `home` route (Library demoted to a 🏠-reachable nav button). **Author-centric "Jump back in"** (`home_continue` reuses `recent_authors()`; resumes at the next unplayed chapter — prefer most-recently-played work, else first active work by `sort_key`; fully-played authors omitted; **chapter granularity, no offset**) + **"Your listening"** (`home_stats`: total time & chapters-finished from the `played` flag — replays not double-counted; **tz-offset local-day-bucket streak**, std-only math no `chrono`; recent-10 history). Read-only `query_home(now_ms, tz_offset_minutes)`. **No schema migration, no `Cargo.lock` churn, fixtures stayed 43/44/47.** `home` walkthrough (empty → seeded-2-days populated). 110 FE + 50 Rust tests; both home shots PASS, browse/player unregressed. |
| 12 | Design System & Theming | ✅ Merged | [M12](docs/superpowers/plans/2026-06-12-audioshelf-m12-design-system.md) | [#19](https://github.com/yovanmc/AudioShelf/pull/19) | The app's first design system, **dark-only**: `src/styles/{tokens,base,components,layout}.css` (near-black canvas, electric-blue accent) + shared `src/components/` layer (`ui`, `Icon` local SVGs, `AppShell`, `Menu`, `WorkCard`, `CreatorIdentity`, `Cover`→`WorkArtwork`/`CreatorAvatar`); persistent collapsible sidebar (only new setting `sidebar_collapsed`, fails safe to expanded); every view restyled. Home = **Keep listening to [Creator]** feature (preserves M11 next-unplayed resume) + six-card **You May Like** grid (read-only `home_keep_listening`/`home_recommendations`: excludes featured work, favors mostly-unplayed + creator diversity, deterministic fallback). Expandable **Now Playing** panel reusing the single audio element. **No schema/dep/audio-mutation change; fixtures stayed 43/44/47.** 122 FE + 52 Rust tests; 13-shot `m12` matrix + all 10 prior walkthroughs verified. |

### v4 — Player & Home Polish

> Post-v3 refinement pass, scoped 2026-06-12. M13 came from a two-lens expert UI/UX review of the post-M12 screens (see the review spec); M14/M15 from a survey of audio apps (Audiobookshelf, Plexamp, Pocket Casts, Libby, Audible, Headspace, Letterboxd/Goodreads). **Read-only / additive** — keeps the M8→M12 no-migration streak; **per-second mid-chapter resume stays deferred** (would need a `chapters.playback_position_secs` column). Three milestones, **consistency foundation first**, then the user-chosen player→home feature split: M13 fixes cross-cutting consistency + builds the shared primitives (modal, section heading, tag group, tooltips, responsive card grid) that M14/M15 then build their feature additions on.

| # | Title | Status | Plan | PR | Notes |
|---|-------|--------|------|----|----|
| 13 | UI Consistency & UX Refinement | ✅ Merged | [M13](docs/superpowers/plans/2026-06-12-audioshelf-m13-ui-consistency.md) | [#25](https://github.com/yovanmc/AudioShelf/pull/25) | **FE-only** (no Rust/schema/dep change; `cargo test` stayed 47). Shared primitives `PageHeader`/`SectionHeading`/`TagGroup`/`Dialog` (in `ui.tsx`) adopted across views. Global visual: standardized eyebrows (de-jargoned Rename; search-aware Library eyebrow), card-grid `auto-fill` reflow, settings max-width, artwork clip, progress contrast, card eyebrow tones, single `WorkCard`, rename pill accent, "Has unlistened chapters". Core-loop: **Play-next-chapter** on every work card (via `getAuthorDetail`→first-unplayed→`playChapter`, no new Rust), true **first-run Home** (suppresses false recs), **author-detail editing isolated in a `Dialog`** behind a per-chapter overflow menu (fixed the shot-06 collision; `useId` datalist), **Now Playing** "Chapter X of Y" + stop-note + top-right close, Discovery pill-toggles above "For You", sidebar tooltips + global focus ring. 141 FE + 47 Rust; before/after `m12` matrix subagent-verified (all criteria PASS) + 7 regressions unregressed. |
| 14 | Player & Now Playing Polish | ✅ Merged | [M14](docs/superpowers/plans/2026-06-12-audioshelf-m14-player-polish.md) | [#28](https://github.com/yovanmc/AudioShelf/pull/28) | **FE-only** (no Rust/schema/dep; `cargo test` stayed 47; fixtures 43/44/47). (1) **Cycling time label** — player timestamp is a button cycling elapsed → time-left (`-m:ss`) → percent; mode shared App-level (`timeLabelMode`) so compact PlayerBar + expanded Now Playing stay in sync; helpers `formatTimeLeft`/`formatPercent`/`timeLabel` in `playback.ts`. (2) **Chapters-in-this-work panel** — expanded Now Playing lists the current work's sibling chapters ("In this work": played dot, `Ch N — Title`, current highlighted via `aria-current`), one-click jump via `playChapter` (no per-second offset); App fetches via `getAuthorDetail` keyed on `workId`/`authorId`, panel stays pure prop-driven, shows only for multi-chapter works. (3) Layout polish (scrollable list in M13 `Dialog`). New harness `player-chapters` step. 161 FE (+20) + 47 Rust; before/after `m12`+`player` subagent-verified PASS. |
| 15 | Home & Browsing Polish | ✅ Merged | [M15](docs/superpowers/plans/2026-06-12-audioshelf-m15-home-browsing-polish.md) | [#31](https://github.com/yovanmc/AudioShelf/pull/31) | **FE-only** (no Rust/schema/dep; `cargo test` stayed 47; fixtures 43/44/47; 210 FE +49; **no new backend command**). (1) **Configurable Home shelves** — user-defined named horizontal rows; 3 kinds via EXISTING commands (`tag`→`getDiscoveryByTags`, `creator`→`getAuthorDetail`, `status`→`getAuthors`+`filterAuthors`→creator cards); persisted as one `home_shelves` JSON setting (failsafe parse); new `src/lib/shelves.ts` + **pure** `Shelf` component + `.card-row` scroll CSS; **App fetches shelf data, views stay pure (no `invoke`)**; default empty → Home unchanged until customized. (2) Shelf **management in Settings** (add/remove/reorder/rename). (3) **Played-status Library tabs** (promoted the status `<select>`→top-level tab bar; hidden during search). (4) **Richer creator hub** — Author Detail header + per-work hours via `formatLong`; per-work progress bars intact. (5) Home recent-listened rows clickable→open author. New harness `home-shelves` step. Before/after `m12` matrix subagent-verified PASS (Author-Detail total reads `0m` only because fixtures use ~5s synthetic clips; `formatLong` unit-tested for real hours). |

### v5 — Depth & Reach  ⚠️ PROVISIONAL (M16 ✅ · M17 ✅ · M18 ✅ · M19 ✅ · M20 ✅ · M21 ✅ Metadata & Discovery (narrator + per-audio) shipped 2026-06-13 PR #51)

> **One version above the current work (v4).** A forward-looking plan synthesized from an
> intensive **five-lens** subagent UX review (power-user, accessibility, engagement/insight,
> library-intelligence, spoken-audio domain) — full idea bank in the
> [v5 backlog](docs/superpowers/specs/2026-06-12-audioshelf-v5-backlog.md). **Refine & re-validate
> before starting** — v4 (M13–M15) ships first and may change what v5 should contain; each
> milestone is still planned just-in-time. **v5 deliberately ends the M8→M12 no-migration streak**
> (the Journal + Intelligence bets need the first schema migrations; reuse the crash-safe
> table-rebuild migration pattern). Read-only-on-disk preserved; non-goals (queue/autoplay/speed/
> social) still hold. Order below is a proposal (foundations first); validate at v5 start.

| # | Title | Status | Plan | PR | Notes |
|---|-------|--------|------|----|----|
| 16 | Library Intelligence | ✅ Merged | [M16](docs/superpowers/plans/2026-06-12-audioshelf-m16-library-intelligence.md) | [#34](https://github.com/yovanmc/AudioShelf/pull/34) | *Foundation; the project's **first** schema migrations.* Versioned crash-safe migration runner (`db.rs` `migrate()` → `PRAGMA user_version`-gated; `run_step` bumps `user_version` atomically inside each step's txn; `open_at_version(v)` test helper; legacy v1/`user_version=0` DBs upgrade cleanly). **All 5 migrations ADDITIVE** (v2 `tag_aliases`/`tag_parents`, v3 `metadata_source` col, v4 `series`/`work_series_membership`, v5 `transcripts`) — **no FK-off table-rebuild exercised**. All 8 sub-features shipped: **tag taxonomy** (rename/merge/alias via INSERT-OR-IGNORE-then-DELETE across the 3 denormalized tag tables + usage counts + alias-aware `discovery_for_tags`; Tag manager in Settings), **embedded-metadata ingestion** (`lofty` `primary_tag()` read at scan, `preview_metadata`/`apply_metadata` diff-preview→apply mirroring Rename, **never auto-at-scan**; "Import metadata" view), **series detection** (reuses `grouping::parse_stem`; auto-detect+apply; spine in Author Detail + continue-series), **transcripts** (sidecar `.srt`/`.vtt` ingest + `parse_srt_vtt` + **`LIKE` search, no FTS5/no new dep**; Library bucket + Now-Playing panel), **dormancy "Forgotten" shelf**, **more-like-this**, **calmer Discover** (additive `DiscoveryWork.reason`), **auto-tag chips**. **`lofty 0.21` already a dep → NO new crate; `Cargo.lock`/`Cargo.toml` unchanged. Read-only-on-disk preserved (all writes SQLite). Fixtures held at 43/44/47.** cargo all green (95 unit + integration; prior 47 unregressed) · tsc clean · 248 FE (+38) · `m16` walkthrough + `m12` matrix subagent-verified PASS. |
| 17 | The Listening Journal | ✅ Merged | [M17](docs/superpowers/plans/2026-06-12-audioshelf-m17-listening-journal.md) | [#37](https://github.com/yovanmc/AudioShelf/pull/37) | *Signature; schema v6 (additive) — first migration since M16.* All 7 sub-features shipped broad in one milestone: timestamped **notes** (position+created_at), **positional bookmarks** (**jump-to only — no auto-resume**; the app's first sub-chapter *seek*, applied only after `loadedmetadata` on explicit action), per-chapter **summary**+**takeaway**+**★ favorite** (chapter cols), per-work **"where I left off"** note + **one-word completion rating** (work cols), unified **Journal view** (browse/filter-by-kind/search + **export Markdown/JSON**). Migration v6 = 2 tables (`chapter_notes`/`chapter_bookmarks`) + 5 `ADD COLUMN` on the M16 `run_step`/`user_version` runner (additive, `SCHEMA_V1` untouched, `open_at_version` extended, legacy DBs upgrade clean). `collect_journal`/`build_journal_markdown`/`build_journal_json` pure+unit-tested; `query_journal` Rust-side filter. **Read-only-on-disk preserved** — only new write is `export_journal`→user-chosen non-audio path (`std::fs::write`, save dialog; added `dialog:allow-save` capability). **Fixtures 43/44/47** (all journal data seeded at runtime in new `journal` walkthrough); **no `Cargo.lock`/`Cargo.toml` churn** (`serde_json` already a dep). 108 Rust + 292 FE tests; `journal` 6-shot walkthrough + `m16`/`m12` regression subagent-verified PASS (sole cross-screen change = new Journal sidebar nav item). |
| 18 | Insight & "Your Year in Listening" | ✅ Merged | [M18](docs/superpowers/plans/2026-06-12-audioshelf-m18-insights.md) | [#40](https://github.com/yovanmc/AudioShelf/pull/40) | *Pure visualization over `play_events` + Journal — **NO schema migration** (`LATEST` stays 6); returns to the pre-M16 read-only/no-migration approach.* All 5 sub-features shipped: 52-week **heatmap** (+Less→More legend), trends (month-vs-month + time-of-day 24h + day-of-week 7), per-creator/per-tag breakdowns (work-level `work_tags ∪ author_tags`), weekly **rhythm** (16wk), exportable annual **recap** (self-contained dark SVG → WebView canvas → bytes → Rust `export_recap_png`/`std::fs::write` to a user-chosen path; **no new dep, no canvas taint**). New `src-tauri/src/insights.rs`: pure unit-tested `build_insights` + std-only date math (`civil_from_days` Hinnant, `weekday_of` Sun=0, `longest_run`, `local_hour`), thin `compute_insights` does the SQL; reuses `home_stats` tz-offset local-day bucketing + existing `streak_len`. Read-only `query_insights`; harness-only `seed_play_events`. FE: pure prop-driven `InsightsView` w/ hand-rolled SVG/CSS viz (no charting lib), `insights.ts`/`recap.ts` helpers unit-tested, canvas rasterizer in `App.tsx`; new bar-chart Icon + `insights` sidebar nav. FE tag iface named `InsightTagStat` (`TagStat` taken by M16). **Fixtures 43/44/47, deps byte-clean, read-only-on-disk** (only new write = user-chosen recap PNG). 303 FE + 114 Rust unit + 5 integration green; `insights` 4-shot walkthrough (split to clear the 860px fold) + `m12` 15-shot matrix subagent-verified PASS (sole cross-screen change = new Insights nav item). Self-knowledge, not gamification (streak framed "days in a row"). |
| 19 | Power & Scale | ✅ Merged | [M19](docs/superpowers/plans/2026-06-13-audioshelf-m19-power-scale.md) | [#43](https://github.com/yovanmc/AudioShelf/pull/43) | All 8 sub-features shipped broad (17 serialized phases, 9 backend → 7 FE → 1 harness): **Ctrl+K command palette**, **advanced/scoped search** (one pure `query.rs::parse_query` `tag:`/`duration:<op><n>{s,m,h}`/`status:` DSL → `scoped.rs::run_scoped_query`, shared by search + saved searches + collections — DRY) + **saved searches**, **multi-select + additive bulk tagging** (never blanket-replace), **smart collections** route + Settings CRUD/reorder, **density toggle** (`comfortable` = exact current spacing), **per-work chapter-sort override** (DB-only, non-destructive, applied in `query_author_detail`), **curation export/import** (identity-keyed JSON `build_curation_export` + `VACUUM INTO` snapshot; import = **strictly additive merge**, never deletes), **read-only library-health scan** (orphan/0-byte/unreadable/schema-drift). **Schema v7** additive on the M16 runner (`saved_searches`/`smart_collections` tables + `works.chapter_sort`; `LATEST`→7, now a module const). **Crash-safe DB restore**: read-only `integrity_check`+`user_version≤7` probe → stage `restore_pending.db` (never touches live) → at bootstrap backup live to `.bak-<ts>` FIRST then place-then-rename (failed backup aborts; never destroys original). **No new dep** (only `dialog:allow-open` capability); **read-only-on-disk** (diff-audited — every fs write hits SQLite / staged-restore+backup / user-chosen path); **fixtures 43/44/47**. 310 FE + 138 Rust tests · `cargo tauri build --debug` full · `m19` 9-shot walkthrough + `m12` 15-shot regression subagent-verified PASS (sole cross-screen change = new Collections nav item). |
| 20 | Accessibility & Platform Integration | ✅ Merged | [M20](docs/superpowers/plans/2026-06-13-audioshelf-m20-accessibility-platform.md) | [#46](https://github.com/yovanmc/AudioShelf/pull/46) | **All 11 sub-features shipped (16 commits, 12 serial tasks).** `src/lib/a11y.ts` `parseA11yPrefs`/`a11yDataAttrs` → one `a11y_prefs` JSON setting applied as `data-theme`/`data-text-size`/`data-font`/`data-reduced-motion` on `.app-shell` (mirrors M19 density). **Light + high-contrast `[data-theme]` token blocks + `forced-colors`**; `[data-text-size]` `--font-scale` + dyslexia **system-Verdana** stack (asset-free) w/ looser letter/word/line spacing; reduced-motion (CSS-only, OS query + manual). Settings → Accessibility section. **Color-blind-safe status** (✓ check played / ○ ring unstarted, shape not color; `.visually-hidden` SR labels). Keyboard: skip-link, Dialog focus-trap+return-focus, global Space/Arrow shortcuts (Space NOT hijacked from focused buttons/links). SR semantics: browse `role=tree`, player `aria-live=polite` title region, landmarks. RTL/i18n: physical→logical CSS + `dir=auto` on user content (no framework). **Media Session API** (`navigator.mediaSession` metadata+handlers+position → Windows SMTC card + media keys, pure TS). **Focus mini-player** = always-on-top 2nd Tauri window (`open/close_mini_player`, `["main","miniplayer"]` capability), event-synced `playback:state`/`miniplayer:command`, `?miniplayer=1` branch in `main.tsx`; pop-out button in expanded Now Playing (sole new always-visible control). **NO schema migration (LATEST stays 7), NO new dep (`Cargo.*`/`package*.json` diff-stat empty — only `gen/schemas/capabilities.json` regen), read-only-on-disk (only new write = `a11y_prefs`), fixtures 43/44/47.** tsc clean · 383 FE · 139 Rust · `cargo tauri build --debug` full · `m20` 11-shot + `m12` 15-shot subagent-verified PASS (10/11 m20 visual; dyslexia by instrumentation+unit-tests). |
| 21 | Metadata & Discovery (narrator + per-audio) | ✅ Merged | [M21](docs/superpowers/plans/2026-06-13-audioshelf-m21-metadata-discovery.md) | [#51](https://github.com/yovanmc/AudioShelf/pull/51) | **SHIPPED 2026-06-13 (PR #51, 16 commits/15 tasks).** Schema **v8** additive (`metadata_terms` vocabulary + `chapter_metadata`/`author_metadata` attach tables; `LATEST` 7→8, first schema touch since M19; `SCHEMA_V1` untouched, no FK-off rebuild). New `metadata.rs` (fixed `FACETS=[narrator,language,mood]` + injection-safe `scope_table`). `MetaTag`/`MetaTerm` + `metadata` on `ChapterRow`/`WorkRow`/`AuthorDetail` (work aggregate = deduped union of its chapters' terms). Vocabulary CRUD + attach/detach; `query_author_detail` surfaces chapter+work+author metadata. Search DSL `narrator:`/`language:`/`mood:` via `parse_query`→`run_scoped_query` EXISTS clauses; `discovery_for_metadata` ("more by this narrator", author- or chapter-attached, **unplayed-only by design**). FE: vocabulary manager (Settings), reusable `MetadataEditor` (author header + per-chapter Edit-tags Dialog), first-class **Narrators** browse route (new `voice` icon + nav — sole `m12` cross-screen change), Discover facet picker, read-only `chip--meta` chips. **Invariants HELD:** no new dep (`Cargo.*`/`package*.json` diff empty), read-only-on-disk (all writes SQLite, no export, `lofty`/M16 ingestion untouched), fixtures 43/44/47 (all metadata seeded at runtime in `m21` walkthrough; `fixture_scan.rs` untouched). 389 FE + 152 Rust green; `m21` 5-shot + `m12` 15-shot subagent-verified against a **frozen `cargo tauri build --debug`** (`02-metadata-manager` renders below the captured fold — covered by `MetadataManagerView.test.tsx`). **Durable gotchas:** (1) a plain `cargo build`/`cargo test` of a Tauri app produces a **dev-mode exe** that loads `devUrl` (`localhost:1420`) → "localhost refused to connect" when launched without Vite; only `cargo tauri build --debug` embeds the frozen `dist`, and the `cargo test` gate **re-overwrites the exe in dev mode**, so re-do the frozen build before `verify.ps1 -SkipBuild` (or run verify without `-SkipBuild`). (2) `discovery_for_metadata` is unplayed-only, so the `m21` seed must attach the narrator to a work with an **unplayed** chapter, else the Narrators page shows empty once m12 marks the standalone played in the shared persisted DB (fixed by seeding onto a multi-chapter work). **Original scope (owner forks 2026-06-13):** Owner forks decided: **manual-only, NO embedded ingestion** (ignore file tags; only duration/size are file-derived); three configurable facets **narrator/language/mood** with **user-defined values** (create a value → apply to anything); applied to **files (chapters) + creators (authors)**, **works aggregate**; **both pillars broad** (browse + search + Discover). Architecture: additive **migration v8** (`metadata_terms` vocabulary + `chapter_metadata`/`author_metadata` attach tables; `LATEST` 7→8, first schema touch since M19) + faceted reuse of the M19 `parse_query`/`run_scoped_query` DSL (`narrator:`/`language:`/`mood:`) + new `discovery_for_metadata` ("more by this narrator") + Narrators browse route + Discover facet picker + vocabulary manager (Settings) + per-entity editor (Author Detail). 15 serial tasks. Invariants: no new dep, read-only-on-disk (all writes SQLite, no file export), fixtures 43/44/47. File-size capture explicitly DEFERRED/out-of-scope. **Scope set by owner 2026-06-13** (was "Spoken-Audio Rituals", then narrator-only; now TWO metadata systems that BOTH drive discovery across the app). **(1) Narrator metadata + "more by this narrator"** — narrator as a first-class browse/discovery dimension (today everything is author-centric). **(2) Per-audio metadata** — each audio (per-chapter/per-file; works aggregate) carries rich, queryable metadata that feeds discovery — **"if it doesn't already exist"**: AUDIT the M16 embedded-metadata ingestion (`lofty primary_tag`, `preview_metadata`/`apply_metadata`, "Import metadata" view) + the M9/M16 author/work/chapter **tag** systems FIRST and reuse them; only ADD the fields/flows that are genuinely missing (avoid rebuilding M16). **Both systems must surface in browse + search + Discover** (reuse `discovery_for_tags`/`scoped.rs run_scoped_query`/`parse_query` DSL — e.g. `narrator:`/metadata filters). **SCRAPPED** (owner): "What fits?" suggestions, wind-down/bedtime fade, "haven't returned in N days" nudges. **Open questions for planning:** narrator + per-audio DATA SOURCE (files may lack embedded tags; need embedded-tag ingestion preview→apply à la M16 AND/OR manual editors) and the exact metadata field set (narrator, genre/mood, year, description, …) — keep it queryable for discovery. Likely the **first schema touch since M19** (additive `narrator`/metadata columns or tag tables on the M16 `run_step`/`user_version` runner). Read-only-on-disk + no-new-dep (`lofty` already a dep) still hold. Validate buildable scope before planning. |

**Parked (needs an explicit product decision — NOT scheduled):** listen-later "queues of intent" (brushes the no-queue non-goal), opt-in daily reminder notification (outbound signal), and the still-deferred per-second mid-chapter resume + multi-root libraries. *(2026-06-13: the v5 backlog is now fully consumed — all 37 ideas shipped across M16–M21. Per-second resume is the strongest parked candidate and pairs naturally with M24; see v6 below.)*

### v6 — UX/UI Coherence  ✅ COMPLETE (M22–M25 shipped 2026-06-13; old M26 "Power, Calm" folded into M22/M23)

> **Owner redirect (2026-06-13):** with the v5 feature backlog exhausted, "the next few versions [should be] focused on user experience and ui exploration — the app should make sense, be easy to navigate, not be confusing, and be something people want to use." v6 turns inward: make the 21-milestone surface **coherent, calm, and obvious**, not add new feature areas. Produced by a **five-lens UX/UI subagent review** of the live post-M21 build (grounded in the `.shots/m12`+`.shots/m21` screenshots) → consolidated **actionable log**: [`v6 UX/UI backlog`](docs/superpowers/specs/2026-06-13-audioshelf-v6-ux-ui-backlog.md). Order = structure → words → core experience → polish (foundation-first, mirrors v5/M12). **Mostly FE-only**; only M24 likely touches schema. Invariants hold: read-only-on-disk, no new dep unless a milestone justifies one, fixtures 43/44/47, dark-first M12 system. Non-goals unchanged (no autoplay/up-next queue, no social). Validate scope at each milestone's turn.

| # | Title | Status | Plan | PR | Notes |
|---|-------|--------|------|----|----|
| 22 | Navigation & IA Coherence | ✅ Merged | [M22](docs/superpowers/plans/2026-06-13-audioshelf-m22-navigation-ia.md) | [#54](https://github.com/yovanmc/AudioShelf/pull/54) | **SHIPPED 2026-06-13 (PR #54, 6 commits/6 impl tasks).** *Foundation of v6; FE-only — `git diff --stat main` of `src-tauri`/`Cargo.*`/`package*.json` EMPTY, no schema/dep, read-only-on-disk, fixtures 43/44/47.* Flagged by ALL FIVE lenses. Grouped the 9-item flat sidebar into **Browse** / **My listening** (6 primary items); demoted one-off admin tools (**Rename**, **Import-metadata**) into **Settings → Library tools** (routes stay, back→Settings via new `onBack`; `shellRoute` maps rename/metadata→settings); **merged the redundant Narrators route into Discover** (deleted `NarratorsView`+test; narrator browse + chapter-counts preserved in Discover's per-facet labelled picker — props `*Options:string[]`→`*Terms:MetaTerm[]`; repointed m21 `narrators-browse` step via `listMetadataTerms()`→`pickFacet`, name kept so `runner.test.ts` stayed green); **surfaced the invisible power features** (Ctrl+K palette → sidebar Search affordance w/ `Ctrl K` kbd badge; raw `tag:cozy …` hint → plain-language + collapsible "Search tips"; `window.prompt` save-search → inline field); **sectioned the Settings wall** into Library/Display/Curation/Maintenance dividers. `tsc` clean · **394 FE** (NarratorsView's 2 removed; Discovery/Settings/Library added) · `cargo test` green & unchanged · `m12` 15-shot + `m21` 5-shot against a frozen `cargo tauri build --debug` → subagent verdict PASS on all IA/structure criteria. Re-audit caught: "Import tags" opened the M16 *embedded-metadata importer* (not the vocab manager — already in Settings); the multi-select **Select** button already existed in scoped mode (dropped from scope). |
| 23 | Clarity & Onboarding | ✅ Merged | [M23](docs/superpowers/plans/2026-06-13-audioshelf-m23-clarity-onboarding.md) | [#57](https://github.com/yovanmc/AudioShelf/pull/57) | **SHIPPED 2026-06-13 (PR #57, 8 commits/8 tasks).** *FE-only copy/clarity sweep; diff gate of `src-tauri`/`Cargo.*`/`package*`/`capabilities.json` EMPTY, no schema/dep, read-only-on-disk, fixtures 43/44/47.* (1) `Dialog` primitive gained optional visible `title`+`context` header (`useId` labelling, backward-compatible) + `.dialog__header/__title/__context` styles; (2) Home — single "Set up my library" CTA (CL-1) + zeroed stats grid hidden on first run via `!noHistory` (CL-2); (3) first-run reassurance copy on Settings+Scan ("never moves, renames, or changes them" + what-happens-next, CL-3 = copy-only); (4) context-aware empty-state rewrites (Discovery per tag/facet/for-you, Journal, Library, ScopedResults, Collections, CL-4); (5) de-jargon ("Metadata vocabulary"→"Narrator, Language & Mood", "canonical"→"standardized", "scoped query"→"filter"; also caught "Schema drift detected"→"Database version mismatch", CL-5); (6) visible dialog titles+context at every call site incl. Now Playing (CL-6); (7) tags-vs-metadata-vs-collections point-of-use `.field-hint` microcopy on Author Detail (CL-9). **Re-audit corrections confirmed:** CL-7 (`window.prompt` save-search) already shipped in M22 → verified gone & dropped (2 unrelated note/bookmark `window.prompt`s in App.tsx left as out-of-scope); CL-3 nav-gating already handled by the `standalone` first-run render → no dead nav-disable logic added. `tsc` clean · **395 FE** (+1 CL-2 stats-grid regression test) · `cargo test` **152 green & unchanged** · `home`/`settings`/`m12` (15)/`m21` (5) frozen-build → Sonnet subagent verdict **PASS** on CL-1/2/4/5/6/9 + no regression; CL-3 source-confirmed (walkthrough fixture is non-first-run so it can't reach the first-run copy — harness limitation, not a defect). |
| 24 | The Listening Loop & Player | ✅ Merged | [M24](docs/superpowers/plans/2026-06-13-audioshelf-m24-listening-loop-player.md) | [#60](https://github.com/yovanmc/AudioShelf/pull/60) | **SHIPPED 2026-06-13 (PR #60, 9 commits/12 tasks).** All 10 PL findings broad + per-second mid-chapter resume. **Schema v9** (first migration since M21's v8) — additive `chapters.playback_position_secs` on the M16 `run_step`/`user_version` runner (`LATEST` 8→9, `SCHEMA_V1` untouched, no FK-off rebuild). PL-1 speed (cycle btn + 0.75–2× segmented control, persisted `playback_speed` setting, re-applied on `onLoadedMetadata`) · PL-2 user-initiated chapter-end actions (Play-next / Mark-complete + More-by; **last-chapter detected by *position*, not `chapterNo`** — grouped works repeat 1,2,2,3,3) · PL-3 Start/Keep labels · PL-4 Now-playing/Played/New states · PL-5 elevated next-chapter title · PL-6 chapters btn + Library row play · PL-7 1s sleep countdown + "End of chapter" mode · PL-8 mute (`audio.muted`, slider preserved) · PL-9 no "−0:00" + scrubber hit-area · PL-10 "Pop out mini player" text btn. Resume saved throttled (~10s `onTimeUpdate` + pause flush), applied on chapter load (bookmark seek keeps precedence), zeroed by `mark_finished` — **never auto-starts** (no-autoplay intact). **Invariants HELD:** no new dep (Cargo/package diff-stat empty), read-only-on-disk (writes only settings/new column/play_events), fixtures 43/44/47 (`fixture_scan.rs` untouched). `tsc` clean · **401 FE** · `cargo test` **146 + integration** green · CI 2m53s · `m24` (7) + `m12` (15) + `m21` (5) frozen-build → Sonnet subagent verdict **PASS**. |
| 25 | Visual Polish & Design-System Consistency | ✅ Merged | [M25](docs/superpowers/plans/2026-06-13-audioshelf-m25-visual-polish.md) | [#63](https://github.com/yovanmc/AudioShelf/pull/63) | **SHIPPED 2026-06-13 (PR #63, 12 commits/12 tasks). The v6 capstone.** *FE-only — `git diff --stat main` of `src-tauri`/`Cargo.*`/`package*`/`capabilities.json` EMPTY; schema stays `LATEST`=9; read-only-on-disk; fixtures 43/44/47.* Shipped VIS-1…VIS-9 + CL-8: designed cover-art placeholders (music glyph @ ~50% behind initials, large `size>=64` slots only), accent discipline (recommendation/affinity → success teal; `#218bff` interactive-only), chip legibility (new `--color-chip-bg`/`-hover`/`-border` tokens in all theme blocks + solid `.chip--on`), expanded-player two-column redesign + row separators, eyebrow/heading consistency via `SectionHeading`, new styled `Select` primitive adopted in content selects only (Settings/Tag/Metadata natives kept), creator-header identity-band→divider→tags, saved-search single-row scroll strip, Rename-table polish. `tsc` clean · **421 FE** · `cargo test` **146 + integration** unchanged · CI 3m0s · m12/m21/m24/m25 frozen-build verified (verification-caught: glyph `.30`→`.5` & divider `--color-border`→`--color-border-strong`). |


### v7 — Discovery & Curation Coherence  ⚠️ PROVISIONAL (arc set by owner 2026-06-14; M26 ✅ shipped — foundation in place)

> **Owner redirect continues (2026-06-14):** v6's nav/clarity/player/visual pass shipped and **fully absorbed the original M26 "Power, Calm" residue** (admin-demote, bulk-select discovery, Ctrl+K affordance, Search tips, first-run guidance all landed in M22/M23), so M26 is **redefined**. A fresh **five-lens UX/UI review** of the live **post-M25** build (grounded in `.shots/m12`+`.shots/m21`+`.shots/m24`+`.shots/m25`) surfaced one dominant cross-lens theme: the app has **six overlapping ways to organize/find audio** (tags, fixed-facet metadata, series, collections, saved-searches, more-like-this) + **three disconnected discovery surfaces** — nobody can tell which to use. Full idea bank: [v7 backlog](docs/superpowers/specs/2026-06-14-audioshelf-v7-discovery-curation-backlog.md). **Owner decision:** collapse it to **one simple, user-defined "Types & Labels" system** (type a word → it's a filter everywhere + plain-search-matchable; Narrator & Language are built-in types, Mood is just a label; keep collections + more-like-this, all driven by the unified labels). Order = foundation-first (the labels system underpins everything). Invariants hold: read-only-on-disk, no new dep, fixtures 43/44/47, dark-first M12 system; M26 is the only v7 milestone expected to touch schema (additive v10). Validate scope at each milestone's turn.

| # | Title | Status | Plan | PR | Notes |
|---|-------|--------|------|----|----|
| 26 | Discovery & Curation Coherence (Unified Labels) | ✅ Merged | [M26](docs/superpowers/plans/2026-06-14-audioshelf-m26-unified-labels.md) | [#66](https://github.com/yovanmc/AudioShelf/pull/66) | **SHIPPED 2026-06-14 (PR #66, 13 commits/13 tasks).** Unified the free-form **tags** + M21 fixed-facet **metadata** into ONE user-defined **Types & Labels** system driving search/filter/Discover/collections/more-like-this. **Schema v10** additive on the M16 `run_step`/`user_version` runner (`LATEST` 9→10, `SCHEMA_V1` untouched, no FK-off rebuild, table-count +2): new `label_types` table (built-ins narrator/language/tag + mood deletable) + `work_metadata` work-level attach + **crash-safe COPY** of `author_tags`/`work_tags`/`chapter_tags` → `metadata_terms(facet='tag')`+attach via `INSERT OR IGNORE` — **legacy tag tables left dormant/recoverable, NOT dropped** (destructive-op discipline). Backend: DB-backed facet validation + label-type CRUD (`list/create/rename/delete[builtin-blocked]/reorder_label_types`); `scope_table` gains `work`; model gains `labels: Vec<MetaTag>` (all facets) with `tags`/`metadata` kept **derived**; work labels are **direct**; `search_library` bare term matches label **values**; `parse_query` generic `type:value` (keeps `tag:`/`narrator:`/`language:`/`mood:`); `run_scoped_query` EXISTS repointed at unified attach (work+chapter+author — fixed a latent missing `work_metadata` arm); discovery + more-like-this over unified attach. FE: **2 editors → `LabelEditor`**, **2 managers → `LabelManagerView`** (old Tag/Metadata managers deleted), Discover tag/facet pickers → **one labelled picker**, Library type→value filter (tag-facet effective client-side; cross-facet via plain search). **Invariants HELD:** no new dep (Cargo/package diff-stat empty), read-only-on-disk (zero new `std::fs` writes; all writes SQLite), fixtures 43/44/47 (`fixture_scan.rs` untouched). `tsc` clean · **464 FE** · `cargo test` **166 + integration** green (incl. new v10 additive-migration + `open_at_version_10` tests) · CI 2m57s · `m26`(6) + `m12`(15)/`m21`(5)/`m24`(7)/`m25`(3) frozen-build → Sonnet subagent verdict **PASS** (verification-caught + fixed: manager-card-below-fold → added `.label-manager` class + scroll; Library-filter empty-on-custom-type → shot now filters Tag→cozy for a non-empty result). |
| 27 | Reflection that Connects | ✅ Merged | [M27](docs/superpowers/plans/2026-06-14-audioshelf-m27-reflection-that-connects.md) | [#68](https://github.com/yovanmc/AudioShelf/pull/68) | **SHIPPED 2026-06-14 (PR #68, 12 commits).** Made the *records* actionable, **NO schema migration** (`db::LATEST` stays **10**). **CUR-2** journal note/bookmark → "▶ Play from <time>" (reuses in-memory `playChapterById` + `pendingSeekRef` precedence; no backend). **CUR-5** Insights heatmap-day/rhythm-week → focused **"Played · <range>"** list (new read-only `query_played_in_range` over `play_events`→existing `ScopedResults` in a thin `PlayedRangeView` under new `{kind:"played-range"}` route→shellRoute `insights`); top-tag bar → Library pre-filter via M26 `setLabelFilter`; local-day math via `DayCell.dateMs` + additive computed `weekStartMs` on `WeekPoint` (no DB change). **CUR-10** `discovery_for_you` prefers a rated/re-entered seed (rank re-entered+rated>rated>re-entered, tie-break work id) with `"Because you rated…/You came back to…"` reason (cold-start fallback preserved) + additive `worksRated`/`worksReEntered` "Reflections" stat in `compute_insights`; Collections untouched. **IA7-3** back-nav ("‹ Home") on Journal+Insights. **IA7-6** Collections→Browse group. **IA7-7** additive computed `has_journal` bool on `ChapterRow` (OR of `user_summary`/`takeaway`/`is_favorite`/EXISTS notes/EXISTS bookmarks, appended LAST in BOTH `load_chapter_row` + `query_author_detail` SELECTs) → journal glyph on annotated chapter rows opening the existing `ChapterJournalDialog`. **Invariants HELD:** db.rs/Cargo/package/fixture_scan diff-stat EMPTY, no new dep, read-only-on-disk, fixtures 43/44/47 (m27 walkthrough runtime-seeds notes/ratings/play_events idempotently). `tsc` clean · **486 FE** (+22) · `cargo test` **173 lib** (+7) + integration green · CI `build-and-test` 3m15s · `m27`(10) + `m12`(15)/`m21`(5)/`m24`(7)/`m25`(3)/`m26`(6) frozen-build → subagent + controller-direct verdict **PASS** on all 9 criteria. |
| 28 | Visual Consistency II | ✅ Merged | [M28](docs/superpowers/plans/2026-06-14-audioshelf-m28-visual-consistency-ii.md) | [#71](https://github.com/yovanmc/AudioShelf/pull/71) | **SHIPPED 2026-06-14 (PR #71, 9 commits/T1–T10).** FE-only v7 visual-consistency capstone (VIS7-1…10). Systemic **border-strength rule**: new per-theme **`--color-divider`** (dark→`border-strong`, light→`border`, HC→`border-strong`) applied to 18 structural borders (sidebar/search/kbd/list-rows/player-bar, inputs, cards, data-table rows, tabs, palette, bulk-bar, collection-row, settings divider, now-playing rows, speed controls) — they now READ on dark; `--color-border-strong` stays the emphasis tier (work-card hover). Also: chip-gaps → `--chip-gap` 8px; dialog context 0.85→0.9rem + margin; work-card hover lift −3→−4px + `--shadow-card-hover` (HC=`none`, border-driven); UI icons → `--icon-sm/md/lg` (M25 %-of-tile artwork glyph documented exempt); avatar-initials `text-shadow`; expanded-player gap → `clamp()` (both `.now-playing__layout` rules, layout.css wins cascade). **T3 secondary-text audit = NO-OP** (no ad-hoc text-opacity remained; already on single `--color-text-muted` tier). New `m28` walkthrough (6 surfaces). **Invariants HELD:** FE-only diff-stat EMPTY (no Rust/schema/dep/capability), schema `LATEST`=10, read-only-on-disk, fixtures 43/44/47, every token in all 3 theme blocks. `tsc` clean · **487 FE** (+1 m28Steps) · `cargo test` green & unchanged · CI `build-and-test` 4m42s · `m28`(6)+`m25`(3)+`m12`(15) frozen-build → Sonnet subagent verdict **PASS** + **controller direct-review** of 3 borderline border/contrast shots (data-table dividers / dialog context / card grid) all PASS (large m12 pixel-diffs = M26/M27 baseline drift, not regressions). |
| 29 | Player & Onboarding micro-polish | ✅ Merged | [M29](docs/superpowers/plans/2026-06-14-audioshelf-m29-player-onboarding-polish.md) | [#74](https://github.com/yovanmc/AudioShelf/pull/74) | **✅ SHIPPED 2026-06-14 — v7 (M26–M29) COMPLETE. The v7 closer (M28 already shipped, so M29 stood alone).** FE-only catch-all, all 22 backlog items broad: scrubber always-visible thumb + scrub-preview bubble & resume cue (PL7-1/2), consistent speed/mute/mini-skip (PL7-4/5/6), "Play next — [title]" + chapter-end cross-fade (PL7-7/3), sleep end-of-chapter preview + prominent countdown (PL7-8/10), keyboard-shortcut hints (PL7-9), warmer Home shelf headers + jargon-free empty copy (ON-2/3/5), **gate "My listening" sidebar group until history exists (ON-1)**, scan reassurance + next-steps CTA (ON-7/8), guiding empty-Discover (ON-6), Settings anchor sub-nav + actionable first-run hint (ON-4/9, IA7-4), collapsed-sidebar caption labels (IA7-5), palette section headers (IA7-9). 16 serial tasks (FE-only). |

### v8 — Real-Scale Hardening  ✅ COMPLETE (arc set by owner 2026-06-14; M30 ✅ · M31 ✅ · M32 ✅ · M33 ✅ (Remove transcripts & simplify search; FTS5 dropped) · M34 ✅ — Rendering & Memory at Scale, the v8 closer)

> **Owner direction (2026-06-14):** with v1–v7 (M1–M29) complete and the in-app experience coherent, v8 turns to **making AudioShelf dependable on a real 10k+ file library** — today it's only ever exercised on the 43/44/47 silent-WAV fixtures, so every scale cost (un-batched scan writes, missing indices, non-virtualized lists, full in-memory loads) is invisible. Three Explore digests of the live build mapped the risks across three layers (scan / DB queries / FE rendering); full findings in the M30 plan + decision log. **Two owner forks:** (1) **lead with scan ROBUSTNESS** (incremental skip, deletion detection, progress + cancel, error tolerance), not raw throughput; (2) fixture = **silent-WAV structural** (deterministic, CI-friendly, parameterized to thousands), not heavy realistic media. **Provisional arc** (foundation-first): M30 robust/observable/incremental scan + scale-test foundation → M31 scan & write throughput (O(n²) fix, batched statements, parallelism — needs a `rayon` no-new-dep waiver) → M32 query & index performance (missing indices, `query_authors`→GROUP BY, bound `compute_insights`, FTS5, WAL/cache/busy_timeout PRAGMAs) → M33 rendering & memory at scale (virtualize Home shelves / AuthorDetail chapters / search / journal / collections / LabelManager; preload; result caps). Invariants hold: read-only-on-disk (deletion = soft-delete `status` flip, recoverable, never `fs::remove`), no new dep (rayon stays out until M31), default fixture stays 43/44/47, additive migrations on the M16 runner. Validate scope at each milestone's turn.

| # | Title | Status | Plan | PR | Notes |
|---|-------|--------|------|----|----|
| 34 | Rendering & Memory at Scale | ✅ Merged | [M34](docs/superpowers/plans/2026-06-14-audioshelf-m34-rendering-memory-scale.md) | [#89](https://github.com/yovanmc/AudioShelf/pull/89) | **✅ SHIPPED 2026-06-14 (PR #89, CI 4m0s green; 22 files, +1526/−143). Gates: tsc clean + 520 vitest (baseline 492 → +28 new, none dropped); invariant audit ALL held — `git diff --stat src-tauri/` EMPTY (FE-only), dep-files diff EMPTY (no new dep), `db::LATEST`=13, no new write `invoke` (read-only intact), `fixture_scan.rs` untouched (43/44/47); frozen-build m34 + regression m12/journal/m24 → Sonnet subagent text verdict PASS (virtualization PROVEN: m34 scrolled shot recycles to entirely different entries vs top shot; all touched surfaces — Home/shelves/author-detail/search/discovery/journal/now-playing — structurally intact at fixture scale via the below-threshold path).** Built per plan via 4 Sonnet subagents (A foundation → B Journal+AuthorDetail → C Home+bounded → D walkthrough) + 1 screenshot subagent. Shipped: `VirtualList.tsx` (Fixed/VariableSizeList wrapper, `resetAfterIndex` on item-set change, `VIRTUALIZE_THRESHOLD=40`) + pure `flattenRows.ts` (`flattenJournal`/`flattenAuthorDetail`/`ROW_H`). HIGH full-virtualization-above-threshold: Journal, AuthorDetail works+chapters (collapse = pure recompute of the flat array), Home vertical shelf-stack (`SHELF_ROW_HEIGHT=280`). Bounded caps: Home shelf items `CAP_SHELF_ITEMS=20` + "+N more" (creator shelf → author detail; tag/status/dormant shelves had NO nav target → static "+N more" label), search buckets gate+VirtualList, LabelManager `<table>`s `LABEL_TABLE_PAGE=100` cap + "show more" (NOT react-window — keeps table layout), ScopedResults honest "showing first 50" note, Discovery `CAP_FACET_CHIPS=24` + toggle. New `m34` walkthrough runtime-seeds 62 journal entries (m27 precedent) → captures virtualized+scrolled journal. **As-built deviations (durable):** (1) real types live in `src/lib/api.ts` NOT `src/types.ts` (`JournalEntry`/`WorkRow{id,baseTitle,chapters}`/`ChapterRow{id,title,...}`); journal grouped shape is `Map<authorName, Map<workTitle, JournalEntry[]>>`. (2) AuthorDetail chapter rows ported `<li className="recent-row">`→`<div className="recent-row">` in the virtualized branch only — `.recent-row` CSS is element-agnostic so NO new CSS needed; below-threshold keeps original `<ul>/<li>`. (3) journal entry text got a fresh `.journal-entry__text` `-webkit-line-clamp:2` clamp applied to BOTH paths (benign on short fixture text). (4) m34 `journal-virtualized` step is a no-op capture (journal already shown by `seed-journal`) → shots 01≈02 identical (harmless; proof is 02 vs 03). (5) `.shots` is gitignored so the pixel-diff "CHANGED" flags are stale-baseline drift (untouched surfaces like settings/player also flagged) — NOT regressions; authoritative gate = subagent visual verdict. (6) running m34 (seeds 62 journal entries into the PERSISTENT app-data DB) before the `journal` walkthrough left leftovers visible in `journal/01-journal-empty` — verification-run-ordering artifact (DB persists across runs), not code. **v8 (M30–M34) COMPLETE.** ~~**📝 PLAN READY — the v8 closer (execute next).**~~ FE-only render-virtualization + result caps so a 10k+ library doesn't render thousands of DOM nodes; **no backend / no schema (`db::LATEST` stays 13) / no new dep (`react-window@^1.8.10` already present) / read-only / fixtures 43/44/47.** Owner decisions (batched AskUserQuestion): (1) **all six surfaces, proportionate** — full virtualization for the unbounded HIGH ones (Home shelves, AuthorDetail chapters, Journal), caps + virtualize-above-threshold for the bounded ones (search/collections/LabelManager/discovery); (2) **FE-only** (digest proved JS arrays are ~1–10 MB even at 10k — the bottleneck is DOM nodes, not memory; backend data-windowing rejected — would break M10 in-memory client sort/filter for marginal gain); (3) **Home shelves = virtualize the vertical stack + cap items/shelf (~20) with "+N more"** (no fragile horizontal-carousel virtualization). **The load-bearing safety property = NO regression at fixture scale:** every virtualized surface uses an external `length > VIRTUALIZE_THRESHOLD(40)` gate → renders the existing markup unchanged below threshold (so all existing tests + m12/journal/m24 screenshots stay green with no edits) and only switches to the new `<VirtualList>` (react-window FixedSize/VariableSize wrapper) above it. New: reusable `src/components/VirtualList.tsx` (positioned-div rows; VariableSizeList `resetAfterIndex` on item-set change) + pure `src/lib/flattenRows.ts` (`flattenJournal`/`flattenAuthorDetail` flatten the grouped trees → flat row arrays honoring collapse) + unit tests proving DOM windowing. LabelManager `<table>`s use cap + "show more" (NOT react-window — `<tr>`-in-positioned-`<div>` breaks tables); ScopedResults gets an honest "showing first 50 of N" note. New `m34` walkthrough runtime-seeds ≥60 journal entries (m27 precedent) to screenshot the virtualized+scrolled journal; AuthorDetail/label virtualization proven by unit tests (chapters can't be runtime-seeded read-only). Verify = tsc + vitest (count must not drop) + invariant audit (`src-tauri` diff EMPTY) + frozen-build m34 + regression m12/journal/m24 → Sonnet subagent text verdict. **NEXT: execute M34 → v8 (M30–M34) COMPLETE.** |
| 33 | Remove Transcripts & Simplify Search | ✅ Merged | [M33](docs/superpowers/plans/2026-06-14-audioshelf-m33-remove-transcripts.md) | [#86](https://github.com/yovanmc/AudioShelf/pull/86) | **✅ SHIPPED 2026-06-14 (PR #86, CI 3m8s green; 13 files, −672/+64 = a deletion milestone). Gates: cargo test 169 lib (−16 transcript tests) + integration green, `fixture_scan` 43/44/47, `migration_v13_drops_transcripts` proves the drop; tsc clean + 492 vitest (−7); dep diff-stat EMPTY (no new dep); read-only intact (no added `fs::`); dead-ref sweep clean (zero `transcript` in `src`, only the intentional v5-creates/v13-drops chain + boundary/kept-scan-fixture refs in db.rs/scan.rs); m7+m24 frozen-build screenshots PASS (search = authors/works/chapters, no Transcripts bucket; now-playing has no Transcript section).** Built per plan via 2 Sonnet removal subagents (Rust T1–3, FE T4–5) + 1 screenshot subagent. Schema v12→**v13** (`migration_v13_drop_transcripts` = `DROP TABLE IF EXISTS transcripts` on the M16 runner; safe — nothing FK-references it, content re-derivable from on-disk sidecars). Deleted `transcripts.rs`/`search_transcripts`/`get_chapter_transcript`/`ingest_sidecar_transcript` + FE wiring/bucket/panel/walkthrough-step. Non-transcript search (`search_library`/`SearchResults`/`run_scoped_query`) BYTE-IDENTICAL → author/work/chapter buckets unchanged; M9/M26 label/tag-value matching KEPT. **As-built deviations (durable):** (1) `make_snippet` + its test `snippet_is_centered_on_match` were tied to `search_transcripts_inner` → removed too (so the net is −16 lib not −16-from-185; new count **169**). (2) `cargo test` caught **3 extra `assert_eq!(…,12)` "reaches latest" asserts in commands.rs** beyond the plan's list + the neighboring `upgrade_from_v4` test's `post==12`/`has_transcripts==1` pair → bumped to 13 / flipped to `==0` (the M30/M32 "grep-and-let-cargo-test-confirm" lesson held). (3) FE transcript `useEffect` removal left a stray `}, [current?.chapter.id]);` → caught by tsc (TS1128), removed. (4) m16 has no now-playing step → now-playing surface verified via **m24** (`03-03-now-playing.png`), the correct surface. ~~**Owner pivot (2026-06-14) — SUPERSEDES the planned "FTS5 full-text search."**~~ Owner: completely remove the transcripts feature + "search should purely be off the title of the files, lists, or names of the creators." **FTS5 DROPPED** — its only meaningful payload was the large transcript text; with transcripts gone, search runs purely over short fields (M32 measured `searchMs` ≈4–7ms at 12k), so FTS5 would add machinery + virtual tables + triggers for **no measurable win**. So M33 = a **deletion** milestone + one safe destructive schema step. **Decisions (4 batched AskUserQuestion, owner-confirmed):** (1) drop FTS5, keep the existing fast substring `LIKE` search; (2) **drop the `transcripts` table** via a **v13 migration** (`DROP TABLE IF EXISTS transcripts` on the M16 `run_step` runner, `LATEST` 12→13 — safe: nothing FK-references it, content re-derivable from on-disk sidecars, no user data lost); (3) **KEEP** label/tag-value matching in `search_library` (the M9/M26 unified-labels `EXISTS` discovery affordance — typing `cozy` still finds tagged works; unrelated to transcripts). **Removal surface (exhaustive inventory in the plan):** delete `transcripts.rs` (`parse_srt_vtt`+8 tests); scan.rs `ingest_sidecar_transcript`+call site+3 tests (KEEP `srt_and_vtt_files_alone_do_not_add_chapters`); commands.rs `search_transcripts`/`get_chapter_transcript`(+inner+`TranscriptHit`+5 tests); lib.rs `mod transcripts`+2 handler regs+2 testing re-exports; FE `searchTranscripts`/`getChapterTranscript`/`TranscriptHit`, App.tsx state+effect+parallel-search→`searchLibrary` only, LibraryView Transcripts bucket, NowPlayingPanel Transcript section, dead CSS, `m16` `transcript-search` walkthrough step + runner.test. **Non-transcript search shapes (`SearchResults`/`AuthorHit`/`WorkHit`/`ChapterHit`) + scoped `run_scoped_query` byte-identical → author/work/chapter buckets render unchanged.** **Invariants:** no new dep (only removals), read-only-on-disk (removes a read+derive feature), fixtures **43/44/47** (no `.srt`/`.vtt` in fixture tree — transcripts were runtime-seeded), v12→**v13** on the M16 runner. Gotcha baked in: bump every `==12`/`open_at_version(12)`/schema-version `"12"` assert in BOTH db.rs AND commands.rs to 13 (`cargo test` is the catch); rename `schema_creates_all_tables_including_transcripts` (transcripts gone at LATEST) + new `migration_v13_drops_transcripts` test. FE surfaces changed (search panel −1 bucket, now-playing −1 section) → m7+m16 frozen-build screenshot via Sonnet subagent text verdict. **NEXT: execute M33.** |
| 32 | Query & Index Performance | ✅ Merged | [M32](docs/superpowers/plans/2026-06-14-audioshelf-m32-query-index-performance.md) | [#83](https://github.com/yovanmc/AudioShelf/pull/83) | **✅ SHIPPED 2026-06-14 (PR #83, CI 3m49s green; Rust 184 lib + integration green, FE untouched — tsc clean + 499 vitest, `src/` diff EMPTY).** Backend-only read-query perf shipped per plan. **A/B (interleaved, 12k chapters / 3000 play_events; observational not a CI gate, genMs ~37–43s both runs = stable env): `insightsMs` 3394→11–15 (~225–300×), `queryAuthorsMs` 32→7–11 (~3–4×), `searchMs` 7→4–7 unchanged (expected — `search_library` untouched).** EXPLAIN QUERY PLAN test is the durable proof `idx_chapters_work` is used. **4 additive low-risk levers** (FTS5 + `search_library` SQL **deferred to a dedicated next milestone** per owner 2026-06-14; `compute_insights` = index+bound, Rust math kept): (1) **v12 migration** adds the **only two genuinely-missing indices** — covering `idx_chapters_work ON chapters(work_id,status,played,duration_secs)` + FK `idx_play_events_chapter ON play_events(chapter_id)`. **Scope correction (mirrors M31's "already done"):** `works(author_id)` is already covered by `UNIQUE(author_id,base_title)`, `metadata_terms(facet,value)` by its own `UNIQUE(facet,value)`, and `chapter/author/work_metadata` owner-id lookups by their composite PKs → those four are **NOT** added (redundant, would slow the M30/M31 scan-write path). (2) `query_authors` 4 correlated subqueries → single `GROUP BY` (status filters in JOIN-ON to keep LEFT-JOIN semantics; `COUNT(DISTINCT w.id)` guards fan-out). (3) `compute_insights` per-work 2-subquery block → `GROUP BY` over `idx_chapters_work` (event-scan + `build_insights` Rust date/streak/heatmap math untouched). (4) query PRAGMAs `cache_size=-16384`/`temp_store=MEMORY`/`mmap_size=128MB` in file-backed `open()` only. **Backend-only: ZERO FE change** (`AuthorRow`/`InsightsData`/command shapes byte-identical → no screenshot walkthrough, M31 precedent). New: `measure_queries_at_scale` (`QUERY-METRICS` JSON) + first `EXPLAIN QUERY PLAN` test (proves `idx_chapters_work` is used) + multi-work fan-out equivalence test. **Invariants:** no new dep (FTS5 deferred → bundled SQLite suffices, no rayon), schema v11→**v12** additive on the M16 runner, read-only-on-disk, fixtures 43/44/47. Perf = observational interleaved A/B (M31 noisy-machine lesson), not a CI gate. |
| 31 | Scan & Write Throughput | ✅ Merged | [M31](docs/superpowers/plans/2026-06-14-audioshelf-m31-scan-write-throughput.md) | [#80](https://github.com/yovanmc/AudioShelf/pull/80) | **✅ SHIPPED 2026-06-14 (PR #80, 5 commits; Rust 179 lib + integration green; CI 2m59s).** Backend-only scan write-path perf, `src-tauri/src/scan.rs` only — **no new dep / no schema (`db::LATEST` stays 11) / read-only-on-disk / fixtures 43/44/47**. Landed: `conn.prepare_cached(...)` on every hot per-row statement in `scan_author()` (works/chapters upsert, existing-row probe, skip UPDATE) + the transcript sidecar INSERT (SQL compiles once per connection, reused from rusqlite's statement cache); `RETURNING id` on the work + chapter upserts dropped the two trailing `SELECT id` round-trips (`upsert_chapter` now returns `i64`). All inside M30's per-author transaction + behind M30's incremental skip (behavior identical). Guard test `changed_file_counts_as_update_and_preserves_chapter_identity` locks update-path row identity (green before & after). **Perf (fair back-to-back A/B at 12k chapters, since the machine degraded mid-session — main's own baseline drifted 6233→10075ms so start-vs-end is invalid): cold scan ~21–45% faster (branch 5539/5557ms vs main 10075/7013ms), rescan ~8–16% faster, and branch is markedly more STABLE.** Measurement observational per plan, not a CI gate. **Scope corrected at plan time:** the original "O(n²) `files.iter().find()` fix" is **already done** — M30 replaced it with a `by_stem` HashMap ([scan.rs:218–246](src-tauri/src/scan.rs)) doing O(1) chapter→file lookup; dropped from scope. Remaining levers = (1) `conn.prepare_cached(...)` on every hot per-row statement in `scan_author()` (works/chapters upsert, existing-row probe, skip UPDATE, transcript INSERT) so each SQL compiles once instead of per row; (2) append `RETURNING id` to the work + chapter upserts to eliminate the trailing `SELECT id` round-trips (`upsert_chapter` now returns `i64`). All inside M30's per-author transaction (atomicity kept) and behind M30's incremental skip (unchanged). **Parallelism deferred** (owner declined the rayon waiver 2026-06-14): the `lofty` probe stays single-threaded; when parallelism lands in a later v8 milestone it must use `std::thread::scope` (no dep) and can only cover the read-only probe phase (`rusqlite::Connection` isn't `Sync`). Cross-author commit batching **rejected** (would weaken M30 crash-safety; WAL already makes per-author commits cheap). Proof = before/after `SCALE-METRICS` from `verify.ps1 -Measure` on one machine (12k chapters); guard test `changed_file_counts_as_update_and_preserves_chapter_identity` locks update-path identity. No FE/command-signature change → screenshot walkthrough not required. |
| 30 | Robust, Observable, Incremental Scan + Scale-Test Foundation | ✅ Merged | [M30](docs/superpowers/plans/2026-06-14-audioshelf-m30-robust-incremental-scan.md) | [#77](https://github.com/yovanmc/AudioShelf/pull/77) | **✅ SHIPPED 2026-06-14 (PR #77, 9 commits; Rust lib 178 + FE 499 tests green; CI 3m17s).** v8 foundation. Baseline **`SCALE-METRICS {"chapters":12000,"scanMs":~6400,"rescanMs":~4900,"rescanSkipped":12000}`** (12k silent-WAV chapters; rescan skips all 12000). Schema **v11** additive (chapters `file_mtime`/`file_size`/`last_seen_scan` + `idx_chapters_last_seen`) on the M16 runner. Refactor `scan_into`→`scan_into_with(conn,root,&ScanOpts)` keeping the `scan_into(conn,root)` wrapper so existing scan.rs tests + `fixture_scan.rs` (43/44/47) stay green. Robustness: per-author **transaction** (crash/cancel atomicity), mtime+size **incremental skip** (no `lofty` probe on unchanged), **deletion detection** via a `scan_generation` settings counter stamping `last_seen_scan`→sweep `status='inactive'` + cascade (**soft-delete, recoverable, never hard-delete**), per-file **error capture** (`ScanError{path,reason}`, never abort; replaces silent `.flatten()`), **progress events** (`app.emit("scan:progress")` + `tauri::Emitter`) + **cancellation** (`ScanControl(Arc<AtomicBool>)` + `cancel_scan` touching only the flag, not `DbState`). `ScanResult` gains added/updated/removed/skipped + errors → scan-diff summary in `ScanView`. Foundation: `gen-fixture::generate_scaled(root,authors,works_per,chapters_per)` (keep `generate` unchanged) + `verify.ps1 -Measure` times scan/rescan at scale → `SCALE-METRICS` baseline. Robustness-grade PRAGMAs (WAL/synchronous=NORMAL/busy_timeout) in `open()` only. No new dep; read-only-on-disk; default fixture 43/44/47. |

### v9 — Real-World Readiness  ⚠️ PROVISIONAL (arc set by owner 2026-06-15; M35 ✅ Merged)

> **Owner direction (2026-06-15):** with v1–v8 (M1–M34) complete — feature-rich, coherent, and proven to *handle* a 10k+ library in tests — the conspicuous gap is that the audio pipeline has **only ever been exercised on synthetic silent-WAV fixtures and non-decodable `b"x"` stub bytes**; a real encoded file (mp3/m4a/mp4/flac/ogg) has never been scanned, probed, or played. v9 turns to **real-world readiness**, starting with audio-format confidence. **Owner forks (batched AskUserQuestion 2026-06-15):** (1) scope = **real-media robustness for the 6 owner-blessed formats** `mp3/m4a/mp4/flac/wav/ogg` (all already in the scan allowlist + all WebView-decodable → **no new decoder dep, no allowlist change**); (2) **`.m4b` declined** (stays skipped); (3) **honest inline playback error** for corrupt/undecodable files (not silent); (4) **commit tiny real encoded samples** for CI proof (vs today's silent-WAV stubs). M35 is a **single standalone milestone**, not a multi-milestone arc — follow-on work (m4b, more formats, embedded-metadata-at-scan, packaging/installer/auto-update) is deferred and decided later. Invariants hold: no new dep (`lofty`/`image`/`hound` already present; samples are test fixtures not deps), read-only-on-disk, default fixtures 43/44/47 unchanged, schema `db::LATEST` stays **13**, dark-first M12 system.

| # | Title | Status | Plan | PR | Notes |
|---|-------|--------|------|----|----|
| 35 | Real-Media Confidence | ✅ Merged | [M35](docs/superpowers/plans/2026-06-15-audioshelf-m35-real-media-confidence.md) | [#92](https://github.com/yovanmc/AudioShelf/pull/92) | **✅ SHIPPED 2026-06-15 (PR #92, CI 3m12s green; cargo 169 lib + integration incl. 4 new `real_media` green, tsc clean, 523 vitest +5).** Starts v9. The 6 formats `mp3/m4a/mp4/flac/wav/ogg` are now **proven on real encoded files**, with honest inline failure when a file can't be played — replacing silent-WAV/`b"x"`-stub-only coverage. **First real-file pass found NO decode bug** (real FLAC plays with a real 0:02 duration in the WebView, NO MIME fix needed — Step 5 skipped). Landed exactly per plan via 5 Sonnet subagents: (1) **8 tiny real clips** (`src-tauri/tests/media/Real Formats/`, ~280KB total; one per format + art-bearing APIC mp3 + 10-byte corrupt mp3) generated once via local `ffmpeg` (reproducible `tools/gen-media-samples.ps1`; ffmpeg is **build-time, NOT a dep**); (2) `real_media.rs` **4 integration tests** — each real format probes a **nonzero** duration via `lofty`, embedded art extracts+thumbnails via `covers`, corrupt file **ingests with duration 0** (browsable, no panic); (3) `ScanResult.unknown_duration` (Rust `usize` + serde-default → TS `unknownDuration?`) computed in `finish_result` (`COUNT(*) WHERE duration_secs=0 AND status='active'`), surfaced in `ScanView` scan-diff ("… · N unknown length"); (4) **honest playback error** — pure `playbackErrorText(title)` helper → App `playbackError` state via `<audio onError>` → inline "…couldn't be played" in `PlayerBar` (reuses `--color-danger`), cleared on next successful load; (5) `m35` walkthrough — `verify.ps1` points `--library` at the real samples (skips gen-fixture) → plays real clip + triggers corrupt-file error → Sonnet **visual verdict PASS** (m24/m12 regression intact). **Invariants ALL held:** dep diff-stat (`Cargo.*`/`package*`) **EMPTY**, `db::LATEST`=**13**, read-only-on-disk, gen-fixture/`fixture_scan.rs` (43/44/47) untouched. Spec: [design](docs/superpowers/specs/2026-06-15-audioshelf-m35-real-media-confidence-design.md). **Deliverables:** (1) commit ~8 tiny (<50KB) **real** clips via the locally-available `ffmpeg` (one per format + one art-bearing + one corrupt) in a separate `src-tauri/tests/media/` dir (the default 43/44/47 fixture is untouched) → new Rust integration test `real_media.rs` asserts each probes a **nonzero** duration, format stored, embedded art extracts via `covers.rs`, corrupt file **ingested with duration 0** (lofty error → 0, browsable, no panic) — 4 tests; (2) **playback error state** (FE) — pure `playbackErrorText(title)` helper + `<audio onError>` → App `playbackError` state → inline "…couldn't be played" in `PlayerBar` (reuses `--color-danger`), cleared on next successful load; (3) `ScanResult.unknownDuration` diagnostic (computed in `finish_result` via `COUNT(*) WHERE duration_secs=0 AND status='active'`) surfaced in `ScanView` scan-diff; (4) `m35` walkthrough — `verify.ps1` points `--library` at the real-sample dir (skips gen-fixture) → plays a real clip (progress advancing) + triggers the corrupt-file error → Sonnet subagent text verdict. **Real-format MIME/duration fix only if the walkthrough proves a decode failure** (player already trusts live `<audio>.duration`, so likely a no-op). **Invariants:** Cargo/package diff-stat EMPTY (ffmpeg is build-time, samples are fixtures), `db::LATEST`=13, read-only-on-disk, gen-fixture/`fixture_scan.rs` untouched (43/44/47). |

## Decision log & gotchas

- **M35 SHIPPED (2026-06-15 — PR [#92](https://github.com/yovanmc/AudioShelf/pull/92), CI 3m12s green) — Real-Media Confidence; STARTS v9 (Real-World Readiness).** Executed exactly per plan via 5 Sonnet subagents (one per task) + 1 frozen-build visual-verdict subagent; controller did the frozen build, walkthroughs, invariant audit, PR/CI/merge. **The headline finding: the first-ever real encoded file pass found NO runtime bug** — real `mp3/m4a/mp4/flac/wav/ogg` clips scan with a real `lofty`-probed duration, embedded APIC art extracts+thumbnails, and a real FLAC **plays in the WebView with a real 0:02 duration and no error** → the speculative real-format MIME/Content-Type fix (plan Task 6 Step 5) was correctly **skipped** (player already trusts live `<audio>.duration`). So M35 validated that the pipeline was already capable; the durable value is the **permanent real-media test corpus + integration tests + the honest playback-error feature**. **Landed:** (1) 8 tiny real clips committed under `src-tauri/tests/media/Real Formats/` (~280KB total — note the PCM **WAV is 176KB**, above the plan's optimistic <50KB estimate because PCM is uncompressed; the 5 compressed clips are 6–32KB, corrupt is 10 bytes) generated via local `ffmpeg` (`tools/gen-media-samples.ps1`; **ffmpeg is build-time, not a runtime dep**); (2) `real_media.rs` 4 integration tests (nonzero-duration per format, corrupt→duration-0 no-panic, art extract+thumbnail, unknown-duration count); (3) `ScanResult.unknown_duration` `usize`+`#[serde(default)]` → TS `unknownDuration?` rendered in `ScanView` scan-diff; (4) honest playback error (`playbackErrorText` pure helper → App `playbackError` via `<audio onError>` → `PlayerBar` inline danger text, cleared on next load); (5) `m35` walkthrough (verify.ps1 points `--library` at the real samples + skips gen-fixture). **Gates:** cargo **169 lib** + integration (incl. 4 `real_media`) all green, `tsc` clean, **523** vitest (+5); visual verdict **PASS** (real-format-playing shows 0:02 no-error; corrupt shows red "…couldn't be played"; m24/m12 regression intact). **Invariants ALL held:** dep diff-stat (`Cargo.*`/`package*`) **EMPTY**, `db::LATEST`=**13**, read-only-on-disk, gen-fixture/`fixture_scan.rs` (43/44/47) untouched. **Durable gotchas:** (1) the real playback files live in **`src/player/playback.ts`**, NOT `src/lib/playback.ts` (the plan guessed `src/lib/`); the subagent adapted. (2) App walkthrough arms open an author via **`openAuthor(id)`** (not `openAuthorFromDetail(detail)`) and construct the `PlaybackContext` **inline** (no `buildContext` helper) — future walkthrough work must reuse those exact shapes. (3) **build via the PowerShell tool with `Set-Location` to the repo + `& "...\tools\dev-env.cmd" <cmd>`** — the Bash-tool `cmd /c "tools\dev-env.cmd …"` form no-ops (just opens a prompt) AND PowerShell cwd resets to `C:\Agent Zone` every call so `Set-Location` is required each time; `dev-env.cmd` prints a harmless `'vswhere.exe' is not recognized` line but MSVC linking still succeeds (`audioshelf.exe` builds). (4) `.shots` is gitignored so the verify.ps1 pixel-diff flags EVERY regression shot "CHANGED" (~0.15–0.47) — that's stale-baseline drift, NOT a regression; the Sonnet **visual verdict** is the gate. Plan: [`docs/superpowers/plans/2026-06-15-audioshelf-m35-real-media-confidence.md`](docs/superpowers/plans/2026-06-15-audioshelf-m35-real-media-confidence.md). **NEXT: re-scope v9 follow-on (m4b / more formats / embedded-metadata-at-scan / packaging-installer-auto-update — all deferred at M35 planning) and plan the next milestone.**
- **M35 PLANNED (2026-06-15 — `📝 Plan ready`, execute next) — Real-Media Confidence; starts v9 (Real-World Readiness).** Brainstormed + planned via `/roadmap` Phase A. **Honest scope finding (owner validated):** the 6 target formats are *already* in the scan allowlist and *already* WebView-decodable, and `lofty`/`image` already probe duration + extract art — so the runtime code is largely capable; the real gap is that **no real encoded file has ever run through scan or playback** (only silent-WAV + `b"x"` stubs). So M35 is mostly **test media + integration tests** + one small new FE feature (the playback error state) + **whatever real bugs the first real-file pass surfaces** (the actual value). Single modest milestone, NOT a version arc. **Key decisions:** `.m4b` declined (allowlist unchanged); commit real samples (ffmpeg verified available locally — `aac`/`flac`/`libmp3lame`/`libvorbis` encoders present — but it's a **build-time tool, not a runtime dep**); corrupt-but-readable file → duration 0 (browsable, honest playback failure) NOT a hard `ScanError`. **Gotchas baked into the plan:** (1) FROZEN build (`npm run build` → `cargo tauri build --debug --no-bundle`) before `verify.ps1 -SkipBuild`, never `cargo test`/`tauri dev` between (dev-mode "localhost refused"); (2) `verify.ps1` must point `--library` at `src-tauri/tests/media` for `-Walkthrough m35` and skip gen-fixture; (3) the m35 walkthrough must reuse the EXISTING `player`/`m24` chapter-play helpers (`getAuthors`/`getAuthorDetail`/`playChapter` + the neighboring arm's `PlaybackContext` construction) — STOP if names differ; (4) register `"m35"` in `walkthroughs.ts` (+ any list assertion in `runner.test.ts`); (5) integration test reaches the crate via `audioshelf_lib::testing::{open_in_memory, scan_into}` and the media dir via `env!("CARGO_MANIFEST_DIR")/tests/media`; (6) the playback-error FE gate is the pure `playbackErrorText` unit test + the walkthrough visual verdict (avoids `PlayerBar`/`CreatorIdentity` invoke-mock fragility). Plan: [`docs/superpowers/plans/2026-06-15-audioshelf-m35-real-media-confidence.md`](docs/superpowers/plans/2026-06-15-audioshelf-m35-real-media-confidence.md). **NEXT: execute M35.**
- **M34 SHIPPED (2026-06-14 — PR [#89](https://github.com/yovanmc/AudioShelf/pull/89), CI 4m0s green) — Rendering & Memory at Scale; v8 (M30–M34) COMPLETE.** FE-only render-virtualization + result caps, executed exactly per plan via 4 Sonnet subagents. **The no-regression strategy held perfectly:** every virtualized surface gates on `length > VIRTUALIZE_THRESHOLD(40)` → below threshold renders the existing markup byte-for-byte so all existing tests + screenshot walkthroughs stayed green with zero edits; above it switches to the additive `<VirtualList>`. 520 vitest (492→+28 new, none dropped), tsc clean. Invariants ALL held (src-tauri diff EMPTY, no new dep, `db::LATEST`=13, read-only, fixtures 43/44/47). **Durable gotchas for future FE work:** (1) **types live in `src/lib/api.ts`, not `src/types.ts`** — `JournalEntry`/`WorkRow{id,baseTitle,chapters}`/`ChapterRow{id,title,...}`; journal grouping is `Map<authorName, Map<workTitle, JournalEntry[]>>`. (2) `.recent-row` CSS is element-agnostic → a `<ul>/<li>` list can be virtualized by porting rows to `<div className="recent-row">` with NO new CSS (keep `<ul>/<li>` on the below-threshold path so fixture screenshots don't shift). (3) react-window rows MUST apply the `style` prop to the row's outer element; VariableSizeList needs `resetAfterIndex(0)` in a `useEffect` keyed on the items array so collapse-toggles re-measure. (4) `<table>` surfaces use cap + "show more", NOT react-window (`<tr>`-in-positioned-`<div>` breaks table layout). (5) **`.shots` is gitignored → the verify.ps1 pixel-diff "CHANGED" is stale-baseline drift, not authoritative**; the real gate is the Sonnet subagent VISUAL verdict (untouched surfaces flag CHANGED too). (6) **the app-data DB persists across walkthrough runs** → a walkthrough that runtime-seeds (m34 seeds 62 journal entries) leaves data visible to a LATER walkthrough in the same verify session (e.g. `journal/01-journal-empty` showed leftovers) — run order matters; seed-then-cleanup or run the seeding walkthrough last for determinism.
- **M34 planned (2026-06-14 — `📝 Plan ready`, execute next) — Rendering & Memory at Scale (the v8 closer):** the last provisional v8 lever. Scoped from an Explore digest of the live FE build. **Key scoping fact the digest established:** at a 10k-author library the JS arrays are only ~1–10 MB (authors ~1 MB, journal ~10 MB) — **fine**; the real bottleneck is **DOM node count**, not memory. So the fix is render-virtualization + result caps, **FE-only**. Today **only LibraryView's author list is virtualized** (react-window `FixedSizeList`, M7 — left untouched, it's done/tested); every other large list `.map()`s all rows (digest urgency: HIGH/unbounded = Home shelves, AuthorDetail chapters [5k+ if expanded], Journal [50k+ entries]; bounded = search [backend-capped 50], collections, LabelManager tables [500–2k rows], discovery facets). **Three owner decisions (one batched AskUserQuestion):** (1) **all six surfaces, proportionate** — full virtualization for the unbounded HIGH ones, caps + virtualize-above-threshold for the bounded ones; (2) **FE-only render-virtualization + caps** — backend data-windowing REJECTED (marginal memory win, would move sort/filter server-side and break M10's in-memory client filtering of the virtualized author list); (3) **Home shelves = virtualize the vertical shelf stack + cap items/shelf (~20) with a "+N more" affordance**, NOT fragile horizontal-carousel virtualization. **🔴 Load-bearing safety property = ZERO regression at fixture scale.** react-window rows are absolutely-positioned `<div>`s (the `style` prop MUST be applied to each row's outer element → virtualizing changes markup, incl. `<ul>`/`<table>`→`<div>`). To guarantee no regression, **every virtualized surface uses an external `length > VIRTUALIZE_THRESHOLD(40)` gate**: below threshold it renders the **existing markup byte-for-byte unchanged** (so on the 43/44/47 fixtures — every surface is well under 40 — the original path runs and **all existing component tests + all existing screenshot walkthroughs stay green with no edits**); above threshold it switches to the new virtualized path (additive code, exercised only by new unit tests + the new `m34` walkthrough). Threshold 40 is above every per-surface fixture count — if any surface already renders >40 rows on the default fixture, STOP (deliberate baseline shift). **New shared pieces:** `src/components/VirtualList.tsx` (pure always-virtualized renderer; fixed `itemSize:number`→`FixedSizeList`, variable `itemSize:(i)=>number`→`VariableSizeList` with `resetAfterIndex(0)` in a `useEffect` keyed on the items array so collapse-toggles re-measure; default height 600 mirroring LibraryView's `LIST_HEIGHT`) + `src/lib/flattenRows.ts` (pure `flattenJournal`/`flattenAuthorDetail` that flatten the nested author→work→entry / works+chapters trees into a single typed row array honoring the `collapsed:Set<number>` — makes collapse a pure recompute; exported `ROW_H` height-by-kind constants) + unit tests proving DOM windowing (render 500 items, assert ≪500 in DOM). **Surface treatments:** Journal/AuthorDetail → flatten + VariableSizeList (entry rows CSS line-clamped to a fixed height so they're uniform); Home → vertical-stack VirtualList (FixedSizeList, fixed shelf height — measure the real shelf, ~260px estimate) + `CAP_SHELF_ITEMS=20` per-shelf with "+N more" routed to a natural existing destination (no new routes — static label if none); search buckets → gate+VirtualList per bucket (50-cap > 40 → virtualizes at scale); LabelManager `<table>`s → **cap + "show more" (`LABEL_TABLE_PAGE=100`), NOT react-window** (`<tr>`-in-positioned-`<div>` breaks table layout); ScopedResults → honest "showing first 50 of N" note (no grid virtualization — 50 cards is fine); Discovery → `CAP_FACET_CHIPS=24` + show-more toggle. Covers already lazy + module-cached (`Cover.tsx` `coverCache`) → no work; virtualization also reduces cover fetches (only windowed rows fetch). **Verification:** `m34` walkthrough runtime-seeds ≥60 journal entries (the m27 runtime-seed precedent — journal is seedable; chapters are NOT without a backend write path, so AuthorDetail/label virtualization is proven by **unit tests**, not screenshots) → captures virtualized journal + an inner-list scroll (proves row recycling) + LibraryView; PLUS a **regression** pass (`m12`/`journal`/`m24` at fixture scale must look unchanged — the below-threshold path). Screenshots → Sonnet subagent **text verdict** (never load PNGs into the controller). **Invariants (hard gates):** no new dep (`react-window@^1.8.10`+`@types/react-window@^1.8.8` already present; `package*`/`Cargo.*`/`gen-fixture` diff-stat EMPTY), **FE-only** (`git diff --stat src-tauri/` EMPTY), `db::LATEST` stays **13**, read-only-on-disk (no new write `invoke`), default fixtures **43/44/47** (`fixture_scan.rs` untouched; scale data runtime-seeded). **🔴 Gotchas baked in:** the no-regression gate is the whole safety story — DON'T always-virtualize (would window away rows existing fixture-scale tests assert are present); when virtualizing a `<ul>/<li>` surface, port the row CSS to a styled `<div>` row and keep the original markup on the below-threshold path; VariableSizeList needs `resetAfterIndex` on collapse; build via the PowerShell tool absolute-quoted `dev-env.cmd` (Bash-tool form no-ops); FROZEN build (`npm run build` then `cargo tauri build --debug`) before `verify.ps1 -SkipBuild`, never `cargo test`/`tauri dev` between (dev-mode "localhost refused"); register `"m34"` in `walkthroughs.ts` + lock step names in `runner.test.ts`. Plan: [`docs/superpowers/plans/2026-06-14-audioshelf-m34-rendering-memory-scale.md`](docs/superpowers/plans/2026-06-14-audioshelf-m34-rendering-memory-scale.md). **NEXT: execute M34 → v8 (M30–M34) COMPLETE.**

- **M33 SHIPPED (2026-06-14 — PR [#86](https://github.com/yovanmc/AudioShelf/pull/86), CI 3m8s green) — Remove Transcripts & Simplify Search:** executed per plan via 2 Sonnet removal subagents (Rust T1–3, FE T4–5) + 1 frozen-build screenshot subagent; controller did invariant audits, PR/CI/merge. **Deletion milestone** (13 files, −672/+64): schema v12→**v13** (`migration_v13_drop_transcripts` = `DROP TABLE IF EXISTS transcripts` on the M16 `run_step` runner, `LATEST` 12→13 — safe: nothing FK-references `transcripts`, content re-derivable from on-disk sidecars); deleted `transcripts.rs`/`search_transcripts`/`get_chapter_transcript`/`ingest_sidecar_transcript` + all FE wiring (`searchTranscripts`/`getChapterTranscript`/`TranscriptHit`, App.tsx parallel-search→`searchLibrary`-only + current-chapter transcript effect, LibraryView Transcripts bucket, NowPlayingPanel Transcript section, m16 `transcript-search` walkthrough step). **Non-transcript search BYTE-IDENTICAL** (`search_library`/`SearchResults`/`run_scoped_query`/`parse_query` untouched) + **M9/M26 label/tag-value matching KEPT** (typing `cozy` still finds tagged works). **Gates:** cargo test **169 lib** (−16) + integration green, `fixture_scan` 43/44/47, `migration_v13_drops_transcripts` proves the drop; tsc clean + **492** vitest (−7); dep diff-stat **EMPTY** (no new dep); read-only intact (no added `fs::`); dead-ref sweep clean (zero `transcript` in `src`); m7 (search, no Transcripts bucket) + m24 (now-playing, no Transcript section) frozen-build screenshots PASS. **As-built deviations (durable):** (1) `make_snippet` + its test `snippet_is_centered_on_match` were tied to `search_transcripts_inner` → removed too (final lib count **169**, not "185−16"). (2) `cargo test` caught **3 extra `assert_eq!(…,12)` "reaches latest" asserts in commands.rs** beyond the plan's list + the neighboring `upgrade_from_v4` test's `post==12`/`has_transcripts==1` pair → bumped to 13 / flipped to `==0` (the M30/M32 "grep-and-let-cargo-test-confirm" lesson held again — version-bump asserts always exceed the inventory). (3) FE transcript `useEffect` removal left a stray `}, [current?.chapter.id]);` → caught by `tsc` (TS1128), removed in a follow-up edit. (4) **m16 has no now-playing step** (it's manage-tags/metadata-diff/series-spine/forgotten-shelf/discover-reasons) → the now-playing surface was verified via the **m24** walkthrough (`03-03-now-playing.png`), the correct surface — note for any future now-playing screenshot gate. **Arc:** M30 ✅ → M31 ✅ → M32 ✅ → **M33 ✅** → **M34 = rendering & memory at scale** (virtualize Home shelves / AuthorDetail chapters / search / journal / collections / LabelManager; preload; result caps — the last provisional v8 lever). Plan: [`docs/superpowers/plans/2026-06-14-audioshelf-m33-remove-transcripts.md`](docs/superpowers/plans/2026-06-14-audioshelf-m33-remove-transcripts.md). **NEXT: plan M34 (rendering & memory at scale).**

- **M33 RE-SCOPED + planned (2026-06-14 — `📝 Plan ready`, execute next) — Remove Transcripts & Simplify Search (was "FTS5 full-text search"):** mid-planning, the owner pivoted: *"completely remove the transcripts feature and anything related to it. search should purely be off of the title of the files, lists, or names of the creators."* I flagged that this **guts the rationale for FTS5** — the only large free-text field was the transcript content (parsed from `.srt`/`.vtt` sidecars into the `transcripts` table, searched via the separate `search_transcripts` command + a now-playing transcript panel); searching purely over creator names / work titles / chapter filenames is all SHORT fields, which M32 already measured at `searchMs` ≈4–7ms on 12k chapters, so FTS5 (virtual tables + sync triggers + trigram tokenizer) would be pure machinery for **zero measurable win**. **Four batched-AskUserQuestion decisions (owner-confirmed):** (1) **drop FTS5 entirely** — M33 becomes a deletion milestone, keep the existing fast substring `LIKE` search; (2) **drop the `transcripts` table** via a **v13 migration** (`DROP TABLE IF EXISTS transcripts` on the M16 `run_step` runner, `db::LATEST` 12→13) — safe because nothing FK-references the table (it references `chapters`, not the reverse → no FK-off rebuild) and its content is re-derivable from on-disk sidecars we never wrote (no user-authored data lost); v5 still *creates* it, v13 *drops* it (monotonic chain, never edit history); (3) **KEEP** the label/tag-value matching in `search_library` (the M9/M26 unified-labels `EXISTS` discovery affordance — typing `cozy` still finds works tagged cozy — it is intentional and unrelated to transcripts; "purely titles/names" read conservatively to mean "not spoken content," and removing an existing feature without explicit instruction violates destructive-op discipline). **Scope = exhaustive removal** (full file:line inventory from an Explore digest is in the plan): delete `src-tauri/src/transcripts.rs`; strip `scan.rs` ingestion (`ingest_sidecar_transcript` + call site + 3 transcript tests, **KEEP** `srt_and_vtt_files_alone_do_not_add_chapters` — it guards that stray subtitle files aren't treated as audio, still valid); `commands.rs` `search_transcripts`/`get_chapter_transcript` (+`_inner` + `TranscriptHit` struct + 5 tests); `lib.rs` `mod transcripts` + 2 `generate_handler!` regs + 2 testing re-exports; FE `searchTranscripts`/`getChapterTranscript`/`TranscriptHit`, App.tsx (state, current-chapter transcript effect, parallel `Promise.all([searchLibrary,searchTranscripts])` → `searchLibrary` only), LibraryView "Transcripts" bucket in `SearchResultsPanel`, NowPlayingPanel "Transcript" section, dead `.now-playing__transcript` CSS, the `m16` `transcript-search` walkthrough step + `runner.test.ts`. **Non-transcript search untouched & byte-identical:** `search_library`/`search`/`search_library_for_test`/`SearchResults`/`AuthorHit`/`WorkHit`/`ChapterHit` and the scoped/DSL `run_scoped_query`/`parse_query` are NOT changed → the author/work/chapter buckets render unchanged (FE change is purely *removal* of the transcript bucket + panel). **Invariants (hard gates):** no new dep (`Cargo.*`/`package*`/`gen-fixture/Cargo.toml` diff-stat EMPTY — only removals), read-only-on-disk (removing a read+derive feature → strictly more read-only), default fixtures **43/44/47** (confirmed no `.srt`/`.vtt` in the fixture tree; transcripts were always runtime-seeded in tests), schema v12→**v13** on the M16 runner. **🔴 Gotchas baked in:** grep & bump **every** hardcoded `12` version assert (`assert_eq!(…12)` on a full-open `user_version`, `open_at_version(12)`-as-latest, schema-version `"12"` in `settings_round_trip`) in **BOTH** `db.rs` AND `commands.rs` to 13 — leave step-specific guards (`open_at_version(4)`, "requires v5/v8/v10/v11", and the new `migration_v13_drops_transcripts`'s own `open_at_version(12)`) alone — `cargo test` is the catch (M30/M32 lesson); update/rename `schema_creates_all_tables_including_transcripts` (transcripts is now absent at LATEST) + add `migration_v13_drops_transcripts` (present at v12, gone at v13); expect Rust lib test count to **drop ~16** (8 transcripts.rs + 3 scan + 5 commands) and FE count to drop by the 7 removed transcript tests — that's expected, not a regression. FE surfaces changed (search panel loses a bucket, now-playing loses a section) → frozen build + `m7`+`m16` walkthrough screenshots verified by a Sonnet subagent text verdict (don't load PNGs into the controller). Cargo runs via the **PowerShell tool** absolute-quoted `dev-env.cmd` form (Bash-tool form silently no-ops). Plan: [`docs/superpowers/plans/2026-06-14-audioshelf-m33-remove-transcripts.md`](docs/superpowers/plans/2026-06-14-audioshelf-m33-remove-transcripts.md). **NEXT: execute M33.**

- **M32 SHIPPED (2026-06-14 — PR [#83](https://github.com/yovanmc/AudioShelf/pull/83), CI 3m49s green) — Query & Index Performance:** built per plan via a Sonnet implementation subagent (Tasks 1–6); controller did the strengthened insights test, A/B measurement, PR/CI/merge. Landed all 4 levers: **v12 additive migration** (`migration_v12_query_indices` on the M16 runner, `LATEST` 11→12) adding the **only two genuinely-missing indices** — covering `idx_chapters_work ON chapters(work_id,status,played,duration_secs)` + FK `idx_play_events_chapter ON play_events(chapter_id)` (the 4-listed scope correction held: `works(author_id)`/`metadata_terms(facet,value)`/`*_metadata` owner-id lookups already covered by UNIQUE/PK auto-indexes → not re-added); `query_authors` 4 correlated subqueries → single `GROUP BY` (status in JOIN-ON, `COUNT(DISTINCT w.id)` guards fan-out); `compute_insights` per-work 2-subquery block → `GROUP BY` over `idx_chapters_work` (Rust math untouched); query PRAGMAs (`cache_size=-16384`/`temp_store=MEMORY`/`mmap_size=128MB`) in file-backed `open()` only. **Perf A/B (interleaved same-session, 12k chapters / 3000 play_events; observational, NOT a CI gate; measured by temporarily reverting just the 3 query/index levers then `git checkout -- src-tauri`; genMs ~37–43s both runs = stable env): `insightsMs` 3394→11–15 (≈225–300×!), `queryAuthorsMs` 32→7–11 (≈3–4×), `searchMs` 7→4–7 unchanged (expected — `search_library` untouched).** The huge insights win is the per-work correlated-subquery O(works×chapters) scan collapsing to one indexed `GROUP BY`. **Tests:** Rust **184 lib** (+ the M32 tests: `m32_indices_exist`, `query_authors_aggregates_across_multiple_works` fan-out equivalence, the repo's **first `EXPLAIN QUERY PLAN` test** `query_authors_plan_uses_covering_index`, and `compute_insights_fully_played_flag_with_mixed_chapters` — strengthened by the controller to assert the observable `top_tags` owned=2/finished=1 rather than the subagent's near-worthless `total_chapters==0`) + all integration (`fixture_scan` 43/44/47, `scaled_scan` correctness, 2 `measure_` `#[ignore]`d); FE untouched (tsc clean, **499** vitest, `src/` diff EMPTY). **Invariants held:** no new dep (`Cargo.*`/`package*`/`gen-fixture/Cargo.toml` diff-stat EMPTY — no rayon; FTS5 deferred), schema v11→**v12** additive (only `CREATE INDEX IF NOT EXISTS`), read-only-on-disk (all `fs::` hits in scan.rs/commands.rs are `#[cfg(test)]` or pre-existing export/backup). **As-built deviations (durable):** (1) **subagent found 2 extra "reaches latest" asserts in `commands.rs` beyond the plan's list** (`settings_round_trip` schema_version "11"→"12", and a `migration_v*` test checking `full_ver==11`) — `cargo test` is the catch, as M30 warned. (2) **Task 5c seeded works/chapters directly** (the scanner split "Alpha N"/"Beta N" into 4 works, not 2 — the plan's explicit fallback). (3) **Task 6 imports route through `audioshelf_lib::testing`** (insights is a private `mod`); `search_library_for_test(&Connection,&str)` added to the `testing` re-export in `lib.rs`. (4) **the EXPLAIN QUERY PLAN `detail` column is index 3** in rusqlite 0.32 (`id,parent,notused,detail`) — worked as the plan predicted. **Arc:** M30 ✅ → M31 ✅ → M32 ✅ → **M33 = FTS5 full-text search** (deferred here; rusqlite bundled SQLite has FTS5 → no new dep; trigram tokenizer to preserve substring matching) → **M34 = rendering & memory at scale**. Plan: [`docs/superpowers/plans/2026-06-14-audioshelf-m32-query-index-performance.md`](docs/superpowers/plans/2026-06-14-audioshelf-m32-query-index-performance.md). **NEXT: plan M33 (FTS5 full-text search).**

- **M32 planned (2026-06-14 — `📝 Plan ready`, execute next) — Query & Index Performance:** the third v8 lever (read-query perf, after M30 robust/incremental scan + M31 scan-write throughput). Scoped from three M30-era Explore digests + three fresh targeted digests of the exact hot-path code. **Two owner forks (one batched AskUserQuestion, both → Recommended):** (A) **FTS5 DEFERRED to its own dedicated milestone** — it's the only item that changes search UX semantics and needs a virtual table + sync triggers + scan integration + a trigram tokenizer choice; splitting keeps M32 purely additive/low-risk and shippable like M30/M31 (`search_library` SQL is **untouched** in M32, just gets faster from the new indices). (B) `compute_insights` = **index + bound, keep the tested Rust date/streak/heatmap math** (only its per-work correlated subqueries become a `GROUP BY`); a full SQL-aggregation rewrite was rejected as higher correctness risk for marginal gain once indexed. **M32 scope = 4 levers:** (1) **v12 additive migration** (`migration_v12_query_indices` on the M16 `run_step`/`open_at_version` runner, `LATEST` 11→12) adding the **only two genuinely-missing indices**: covering `idx_chapters_work ON chapters(work_id, status, played, duration_secs)` (makes the work-grouped aggregations in `query_authors` + `compute_insights` index-only scans) and FK `idx_play_events_chapter ON play_events(chapter_id)`. (2) `query_authors` (`commands.rs:273`) 4 per-author correlated subqueries → single `GROUP BY` with status filters in the JOIN-ON clauses (preserves LEFT-JOIN "authors with no active works still appear" semantics) + `COUNT(DISTINCT w.id)` to guard the chapter fan-out. (3) `compute_insights` (`insights.rs:372`) per-work 2×`count(*)` subquery block → `GROUP BY` over `idx_chapters_work` (the event-scan join + `build_insights` Rust math untouched). (4) query PRAGMAs `cache_size=-16384` (16MB) + `temp_store=MEMORY` + `mmap_size=128MB` in the **file-backed `open()` only** (not `open_in_memory`; mmap is the first to drop if Windows+WAL instability). **🔴 Scope correction baked in (the M31 "O(n²) already done" pattern):** of the owner's 4 listed "missing indices," **only 2 are real** — `works(author_id)` is already covered by the `UNIQUE(author_id, base_title)` auto-index, `metadata_terms(facet, value)` by its own `UNIQUE(facet, value)`, and `chapter_metadata`/`author_metadata`/`work_metadata` owner-id lookups by their composite `(owner_id, term_id)` PK auto-indexes (the existing `idx_*_metadata_term` cover the reverse direction); adding them would be redundant **and** would slow the M30/M31 scan-write path. Also confirmed `query_authors`'s two tag-aggregation queries are already **single-scan** (one per facet table, NOT per-author) → left unchanged. **FTS5 availability confirmed:** rusqlite `0.32 features=["bundled"]` ships SQLite with FTS5 compiled in → the deferred FTS5 milestone needs **no new dep** (trigram tokenizer to preserve today's substring matching). **Backend-only — ZERO FE change** (`AuthorRow`/`InsightsData`/all command signatures byte-identical) → **no screenshot walkthrough** (M31 precedent); verification = full `cargo test` (lib+integration, `fixture_scan` 43/44/47) + `tsc`/`npm test` proving `src/` diff EMPTY. **New test prior-art:** `m32_indices_exist`, a multi-work fan-out equivalence test for `query_authors` (locks against `GROUP BY` double-counting — the one real correctness risk), and the repo's **first `EXPLAIN QUERY PLAN` test** (asserts the plan uses `idx_chapters_work`, durable proof the index is used). **Measurement:** new `#[ignore]`d `measure_queries_at_scale` in `scaled_scan.rs` emits `QUERY-METRICS {chapters,queryAuthorsMs,searchMs,insightsMs}` (needs a `search_library_for_test(&Connection,&str)` thin test accessor since the real command takes Tauri `State`); `verify.ps1 -Measure` test filter broadened `measure_scan_at_scale`→`measure_` to run both timing tests. **Perf is observational interleaved A/B in one session (M31 noisy-machine lesson — `genMs` is the env-drift canary; never compare to an old baseline), NOT a CI gate** — the EXPLAIN QUERY PLAN test is the durable proof. **Invariants (hard gates):** no new dep (`Cargo.*`/`package*`/`gen-fixture/Cargo.toml` diff-stat EMPTY — no rayon; FTS5 deferred), schema v11→**v12** additive (only `CREATE INDEX IF NOT EXISTS`, `SCHEMA_V1` untouched), read-only-on-disk, default fixture 43/44/47. **🔴 Gotchas baked in:** bump the "reaches LATEST" version asserts to 12 in **BOTH** `db.rs` AND `commands.rs` (M30 T1 lesson — `cargo test` is the catch), but leave v11-step-specific guards at 11; cargo runs via the PowerShell tool with the absolute-quoted `dev-env.cmd` form (M31 env note — the Bash-tool `cmd /c "tools\dev-env.cmd …"` form silently no-ops); `pwsh -File tools/verify.ps1 -Measure` from the Bash tool needs a forward-slash path; package is `audioshelf` (lib `audioshelf_lib`). **Provisional arc updated:** M30 ✅ → M31 ✅ → M32 (this) → **M33 = FTS5 full-text search** (split out here) → **M34 = rendering & memory at scale**. Plan: [`docs/superpowers/plans/2026-06-14-audioshelf-m32-query-index-performance.md`](docs/superpowers/plans/2026-06-14-audioshelf-m32-query-index-performance.md). **NEXT: execute M32.**

- **v8 arc defined + M30 planned (2026-06-14 — `📝 Plan ready`, execute next) — Robust, Observable, Incremental Scan + Scale-Test Foundation:** v7 (M26–M29) is COMPLETE; owner set v8 = **Real-Scale Hardening** (dependable on a real 10k+ file library; today only 43/44/47 tiny fixtures). Scoped from **three parallel Explore digests** of the live build. **Scan-path risks found:** ~15k un-batched autocommit statements (no transaction wraps the scan), single-threaded/blocking, an **O(n²) `files.iter().find()`** per chapter (`scan.rs`), full `lofty` read for duration (not header-only), **no mtime/size incremental skip** (full rescan every time), **no deletion detection** (deleted files stay `status='active'`), silent `.flatten()` error-swallowing (one bad file/permission-denied dir lost with no log; a DB error aborts the whole scan), no progress/cancellation, and only `PRAGMA foreign_keys` set (no WAL/busy_timeout). **DB risks (deferred to M32):** missing indices on `chapters.work_id`/`play_events.chapter_id`/`metadata_terms(facet,value)`/`works.author_id`; `query_authors` runs 4 correlated subqueries per author; `search_library` is leading-`%` `LIKE` across 5 columns (no FTS5); `compute_insights` loads ALL `play_events` into memory. **FE risks (deferred to M33):** only the LibraryView author list is react-window-virtualized (Home shelves, AuthorDetail chapters, search results, journal, collections, LabelManager all `.map()` every row); the whole author set is held in JS. **Harness gap:** zero timing (screenshots + pixel-diff only); `gen-fixture` hardcoded to 40 filler authors. **Two owner forks (one batched AskUserQuestion):** (A) **M30 leads with scan robustness** (incremental/deletion/progress/cancel/error-tolerance), raw throughput deferred to M31; (B) fixture = **silent-WAV structural** (parameterized to thousands, CI-friendly), realistic-media decode cost measured separately. **M30 design (13 tasks, backend-first):** schema **v11** additive (`file_mtime`/`file_size`/`last_seen_scan` cols + `idx_chapters_last_seen` on the M16 `run_step`/`user_version` runner; `LATEST` 10→11, first schema touch since M26's v10; `SCHEMA_V1` untouched, no FK-off rebuild); refactor `scan_into`→`scan_into_with(conn,root,&ScanOpts{cancel,progress})` keeping `scan_into(conn,root)` as a wrapper so **all existing scan.rs tests + `fixture_scan.rs` stay green** (`rescan_is_idempotent` updated to compare totals, not the now-divergent diff fields); per-author `BEGIN…COMMIT` (crash/cancel atomicity + batching side-benefit); incremental skip compares disk mtime+size to stored (unchanged file → stamp `last_seen_scan`, **no `lofty` probe, no full upsert**); deletion detection via a monotonic `scan_generation` (settings counter) → `sweep_deleted` flips unstamped active rows to `status='inactive'` + cascades works/authors (**soft-delete, recoverable, honors destructive-op discipline — never `fs::remove`**); error capture into `Vec<ScanError>` (one bad file/dir never aborts; replaces `.flatten()`); progress via `app.emit("scan:progress", ScanProgress)` (`tauri::Emitter` — **NO event-emit prior art existed**, FE has `listen` prior art `miniplayer:command`); cancellation via `ScanControl(Arc<AtomicBool>)` managed state + `cancel_scan` command touching ONLY the flag so it runs on a separate worker thread while `scan_library` holds the `DbState` mutex (webview stays responsive; other DB commands DO block during a scan — **honest framing, full concurrency not claimed**); `ScanResult` gains added/updated/removed/skipped + `errors` → scan-diff summary + progress bar + Cancel button in `ScanView`. Foundation: `gen-fixture::generate_scaled(root,authors,works_per,chapters_per)` (keep `generate` unchanged → fixture_scan stays 43/44/47) + `verify.ps1 -Measure` runs an `#[ignore]`d scaled timing test → `SCALE-METRICS {chapters,genMs,scanMs,rescanMs,rescanSkipped}` baseline for M31/M32. Robustness PRAGMAs (WAL/synchronous=NORMAL/busy_timeout=5000) in `open()` ONLY (not `open_in_memory`); cache_size + query PRAGMAs deferred to M32. **Invariants (hard gates):** no new dep (`Cargo.*`/`package*.json`/`gen-fixture/Cargo.toml` diff-stat EMPTY — `rayon` stays out), read-only-on-disk (audit `scan.rs`/`commands.rs` for `fs::remove`/`rename`/`write`/`create` — only reads allowed; WAL `-wal`/`-shm` sidecars are app-data, not library), default fixture 43/44/47. **Gotchas baked into the plan:** `cmd //c` (not `/c`) in the Bash tool; FROZEN build (`npm run build` THEN `cargo tauri build --debug` before `verify.ps1 -SkipBuild`; never `cargo test`/`tauri dev` between → dev-mode "localhost refused"); no `getCurrentWindow().setSize()` (scroll for tall shots); grep & bump any hardcoded `user_version == 10`/`open_at_version(10)`/`_10_` asserts in db.rs+commands.rs to 11; grep `ScanResult {` struct literals across src-tauri (the new fields aren't covered by `#[serde(default)]` on a literal — add the fields or `..Default::default()`); progress/cancel + soft-delete states are seeded deterministically in the `m30` walkthrough (tiny-fixture scan is instantaneous → otherwise un-screenshotable); register `"m30"` in `walkthroughs.ts` + keep step NAMES stable so `runner.test.ts` stays green; the measurement bin must NOT add `audioshelf_lib` as a non-dev dep of `gen-fixture` (cycle/invariant break) → use an `#[ignore]`d timing test instead; controller direct-reviews the thin progress-bar/cancel-button PNG (single-subagent unreliable on low-contrast). Plan: [`docs/superpowers/plans/2026-06-14-audioshelf-m30-robust-incremental-scan.md`](docs/superpowers/plans/2026-06-14-audioshelf-m30-robust-incremental-scan.md).
- **M30 SHIPPED (2026-06-14 — PR [#77](https://github.com/yovanmc/AudioShelf/pull/77), 9 commits, CI 3m17s green) — Robust, Observable, Incremental Scan + Scale-Test Foundation:** built per plan via Sonnet subagents (T1–T10). Schema **v11** additive (`chapters.file_mtime`/`file_size`/`last_seen_scan` + `idx_chapters_last_seen`); `scan_into`→`scan_into_with(conn,root,&mut ScanOpts)` wrapper preserved; per-author `BEGIN…COMMIT`; mtime+size incremental skip; `scan_generation` settings counter + `sweep_deleted` soft-delete + cascade (recoverable, never `fs::remove`); `Vec<ScanError>` capture; `scan:progress` emit + `cancel_scan`/`ScanControl(Arc<AtomicBool>)`; `ScanResult` +added/updated/removed/skipped/errors/cancelled → ScanView progress bar + Cancel + scan-diff line. **Scale baseline (record for M31/M32): `SCALE-METRICS {"chapters":12000,"genMs":~22000-30000,"scanMs":~6400,"rescanMs":~4900,"rescanSkipped":12000}`** — 12k silent-WAV chapters, first scan ~6.4s, incremental rescan ~4.9s skipping all 12000. **Tests:** Rust lib **178** + integration (incl. `fixture_scan` still 43/44/47, `scaled_scan` count+skip, `#[ignore]`d `measure_scan_at_scale`) green; FE `tsc` clean + **499** vitest green (3 new ScanView tests); `m30` walkthrough (summary/progress+cancel/removed) + `m12` 15-shot regression screenshot-verified PASS. **Invariants held:** dependency diff-stat EMPTY (no new dep; `rayon` stays out for M31); production scan path read-only (all `fs::write`/`remove` grep hits are `#[cfg(test)]` helpers or pre-existing export commands); default fixture 43/44/47; additive v11 on the M16 runner. **As-built deviations from the plan (durable):** (1) **T1 missed 4 hardcoded `user_version == 10` asserts in `commands.rs`** (`settings_round_trip` + 3 migration tests that check a *full* open reaches LATEST) — `cargo test` caught them, bumped to 11; `open_at_version(10)` and "requires v10" guards correctly stay at 10. Also `open_at_version()` mirrors `migrate()` and needed the v11 step too. (2) **`scan_author` always calls `regroup_author`** (the plan's `author_changed` optimization was dropped) — needed to keep `scan_reapplies_grouping_overrides` green (overrides can change between scans with no file change); regroup is cheap DB-only, but it means rescan isn't truly near-instant (the ~4.9s/12k baseline reflects per-author regroup + per-chapter stamp, not just the lofty-skip). (3) **CSS token `--color-surface-2` doesn't exist** → used `--color-surface-raised` for the progress track. (4) **One-time post-upgrade rescan reports all existing chapters as `updated`** (pre-v11 rows stored `file_mtime=0`, so the unchanged-branch `mtime != 0` guard re-probes them once; subsequent rescans skip). **Env gotcha (durable):** `tools\dev-env.cmd`'s `%ProgramFiles(x86)%` vswhere lookup fails under PowerShell's `cmd /c` in this session — run cargo via `cmd /c 'set "PATH=%USERPROFILE%\.cargo\bin;%PATH%" && call "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat" >nul && cd /d "...\src-tauri" && cargo ...'`. Package name is **`audioshelf`** (lib target `audioshelf_lib`) → use `cargo test`/`--test <name>`, NOT `-p audioshelf_lib`. **NEXT: plan M31 (scan & write throughput — O(n²) fix, batched statements, parallelism; needs a `rayon` no-new-dep waiver from the owner).**

- **M31 SHIPPED (2026-06-14 — PR [#80](https://github.com/yovanmc/AudioShelf/pull/80), 5 commits, CI 2m59s green) — Scan & Write Throughput:** backend-only scan write-path perf refactor, **`src-tauri/src/scan.rs` only**, built per plan via a Sonnet subagent (Tasks 2–6; Task 1/7 measurement by the controller). Landed: `conn.prepare_cached(...)` on every hot per-row statement in `scan_author()` (works upsert, chapters upsert, existing-row probe, skip UPDATE) + the transcript sidecar INSERT (each distinct SQL compiles once per connection, reused from rusqlite's statement cache instead of re-compiling per row); `RETURNING id` appended to the work + chapter upserts eliminated the two trailing `SELECT id` round-trips (`upsert_chapter` return type `Result<()>`→`Result<i64>`, sole call site updated to `let id = …; id`). All stays inside M30's per-author transaction (atomicity preserved) + behind M30's incremental mtime+size skip — observable behavior identical. **Tests:** full Rust suite **179 lib** (178 baseline +1 new guard `changed_file_counts_as_update_and_preserves_chapter_identity`, which locks update-path row identity and passes before & after) + all integration (`fixture_scan` 43/44/47, `scaled_scan` correctness) green; no FE change. **Invariants held (hard-gated):** `git diff --stat main -- Cargo.toml Cargo.lock db.rs` EMPTY (no new dep — `rayon` still out; no schema — `db::LATEST` stays 11); read-only-on-disk; default fixtures 43/44/47. **As-built — clean run:** the subagent reported zero deviations; every plan `old_string` matched exactly. **Perf measurement lesson (durable):** the start-of-session baseline was `SCALE-METRICS scanMs=6233/rescanMs=5158` on a pristine machine, but the machine **degraded heavily over the session** — fixture-gen `genMs` drifted **20s→54s** and *main's own unchanged baseline code* re-measured at **scanMs=10075** later, so any start-vs-end absolute comparison is **invalid/misleading** (it falsely showed a "regression"). The valid measure is a **back-to-back interleaved A/B** (checkout main → measure → checkout branch → measure, ×2 pairs, same environment): **MAIN scanMs 10075/7013 vs BRANCH 5539/5557 (cold ~21–45% faster); MAIN rescanMs 6020/6007 vs BRANCH 5031/5564 (~8–16% faster)** — and the branch is far more **stable** (5539/5557 within 0.3% vs main's volatile 7013/10075), consistent with removing per-row SQL recompilation + round-trips. **Rule for future `verify.ps1 -Measure` perf milestones: on a shared/noisy desktop, always do a tight interleaved A/B in the same session, never compare against a baseline captured minutes/hours earlier — `genMs` (pure fixture I/O, zero scan-code) is the canary for environmental drift.** **Env (durable, corrects M30 note):** in THIS session the `cmd /c "tools\dev-env.cmd cargo …"` form via the **Bash tool** silently fails (prints only the cmd banner, runs nothing, exit 0); cargo runs reliably via the **PowerShell tool** with absolute-quoted paths — `cmd /c '"C:\Agent Projects\AudioShelf\tools\dev-env.cmd" cargo test --manifest-path "C:\Agent Projects\AudioShelf\src-tauri\Cargo.toml" <filter>'` (the exact form `tools\verify.ps1` itself uses); `pwsh -File tools/verify.ps1 -Measure` works from the Bash tool **only with a forward-slash path** (`tools\verify.ps1` → backslash eaten → `toolsverify.ps1` not found). Plan: [`docs/superpowers/plans/2026-06-14-audioshelf-m31-scan-write-throughput.md`](docs/superpowers/plans/2026-06-14-audioshelf-m31-scan-write-throughput.md). **NEXT: plan M32 (Query & Index Performance — missing indices on `chapters.work_id`/`play_events.chapter_id`/`metadata_terms(facet,value)`/`works.author_id`, `query_authors`→GROUP BY, bound `compute_insights`, FTS5, WAL/cache/busy_timeout PRAGMAs).**

- **M29 planned (2026-06-14 — `📝 Plan ready`, execute next) — Player & Onboarding micro-polish, the v7 closer:** two Explore digests (the [v7 backlog](docs/superpowers/specs/2026-06-14-audioshelf-v7-discovery-curation-backlog.md) item text for PL7-1…10/ON-1…9/IA7-4/5/9, + verbatim FE source anchors) confirmed all 22 items are achievable **FE-only** — no Rust, schema stays `LATEST`=10, no dep, no capability, read-only-on-disk, fixtures 43/44/47. M28 already shipped so the old "may merge with M28" note is moot — **M29 stands alone**. **Two owner decisions (one batched AskUserQuestion):** (A) **ship all 22 broad** (incl. the 6 Low items — mini-player skip, kbd hints, sleep preview/countdown, empty-Discover, Settings reorder, palette section headers — matching the M16–M28 "all findings broad" pattern); (B) **ON-1 = gate the "My listening" sidebar group (Journal/Insights) until listening history exists**, so first-run shows only Browse (reuses the existing `noHistory` signal HomeView already gets from M23 CL-2; AppShell gains a `hasHistory` prop) — also fixes ON-3 jargon copy. **16 serial tasks:** T1 shared glyph(`keyboard`)+tokens → T2 scrubber always-visible thumb + scrub-preview bubble + resume cue (PL7-1/2) → T3 speed-pill/mute-zeros-slider/mini-skip (PL7-4/5/6) → T4 "Play next — [title]" + chapter-end cross-fade (PL7-7/3) → T5 sleep end-of-chapter preview + prominent expiring countdown (PL7-8/10) → T6 keyboard-shortcut hints dialog (PL7-9) → T7 warmer Home shelf headers + de-jargon empty copy (ON-2/3/5) → T8 gate My-listening group (ON-1) → T9 collapsed-sidebar caption labels (IA7-5) → T10 scan reassurance + completion CTA (ON-7/8) → T11 guiding empty-Discover (ON-6) → T12 Settings anchor sub-nav + first-run order + actionable hint (IA7-4/ON-9/ON-4) → T13 palette section headers (IA7-9) → T14 `m29` walkthrough (11 shots) → T15 verify → T16 PR. **Pure helpers are TDD'd** (`formatScrubPreview`/`nextChapterLabel`/`endOfChapterPreview` in `playback.ts`, `hasHistory` AppShell render gate, ScanView CTA render); CSS/copy items verified by the `m29` walkthrough + copy-regression tests (HomeView/ScanView/Settings). **Gotchas baked into the plan:** 🔴 `cmd //c` (not `/c`) in the Bash tool; 🔴 frozen build — `npm run build` THEN `cargo tauri build --debug` before `verify.ps1 -SkipBuild` (FE is EMBEDDED; never `cargo test`/`tauri dev` between the frozen build and a `-SkipBuild` capture → dev-mode "localhost refused"); no `getCurrentWindow().setSize()` (scroll for tall shots); 🔴 first-run-only copy (empty Home / scan-complete) is unreachable by the pre-configured fixture (M23 CL-3 lesson) → seed a first-run-like state OR mark NA + source-confirm, not FAIL; dividers/cues that must read on dark use `--color-divider` (M25/M28); native `<select>` popups don't screenshot (PL7-8 source-confirm if natives); register `"m29"` in `walkthroughs.ts` + keep step NAMES stable so `runner.test.ts` stays green; controller direct-reviews the borderline low-contrast scrubber/cue/caption PNGs (M25/M28 single-subagent-unreliable lesson); FE-only hard gate = `git diff --stat main` of `src-tauri`/`Cargo.*`/`package*`/`gen/schemas` must be EMPTY. After M29 merges, **v7 (M26–M29) is COMPLETE** → next session scopes the next version. Plan: [`docs/superpowers/plans/2026-06-14-audioshelf-m29-player-onboarding-polish.md`](docs/superpowers/plans/2026-06-14-audioshelf-m29-player-onboarding-polish.md).
- **M29 SHIPPED (2026-06-14, [PR #74](https://github.com/yovanmc/AudioShelf/pull/74)) — v7 (M26–M29) COMPLETE.** All 22 items landed FE-only across 14 task-commits + 1 verification commit; invariants held (forbidden-path `git diff main` EMPTY, schema `LATEST`=10, no dep, read-only-on-disk, fixtures 43/44/47). Gates: tsc clean · **498** FE tests + **15** runner tests green · `cargo test` green & unchanged. **Honest no-ops (already done in M24/M28):** PL7-6 mute-zeros-slider + PL7-5 mini-skip pre-existed in the compact bar since M24; ON-9 Library-first order already true (M28-style verified no-op). **Substitutions found during build:** M29 `:root` tokens live in `tokens.css` (not `components.css`); M24 had no `nextChapterTitle` prop → added + wired from App via the same `canPlayNext` chapter-index logic; collapsed class is `.app-shell--collapsed`; palette discriminant is `kind`. **`hasHistory` wired from the existing `home` payload** (`keepListening`/`stats.recent`/`stats.chaptersFinished`) — no new backend query. **Verification lessons:** (1) the screenshot subagent FALSE-FAILED the three thin-element shots (scrubber thumb, resume cue, collapsed-sidebar captions) — controller direct-review confirmed all three render correctly (the documented M25/M28 single-subagent-unreliable case); (2) **the Ctrl+K command-palette overlay cannot be GDI-captured by the harness** — the proven m19 `command-palette` step produces the IDENTICAL blank window (focus/airspace limitation) → IA7-9 section headers are **source-confirmed** in `CommandPalette.tsx` + palette-nav tests, NOT screenshot-verified; (3) the `02-scrubber-cue` shot needed the driver to **seed a resume position via `savePlaybackPosition` then re-fetch** so `context.chapter.playbackPositionSecs > 0` (a bare panel-expand leaves position 0 → no cue). **NEXT: scope the next version/milestone (v7 done).**

- **M28 (✅ shipped, PR #71) — Visual Consistency II (VIS7-1…10), all 10 plan tasks via Sonnet subagents:** the v7 visual-consistency capstone, **FE-only** (invariant diff-stat of `src-tauri`/`Cargo.*`/`package*`/`capabilities` EMPTY; schema `LATEST`=10; read-only-on-disk; fixtures 43/44/47). Shipped the **border-strength rule** (new per-theme `--color-divider`: dark→`border-strong`, light→`border`, HC→`border-strong`) across 18 structural borders + chip-gap/dialog-context/card-hover/icon-scale/avatar-initials/expanded-player-gap cleanups. **Durable decisions/gotchas this run:** (1) **HC `--shadow-card-hover` = `none`** (not the plan's assumed `0 8px 24px /60%`) — HC's `--shadow-card` is already `none` (border-driven, shadowless), so a black-on-black hover shadow would be invisible & off-philosophy; emphasis on HC comes from `--color-border-strong`. The plan explicitly authorized scaling the hover shadow per-theme. (2) **T9 had to change BOTH `.now-playing__layout` rules** (layout.css:63 AND components.css:237) — `layout.css` imports AFTER `components.css` (main.tsx order: tokens→base→components→layout→insights), so the layout.css rule WINS the cascade; editing only the M25 components.css rule would have had ZERO visible effect. (3) **T3 secondary-text audit was a NO-OP** — all 6 `opacity` hits in src/styles are legitimate (disabled-state, artwork glyph, chevron/slider affordances, `opacity:1`); none dimmed text, and `.muted` was already `color: var(--color-text-muted)`. The codebase was already clean on this axis. (4) **showDialogContext walkthrough step uses `BulkTagDialog`, NOT `ChapterJournalDialog`** — the journal dialog doesn't render `.dialog__context`; only dialogs passed a `context` prop to the shared `Dialog` (ui.tsx) render `<p class="dialog__context">`. Both `.dialog__title` + `.dialog__context` sit inside `.dialog__header` (which has `padding-right: 32px`), so the enlarged context clears the close-X (no overlap — the screenshot tightness is just narrow-dialog width). (5) **Plan line-numbers were exact** — only nit: the L211 settings divider's real selector is `.settings-group` (plan said `.settings-group__label`); same line, unambiguous. (6) **Verification**: subagent text verdict PASS, but per the M25 border/contrast lesson the controller DIRECTLY reviewed the 3 borderline thin-rule/contrast PNGs (data-table dividers, dialog context, card grid) — all confirmed PASS. The large `m12` pixel-diffs (0.15–0.45) were **M26/M27 baseline drift** (baselines last promoted at M25, before unified Discover/nav-groups/journal-affordances), NOT M28 regressions — all 18 m25/m12 surfaces render coherently. Plan: [`docs/superpowers/plans/2026-06-14-audioshelf-m28-visual-consistency-ii.md`](docs/superpowers/plans/2026-06-14-audioshelf-m28-visual-consistency-ii.md). **NEXT: M29 (Player & Onboarding micro-polish) — plan first (`[ ] Not started`).**

- **M28 planned (2026-06-14 — `📝 Plan ready`, execute next) — Visual Consistency II (VIS7-1…10):** the v7 visual-consistency capstone, **FE-only** (no Rust/schema/dep/capability — diff-stat of `src-tauri`/`Cargo.*`/`package*`/`capabilities` must stay EMPTY; schema stays `LATEST`=10; read-only-on-disk; fixtures 43/44/47). Two parallel Explore digests over the live post-M27 build pinned the exact scope. **The dominant finding (VIS7-1/2/10, all H): 16 structural borders still use the near-invisible weak `--color-border` (dark `#26364a`) after M25** — enumerated with file:line (base.css L23 inputs; layout.css L4/29/35/47/51 sidebar/search/kbd/list-rows/player-bar; components.css L10/17/83/95/131/135/136/158/177/211/242/260/263 button/card/data-table/player-utility/tab/palette/palette-input/bulk-bar/collection-row/settings-divider/now-playing-rows/speed controls). **Design decision — the "border-strength context rule" VIS7-10 asks for:** a new **per-theme semantic token `--color-divider`** (dark→`var(--color-border-strong)` because weak is invisible *only* on dark; light→`var(--color-border)` since `#d3dde9` already reads & strong would be too heavy; HC→strong) — migrate all 16 structural usages to it; `--color-border` demoted to reserved hairline, `--color-border-strong` stays the explicit emphasis tier (work-card *hover*). Other tokens added in T1: `--shadow-card-hover` (3 themes, VIS7-9), `--icon-sm/md/lg` (VIS7-4), `--chip-gap`=8px (VIS7-5). Remaining items are scoped audits: VIS7-3 secondary-text = collapse ad-hoc text-`opacity` onto the single `--color-text-muted` tier (there is **no** `--color-text-secondary` token); VIS7-7 `.dialog__context` 0.85→0.9rem + more margin; VIS7-6 small-avatar initials contrast; VIS7-8 expanded-player `clamp()` gap. **The M25 artwork cover-placeholder music glyph is intentionally %-of-tile — left EXEMPT from the icon scale (documented in CSS).** 12 serial tasks (T1 tokens additive → T2 apply border rule → T3–T9 audits → T10 `m28` walkthrough → T11 verify → T12 PR). **Baked-in gotchas:** 🔴 frozen build — `npm run build` THEN `cargo tauri build --debug` before `verify.ps1 -SkipBuild` (FE is EMBEDDED; `npm run build` alone runs stale); no `getCurrentWindow().setSize()` (scroll for tall shots); 🔴 border/contrast criteria are where a single screenshot-subagent verdict is unreliable (M25 white-glyph/faint-divider lesson) → controller direct-reviews those PNGs; `runner.test.ts` enumerates the walkthrough names list so register `"m28"` + keep step names stable; tokens added once in `:root` for theme-independent ones (`--icon-*`, `--chip-gap`), in all 3 blocks for color/shadow. Plan: [`docs/superpowers/plans/2026-06-14-audioshelf-m28-visual-consistency-ii.md`](docs/superpowers/plans/2026-06-14-audioshelf-m28-visual-consistency-ii.md). **NEXT: execute M28.**

- **M27 (✅ shipped, PR #68) — Reflection that Connects, all 10 plan tasks via Sonnet subagents:** made the *records* (Journal, Insights) actionable with **NO schema migration** (`db::LATEST` stays **10**; db.rs/Cargo/package/fixture_scan diff-stat all EMPTY; read-only-on-disk; fixtures 43/44/47). Gates: `tsc` clean · **486 FE** (+22) · `cargo test` **173 lib** (+7) + integration green · CI 3m15s. **Durable decisions/gotchas this run:** (1) **CUR-2 play-from-here is pure FE** — sets `pendingSeekRef.current` to the note's `positionSecs` *before* `playChapter` so it beats the chapter's own resume point; the real `authors` state is `AuthorRow[]` (no `.works`) so the handler resolves via `getAuthors()`+`getAuthorDetail()` exactly like `playChapterById` (the plan's `for (d of authors)` snippet was illustrative). (2) **CUR-5 has no work-id→`Vec<ScopedWork>` helper** — `run_scoped_query` takes a DSL `ParsedQuery`, so `played_in_range` replicates the per-work aggregation SQL inline (same `ScopedWork` field set). The local-day trap: `WeekPoint` had no absolute ms → added additive `week_start_ms` (computed via existing `day_to_utc_midnight_ms`), `DayCell.dateMs` already existed — **no DB change**. Library pre-filter reuses the App-level `setLabelFilter({facet:'tag',value})` state (M26 path), no second filter mechanism. (3) **CUR-10 stayed surgical** — changed only `discovery_for_you`'s seed selection + reason text (cold-start fallback invoked verbatim when no rated/re-entered work); Reflections counts run in `compute_insights` (the IO boundary) AFTER pure `build_insights` returns — did not thread through the pure fn. (4) **IA7-7 `has_journal`** appended LAST in BOTH chapter loaders (`load_chapter_row` + `query_author_detail`) to avoid positional `row.get(i)` shift; required adding `hasJournal:false` to ~9 FE test fixtures (now-required field). (5) 🔴 **THE BIG LESSON — the frozen `cargo tauri build --debug` EXE EMBEDS the frontend; `npm run build` alone does NOT update the running binary.** `verify.ps1 -SkipBuild` runs the stale EXE, so EVERY FE walkthrough/capture edit silently had ZERO effect (~10 wasted iterations + several confusing byte-identical `diff=0.0000` shots and a misread "scroll/resize doesn't work" rabbit hole) until I re-ran `cargo tauri build --debug` to RE-EMBED. **Rule: after ANY FE change, rebuild the frozen EXE (`npm run build` THEN `cargo tauri build --debug`) before `verify.ps1 -SkipBuild` — or run verify WITHOUT `-SkipBuild`.** Corollary already in memory ([[reference-tauri-devmode-localhost]]) but it bit hard here. (6) **`getCurrentWindow().setSize()` is NOT permitted** under the current capabilities (`core:default`+dialog) — it silently no-ops / rejects (caused a walkthrough timeout once the rebuilt EXE actually ran it); don't resize the window for tall captures, **scroll** instead. (7) **Tall author-detail capture**: the creator header + labels editor pushes a work's single chapter row below the fold behind the fixed PlayerBar → the m27 affordance step expands works then `scrollIntoView({block:'center'})` on the journal row; `.app-main` is the scroll container (`overflow:auto`). (8) **Determinism**: notes/bookmarks are append-only (no unique constraint) so `addChapterNote`/`addBookmark` ACCUMULATE across walkthrough runs (the app-data DB persists) — the m27 seed now CLEARS existing notes/bookmarks (`getChapterJournal`→`deleteChapterNote`/`deleteBookmark`) before adding exactly one of each; SET ops (summary/rating/re-entry/tags) and `seedPlayEvents` (insert-or-ignore) were already idempotent. (9) **Verification**: a single screenshot subagent UNDER-reads the small low-contrast journal glyph on the chapter row (two subagents split PASS/FAIL) — controller settled it by direct review (the [[wpfui-theming-and-visual-verification]]/M25 white-on-color-glyph lesson). Plan: [`docs/superpowers/plans/2026-06-14-audioshelf-m27-reflection-that-connects.md`](docs/superpowers/plans/2026-06-14-audioshelf-m27-reflection-that-connects.md). **NEXT: M28 (Visual Consistency II) — plan first (`[ ] Not started`).**

- **M26 (✅ shipped, PR #66) — Discovery & Curation Coherence / Unified Labels, the v7 foundation, all 13 tasks via Sonnet subagents (backend-first):** generalized M21's metadata model into ONE user-defined Types & Labels system (additive — not a rebuild). **Schema v10** on the M16 `run_step`/`user_version` runner (`LATEST` 9→10, `SCHEMA_V1` untouched, no FK-off rebuild, table-count +2: `label_types` + `work_metadata`); the migration **COPIES** the three legacy `*_tags` tables into `metadata_terms(facet='tag')`+attach via `INSERT OR IGNORE` and leaves the legacy tables **dormant/recoverable (NOT dropped)** — `metadata_terms UNIQUE(facet,value)` makes the copy collision-safe. Invariants HELD: no new dep (Cargo/package diff-stat empty), read-only-on-disk (zero new `std::fs` writes), fixtures 43/44/47 (`fixture_scan.rs` untouched; all M26 state runtime-seeded in the `m26` walkthrough). **Durable decisions/gotchas this run:** (1) **work labels are DIRECT** (`work_metadata`) not chapter-union — `query_author_detail` now reads `labels_for(table,key,id)` per scope and DERIVES `tags`=facet=='tag' / `metadata`=facet!='tag' (back-compat fields kept so component/test churn stayed bounded); a work no longer auto-aggregates its chapters' metadata (intentional per plan §4; one Rust test was updated to assert the new direct semantics). (2) **`is_valid_facet` is now DB-backed** (`EXISTS label_types`) so several Rust tests that opened at v7/v8/v9 and exercised the metadata path had to move to `open_in_memory()` (latest) — `label_types` only exists at v10. (3) **scoped/search EXISTS must include all THREE attach arms** (work+chapter+author); the pre-existing `p.meta` clause was missing the `work_metadata` arm (narrator-on-work never matched) — fixed. (4) **`parse_query` now treats ANY `word:value` bareword (except reserved `duration:`/`status:`) as `MetaFilter{facet,value}`** — kept option-B (`tags: Vec<String>` field retained, scoped emits the same unified EXISTS with `facet='tag'`). (5) **Old editors/managers:** `TagManagerView`/`MetadataManagerView` deleted (merged into `LabelManagerView`); `TagEditor`/`MetadataEditor` component files KEPT as a gated fallback (LabelEditor activates when `labelTypes` is passed) — harmless dead path, removable later. (6) **`m21` walkthrough step bodies auto-repointed** (the unified manager/editor render at the same nav targets) — exported step NAMES unchanged so `runner.test.ts` stayed green (M22's lesson). (7) **Verification-caught (screenshot subagent):** the `LabelManagerView` card renders below the Settings fold → added `className="label-manager"` to its `<Card>` + a `scrollIntoView` in the m26 step; the Library type→value filter is **tag-facet-effective only** (`AuthorRow` deliberately carries only `tags`, no `labels`) so filtering by a custom/narrator/language type yields an empty creators list — cross-facet discovery is via **plain search** (matches label values) instead, and the m26 filter shot now uses Tag→cozy for a non-empty result. (8) `BUILTIN_FACETS` const kept (plan-mandated guard) under `#[allow(dead_code)]`. Gates: `tsc` clean · **464 FE** · `cargo test` **166 + integration** green · CI `build-and-test` 2m57s · `m26`(6)+`m12`(15)+`m21`(5)+`m24`(7)+`m25`(3) frozen-build → subagent verdict **PASS**. Plan: [`docs/superpowers/plans/2026-06-14-audioshelf-m26-unified-labels.md`](docs/superpowers/plans/2026-06-14-audioshelf-m26-unified-labels.md). **NEXT: M27 (Reflection that Connects) — plan first (`[ ] Not started`).**

- **v7 arc defined + M26 planned (2026-06-14 — `📝 Plan ready`, execute next) — Discovery & Curation Coherence (Unified Labels):** v6 (M22–M25) is COMPLETE and an Explore audit confirmed its milestones **fully absorbed the original M26 "Power, Calm" residue** (admin-demote/bulk-select discovery/Ctrl+K affordance/Search-tips/first-run guidance all shipped in M22/M23) → **M26 redefined**, not a thin catch-all. Owner picked direction via two AskUserQuestion forks: (1) *fresh v7 UX review* over the *current* post-M25 build (not stale residue), then (2) **Discovery & Curation Coherence** as the lead, then (3) **one unified user-defined label system**. Ran a **five-lens** UX/UI review (onboarding/calm · listening-loop/player · IA/nav/discoverability · visual/consistency · curation/reflection), each lens grounded in the current `.shots/m12`(15)+`.shots/m21`(5)+`.shots/m24`(7)+`.shots/m25`(3) PNGs (post-M25, captured 2026-06-13 23:27–23:35 during M25 verify — no re-capture needed) + FE/Rust source; ~50 findings deduped into the [v7 backlog](docs/superpowers/specs/2026-06-14-audioshelf-v7-discovery-curation-backlog.md). **Dominant cross-lens finding (all of curation+IA+onboarding):** six overlapping organizing systems + three disconnected discovery surfaces, no mental model. **Owner's exact intent:** type a word ("talk show") on an audio or in config → it becomes a filter offered everywhere + plain-search-matchable; keep collections + more-like-this; "simple metadata (added through the UI) drives all of it"; **Narrator & Language are distinct built-in types, Mood is just a label like everything else; one unified system, simple**. **Key architectural insight that makes M26 additive (not a rebuild):** the M21 metadata model is ALREADY a general facet/value + attach-table system — only the fixed `FACETS` const, the missing work-level attach, and the parallel free-form tags make it special. So M26 = **generalize M21** (new `label_types` table for user-definable types; add `work_metadata`) + **migrate tags in additively** (copy `*_tags` → `metadata_terms(facet='tag')`+attach via `INSERT OR IGNORE`; **legacy tag tables left dormant/recoverable, NOT dropped** — honors destructive-op discipline) + **repoint every consumer** (plain `search_library` matches label values; `parse_query` generic `type:value` keeping `tag:`/`narrator:`/`language:`/`mood:`; `run_scoped_query` EXISTS → unified attach over work+chapter+author; discovery + more-like-this over unified labels; collections keep resolving because the DSL is repointed) + **merge 2 editors→`LabelEditor`, 2 managers→`LabelManagerView`, Discover tag/facet pickers→one labelled picker**. Model gains `labels: Vec<MetaTag>` (all facets); `tags`/`metadata` kept **derived** for back-compat to bound test churn. **Schema v10** additive on the M16 `run_step`/`user_version` runner (`LATEST` 9→10; `SCHEMA_V1` untouched; no FK-off rebuild; table-count +2). Invariants: no new dep, read-only-on-disk, fixtures 43/44/47. 13 serial tasks (backend-first). **Gotchas baked into the plan:** `cmd //c` (not `/c`) in the Bash tool; no `cargo test`/`tauri dev` between the frozen `cargo tauri build --debug` and `verify.ps1 -SkipBuild` (re-creates a dev-mode "localhost refused" exe); grep & bump the ~13 hardcoded `== 9`/`_9_`/`open_at_version(9)` asserts across db.rs+commands.rs to 10; discovery is unplayed-only so seed labels onto a work with an UNPLAYED chapter (M21's trap); `metadata_terms UNIQUE(facet,value)` makes the tag-copy collision-safe; keep each repointed `m21` walkthrough step's exported NAME so `runner.test.ts` stays green. Plan: [`docs/superpowers/plans/2026-06-14-audioshelf-m26-unified-labels.md`](docs/superpowers/plans/2026-06-14-audioshelf-m26-unified-labels.md). **NEXT: execute M26.**

- **M27 planned (2026-06-14 — `📝 Plan ready`, execute next) — Reflection that Connects:** makes the *records* (Journal, Insights) actionable. Two Explore digests over the post-M26 build confirmed every backlog item (CUR-2/5/10, IA7-3/6/7) is achievable with **NO schema migration** — each is a read-only query, a computed field on an existing query, or UI wiring; `db::LATEST` stays **10**. **Key architecture facts the plan leans on:** (1) **CUR-2 play-from-here reuses the in-memory resolution** already behind `playChapterById` (App scans the loaded `authors` for the chapterId) + sets `pendingSeekRef` to the note's `positionSecs` *before* `playChapter` so the note position takes precedence (same mechanism as a bookmark seek) — no backend. (2) **CUR-5 = focused results panel** (owner-locked): new read-only `query_played_in_range(start_ms,end_ms)→ScopedResults` over `play_events` joined chapters→works→authors, rendered by the EXISTING generic `ScopedResults` grid in a thin `PlayedRangeView` under a new `{kind:"played-range"}` route (shellRoute→insights); top-tag drill-down reuses the M26 Library label-filter path — **no** date-filter UI on Library, **no** author-range query. (3) **CUR-10 = Discover + small Insights stat** (owner-locked): ratings (`works.completion_rating`) + re-entry (`works.re_entry_note`) — both work-level text columns from M17/v6 — seed/boost `home_recommendations`/`discovery_for_you` with a "Because you rated…/You came back to…" `DiscoveryWork.reason` (cold-start fallback preserved) + two additive `worksRated`/`worksReEntered` counts on the InsightsData payload (computed in `compute_insights`, not a DB column) shown as a "Reflections" stat tile; **Collections untouched.** (4) **IA7-7** = additive computed `has_journal` bool on `ChapterRow` (OR of `user_summary<>''`/`takeaway<>''`/`is_favorite=1`/EXISTS notes/EXISTS bookmarks) appended LAST in the `load_chapter_row` SELECT to avoid positional-index shift → journal icon on annotated chapter rows opening the existing `ChapterJournalDialog`. (5) **IA7-3** back-nav + **IA7-6** Collections→Browse group are small `onBack`/AppShell-`groups` edits following the M22 prior art. **The one real trap baked into the plan:** Insights uses **local-day indices** (`tz_offset_minutes`), so heatmap/rhythm→ms windows must use `DayCell.dateMs` (already present) and add an absolute `weekStartMs` to `WeekPoint` if missing (additive payload field, still no DB change) — STOP rather than ship an off-by-a-day drill-down. Invariants asserted as gates: no new dep, read-only-on-disk, fixtures 43/44/47 (all M27 journal/rating/play_events state **runtime-seeded idempotently** in the new `m27` walkthrough — DB persists across runs). 10 serial tasks (CUR-2 → IA7-3 → IA7-6 → IA7-7 → CUR-5 backend → CUR-5 FE → CUR-10 → walkthrough → verify → PR). Plan: [`docs/superpowers/plans/2026-06-14-audioshelf-m27-reflection-that-connects.md`](docs/superpowers/plans/2026-06-14-audioshelf-m27-reflection-that-connects.md). **NEXT: execute M27.**

- **M25 (✅ shipped, PR #63) — Visual Polish & Design-System Consistency, the v6 capstone (re-skins the final post-M24 structure), all 12 tasks via Sonnet subagents:** invariants HELD — **FE-only** (`git diff --stat main` of `src-tauri`/`Cargo.*`/`package*`/`capabilities.json` EMPTY; schema stays `LATEST`=9; no new dep; no capability), read-only-on-disk (zero new fs writes), fixtures 43/44/47 (`fixture_scan.rs` untouched; the m25 walkthrough seeds saved-searches at runtime). Shipped VIS-1…VIS-9 + CL-8: VIS-1 designed cover-art placeholder (music glyph behind initials on large `size>=64` slots only; small swatches initials-only); VIS-2 accent discipline (recommendation/affinity → `--color-success` teal; `#218bff` reserved for interactive); VIS-3 legible chips (new `--color-chip-bg`/`-hover`/`-border` tokens in **all** theme blocks + `.chip--on` solid accent — fixes the ~16%-alpha invisible pill); VIS-4 expanded-player two-column grid + row separators + sentence-case capture label; VIS-5/CL-8 eyebrow/heading consistency (routed loose eyebrows through `SectionHeading`; audit found **none were pure restatements** — M22/M23 had already cleaned them); VIS-6 new styled `Select` primitive (`src/components/Select.tsx`, `role=listbox` + full keyboard nav) adopted in **content selects only** (SortFilterBar sort+tag-filter, player sleep ×2, AuthorDetail work/chapter sorts — Settings/Tag/Metadata natives left per owner decision); VIS-7 creator-header redesign (identity band + CTA top-right → divider → tags/metadata); VIS-8 saved-search single-row scroll strip; VIS-9 rename-table polish (muted no-change rows, success-teal "already clean" badge, calmer eyebrow + reversibility kept in the blurb). Gates: `tsc` clean · **421 FE** (was 401, +20) · `cargo test` **146 unit + integration** green & unchanged · CI `build-and-test` pass 3m0s · `m12`(15)/`m21`(5)/`m24`(7)/`m25`(3) frozen-build → Sonnet subagent verdict. **Three verification-caught findings, resolved before merge:** (1) **VIS-1 glyph at `opacity .30` read as ~15–20% (too faint)** → bumped to `.5` (+ width 52→54%); re-captured, confirmed clearly visible on the no-cover A5/QH tiles. **(2) VIS-7 divider used `1px solid var(--color-border)` (#26364a) — effectively invisible against the dark card surface** → switched to `var(--color-border-strong)` (#36506d); re-captured, confirmed a clear rule between the identity band and the tags. **(Durable: on the dark theme a `var(--color-border)` separator is near-invisible — use `--color-border-strong` for any divider that must read.)** (3) **VIS-9 row-muting could not be screenshot-demonstrated** — the m12 rename fixture is **all "already clean" (noop)** so there is no rename-row brightness baseline; the `.rename-row.rename-noop td` muted rule + teal badge are correct in source and the table reads calm (VIS-9's actual goal). Source-confirmed, not a defect. **Durable gotcha: two screenshot subagents gave inconsistent FAIL/ABSENT reads on the low-contrast white-glyph-on-saturated-tile and the faint divider — borderline visuals the controller had to settle by *direct* image review. White-on-color glyphs and subtle dark-theme separators are exactly where a single-pass subagent verdict is unreliable; when two verdicts disagree on a low-contrast detail, the controller should look once itself.** Also: `<Cover>` component is used NOWHERE (the `<Swatch>` large-fallback gap the plan flagged for T2 is dead-path; all large art flows `WorkArtwork`→`Artwork`→glyph). **ROADMAP-update gotcha (hit this run): `gh pr merge` leaves a merge commit on the *remote*; `git reset --soft HEAD~1` after a *blocked* local commit rewinds local main to the merge's first parent (pre-milestone) — re-derive local main with `git reset --hard origin/main` and branch the docs PR from there.** v6 (M22–M25) COMPLETE. **NEXT: M26 (Power, Calm — optional catch-all; may fully fold into M22/M23) or co-scope the next milestone/version with the user.**

- **M25 planned (2026-06-13 — `📝 Plan ready`, execute next) — Visual Polish & Design-System Consistency, the v6 capstone (re-skins the final post-M24 structure):** plan written from one Explore digest (v6 backlog VIS-1…VIS-9 + verbatim FE excerpts: `tokens.css`/`components.css`, `Cover.tsx`, `Menu.tsx`, `ui.tsx`, `NowPlayingPanel.tsx`, `PlayerBar.tsx`, `AuthorDetailView.tsx`, `SortFilterBar.tsx`, `LibraryView.tsx`, `RenameView.tsx`, harness). **FE-only — hard gate: `git diff --stat main` of `src-tauri`/`Cargo.*`/`package*.json`/`capabilities.json` must be EMPTY** (no Rust, schema stays `LATEST`=9, no dep, no capability), read-only-on-disk (zero new fs writes), fixtures 43/44/47. Ships VIS-1…VIS-9 + CL-8 broad in **12 serial tasks**: T1 accent discipline (move recommendation/affinity → `--color-success` teal; reserve `#218bff` for interactive) + chip legibility (new `--color-chip-bg`/`-hover`/`-border` tokens in **every** theme block; inactive pill ~9% white fill + `--color-text`, selected `.chip--on` solid accent — fixes the ~16%-alpha invisible pill); T2 designed cover-art placeholder (`music` glyph @ ~30% white behind initials, **large slots only `size>=64`**, small inline swatches stay initials-only); T3 new **styled `Select` primitive** (`src/components/Select.tsx`, value-showing trigger + `role=listbox` popover reusing `.menu__popover` + Menu keyboard pattern; new `chevron` glyph); T4 adopt Select in **content selects only** (player sleep ×2, Library sort + tag-filter, Author-Detail sorts — **Settings/Tag/Metadata-manager natives left by owner decision**); T5 expanded-player two-column art/title grid (kills the avatar/title overlap flagged since M22) + bookmark-row separators + rename raw "CAPTURE" label; T6 creator-header redesign (identity band → divider → tags/metadata, CTA top-right); T7 saved-search single-row overflow strip; T8 rename-table polish (muted no-change rows, teal "already clean"); T9 eyebrow/heading consistency (drop redundant eyebrows = CL-8, fix Discover artifact); T10 CSS/glyph consolidation; T11 `m25` walkthrough (styled-Select-open, saved-search strip, placeholders — m12's 15-shot matrix remains the primary visual regression check, mirroring how M13/M15 verified); T12 verify (full gates + EMPTY dep-diff + frozen `cargo tauri build --debug` → `m25`/`m12`/`m21`/`m24` → **Sonnet subagent text verdict**, never load PNGs into the controller). **Two owner design forks (one batched AskUserQuestion):** (1) cover placeholder = **glyph + initials tile** (vs refined-initials / generative pattern); (2) native-select replacement = **content selects only** (vs replace-all / CSS-only). **Gotchas baked into the plan:** `cmd //c` (not `/c`) in the Bash tool; no `cargo test`/`tauri dev` between the frozen build and `verify.ps1 -SkipBuild` (re-creates the dev-mode "localhost refused" exe) — simplest is run the first walkthrough WITHOUT `-SkipBuild`; chip tokens must be added to **all** theme blocks (default dark + `[data-theme=light]` + high-contrast) or light/HC chips break; a native `<select>` popup is OS-rendered and won't screenshot (expected, not a FAIL); any in-scope-ambiguous `<select>` → STOP and report. Plan: [`docs/superpowers/plans/2026-06-13-audioshelf-m25-visual-polish.md`](docs/superpowers/plans/2026-06-13-audioshelf-m25-visual-polish.md). **NEXT: execute M25.**

- **M24 (✅ shipped, PR #60) — The Listening Loop & Player, the v6 third milestone, all 12 tasks (9 commits) via Sonnet subagents:** invariants HELD — **schema v9** additive-only (one `ALTER TABLE chapters ADD COLUMN playback_position_secs INTEGER NOT NULL DEFAULT 0` on the M16 `run_step`/`user_version` runner; `LATEST` 8→9; `SCHEMA_V1` untouched; no FK-off rebuild; new tests `migration_v9_adds_playback_position_and_is_additive` + `open_at_version_9_reaches_latest`; table-count assert stayed 14). **No new dep** (`git diff --stat main` of `Cargo.*`/`package*.json` empty), **read-only-on-disk** (writes only settings `playback_speed` / the new column / play_events; no file export; Rename stays sole audio mutation), **fixtures 43/44/47** (`fixture_scan.rs` untouched; all M24 state seeded at runtime in the new 7-shot `m24` walkthrough). Shipped PL-1…PL-10 broad + per-second resume exactly as planned. **Two verification-caught findings, both resolved before merge:** (1) **PL-2 last-chapter detection was wrong** — `canPlayNext` compared `chapter.chapterNo < workTotalChapters`, but **grouped works repeat chapter numbers (1,2,2,3,3)** so on the last chapter `3 < 5` was true and the chapter-end actions ("Mark work complete" / "More by [author]") never appeared. Fixed to mirror `playNextChapterRef` exactly — a next chapter exists iff the current chapter is **not the last *position*** in `currentWorkChaptersRef` (so the button's presence and the next handler can never desync). Re-captured `05-last-action` → PASS. **(Durable: never use `chapterNo` as a last-chapter/index test — chapter numbers are grouping-derived and non-unique; use array position.)** (2) **PL-9 "−0:02" was a screenshot FALSE POSITIVE** — the verifier flagged the `m12/14-player-chapters` remaining-time label as a "negative elapsed" defect, but that shot **intentionally** cycles the time label to *remaining* mode (`showPlayerChapters` comment: "so the shot shows '-m:ss'") and "−0:02" = 2s genuinely left, the by-design `-m:ss` format; PL-9 only prohibits "−0:00" (unit-tested in `playback.test.ts`). Marked verified-not-a-defect, no code change. **(Durable: the remaining-time toggle legitimately renders "-m:ss"; only "-0:00" is the bug.)** Per-second resume proven by the Rust tests + the `savePlaybackPosition` wrapper (NA for screenshot, as planned). PL-7 "End of chapter" sleep option is in source for both `PlayerBar`/`NowPlayingPanel` but unscreenshotable (native `<select>` popup is OS-rendered) → source-confirmed, not FAIL. Gates: `tsc` clean · **401 FE** · `cargo test` **146 + integration** green · CI `build-and-test` pass 2m53s · `m24`(7)/`m12`(15)/`m21`(5) frozen-build → Sonnet subagent verdict **PASS** (m12/m21 regression matrices intact — uniform ~0.3 pixel-diff across untouched screens was font/AA drift, not regression). **NEXT: plan M25 — Visual Polish & Design-System Consistency** (FE-only; built last so it re-skins the final structure: designed cover-art placeholders, accent-color discipline, chip/pill legibility, expanded-player + creator-header redesign).
- **M24 planned (2026-06-13 — `📝 Plan ready`, execute next) — The Listening Loop & Player, the v6 third milestone (core daily experience):** plan written from a two-Explore digest (v6 backlog player findings + verbatim player-code excerpts) — exact file:line anchors for `src/App.tsx` playback state/handlers, `src/player/{playback.ts,PlayerBar.tsx,NowPlayingPanel.tsx}`, `src-tauri/src/{db.rs,model.rs,commands.rs}`. Ships **all 10 PL findings broad** (PL-1 speed · PL-2 chapter-end actions · PL-3 Start/Keep labels · PL-4 scannable chapter states · PL-5 elevate next-chapter title · PL-6 faster chapter access · PL-7 sleep countdown + end-of-chapter · PL-8 mute affordance · PL-9 −0:00 bug + scrubber hit-area · PL-10 pop-out clarity). **Two owner forks (one batched AskUserQuestion):** (1) **fold in per-second mid-chapter resume** → **schema v9** (first migration since M21's v8) additive `chapters.playback_position_secs` on the M16 `run_step`/`user_version` runner; resume position is *applied when a chapter loads* (reusing M17's `pendingSeekRef`/`onLoadedMetadata` seek, bookmark seek keeps precedence) but **never auto-starts playback** (no-autoplay non-goal intact). (2) **all 10 findings broad** (M16–M21 pattern). **Key framing that made the fork clean:** playback speed persists via the existing `settings` table (`playback_speed` key, FE-only) — **per-second resume is the ONLY thing forcing a migration**, so if resume were parked M24 would be no-migration. **Invariants (hard gate):** v9 purely ADDITIVE (one `ALTER TABLE … ADD COLUMN`, `SCHEMA_V1` untouched, no FK-off rebuild); **no new crate/npm dep** (`git diff --stat main` of `Cargo.*`/`package*.json` must be EMPTY); read-only-on-disk (every write SQLite — settings/the new column/play_events; no file export; Rename stays sole audio mutation); fixtures 43/44/47 (all M24 state seeded at runtime in a new `m24` walkthrough). **Architecture decided:** `ChapterRow` gains `playback_position_secs` (model.rs + **two** struct-literal sites — `query_author_detail` + `load_chapter_row` — + their SELECTs, grep-verified no other literals); new `save_playback_position` command (throttled ~10s on `onTimeUpdate` + flush on pause); `mark_finished` zeroes the column on finish so finished chapters restart; `LATEST` 8→9 + **13 hardcoded `== 8` test asserts bump to 9** (10 in db.rs incl. renaming `open_at_version_8_reaches_latest`→`_9_`, 3 in commands.rs) + 2 new v9 tests; table-count assert stays 14 (column, not table). FE: speed via cycle-button (compact) + segmented control (expanded) with a persisted `playbackSpeed` re-applied in `onLoadedMetadata` (loading a new src can reset `playbackRate`); mute via `audio.muted` (preserves the volume slider); sleep gains an "End of chapter" option + a 1s-interval live countdown; chapter-end actions are **user-initiated buttons** ("Play next chapter →" non-last / "Mark work complete" + "More by [author]" last); Start-vs-Keep label derives from played count; Library rows get a play→next-unplayed button (reuse the M13 work-card pattern). **12 serial tasks** (Rust backend first on shared `db.rs`/`model.rs`/`commands.rs`/`lib.rs` → `playback.ts` + `api.ts` helpers → App.tsx wiring → PlayerBar → NowPlayingPanel → 3 view tweaks → CSS → harness → verify). **Read-first instructions** for `HomeView`/`AuthorDetailView`/`LibraryView` (not extracted into the plan — executor reads then applies precise transform specs). Verify: full gates + EMPTY dep-diff gate + frozen `cargo tauri build --debug` → `m24`/`m12`/`m21` walkthroughs → **Sonnet subagent text verdict** (never load PNGs into the controller); per-second resume proven by Rust tests, not a screenshot (NA, not FAIL). **Gotchas baked into the plan for the executor:** `cmd //c` (not `/c`) in the Bash tool; no `cargo test`/`tauri dev` between the frozen build and `verify.ps1 -SkipBuild` (re-creates a dev-mode exe → "localhost refused"); the `playbackPositionSecs:0` field must be added to any TS `ChapterRow` literal in tests/harness (grep `chapterNo:`); add `volume`/`mute`/`list` Icon glyphs if missing (M18 single-`<path>` pattern, fallback `d=` strings provided). Plan: [`docs/superpowers/plans/2026-06-13-audioshelf-m24-listening-loop-player.md`](docs/superpowers/plans/2026-06-13-audioshelf-m24-listening-loop-player.md). **NEXT: execute M24.**
- **M23 planned (2026-06-13 — `📝 Plan ready`, execute next) — Clarity & Onboarding, the v6 second milestone:** the first-five-minutes + plain-language sweep. Plan written from an Explore digest of the live FE (exact file:line + literal strings) + the v6 backlog clarity/onboarding lens (CL-1…CL-9). **FE-only copy/markup/CSS** (no Rust/schema/dep/capability; `LATEST` stays 8; read-only-on-disk; fixtures 43/44/47), **8 serial tasks**: (1) extend the `ui.tsx` `Dialog` primitive with optional visible `title`+`context` (today `label` is aria-only, dialogs render NO visible heading) + `.dialog__header` CSS — dependency of task 6; (2) Home — replace the doubled `"Choose your library / Go to Settings"` CTA with `"Set up my library"` + gate the unconditional "Your listening" stats grid behind `!noHistory` (was showing `0m/0/0 days`, reads as broken); (3) first-run reassurance on Settings + Scan; (4) rewrite developer-grade empty states (Discover `"Personalized picks — needs listening history"` → context-aware per section, Journal/Library/ScopedResults/Collections); (5) de-jargon (`SettingsView` "Metadata vocabulary"→"Narrator, Language & Mood", "scoped query"→"filter"; `RenameView`/`TagManagerView` "canonical"→"standardized") + a tree-wide grep for stragglers; (6) pass `title`+`context` to the ~5 `Dialog` call sites (Edit grouping, Edit tags & metadata, Chapter journal, Bulk tag, + confirm NowPlayingPanel uses the primitive); (7) tags-vs-metadata-vs-collections point-of-use `.field-hint` microcopy on Author Detail. **Two re-audit corrections baked into the plan so the executor doesn't redo shipped work:** (a) **CL-7** (replace `window.prompt("Save search as:")`) was **already done in M22** → verify-and-drop (grep `window.prompt(`); (b) **CL-3 nav-gating is already handled** — first run renders `standalone` (no `AppShell`/sidebar; `App.tsx` ~2423 `const standalone = … || (route.kind==="settings" && route.firstRun)`), so there's nothing to gate → CL-3 reduces to reassurance copy. **CL-8** (eyebrow audit, Low) folded-or-deferred to M25. Verify: `tsc`/`npm test`/`cargo test`(unchanged) + **FE-only diff gate must be EMPTY** + `home`/`settings`/`m12`/`m21` against a **frozen** `cargo tauri build --debug` → Sonnet subagent text verdict (never load PNGs into the controller). Plan: [`docs/superpowers/plans/2026-06-13-audioshelf-m23-clarity-onboarding.md`](docs/superpowers/plans/2026-06-13-audioshelf-m23-clarity-onboarding.md). **NEXT: execute M23.**
- **M23 (✅ shipped, PR #57) — Clarity & Onboarding, the second v6 milestone, all 8 tasks (8 commits) via Sonnet subagents:** invariants HELD — **FE-only** (`git diff --stat main` of `src-tauri`/`Cargo.*`/`package*.json`/`gen/schemas/capabilities.json` EMPTY), no schema (`LATEST` stays 8), no dep, read-only-on-disk, fixtures 43/44/47. Shipped: (1) `Dialog` primitive (`ui.tsx`) gained optional visible `title`+`context` header — **`useId()`-based labelling** (not `Math.random()`), backward-compatible (call sites passing only `label` render unchanged), `aria-labelledby` when titled + `.dialog__header/__title/__context` styles; (2) Home — doubled "Choose your library / Go to Settings" → single "Set up my library" primary (CL-1) + zeroed `0m/0/0 days` stats grid hidden on first run via `!noHistory` gate (CL-2); (3) first-run reassurance copy on Settings (`firstRun &&` block: "never moves, renames, or changes them" + what-happens-next) and Scan ("Your files are never changed — this just builds your shelf") — **copy-only**, no nav-disable logic since the `standalone` first-run render already has no AppShell (CL-3); (4) context-aware empty-state rewrites — DiscoveryView `WorkList` empty made prop-driven (`emptyTitle`/`emptyBody`) so tag/facet/for-you each get correct words (was the wrong "Personalized picks…" for all three), + Journal/Library/ScopedResults/CollectionsView (CL-4); (5) de-jargon: "Metadata vocabulary"→"Narrator, Language & Mood", "canonical"→"standardized" (RenameView, TagManagerView), Collections "scoped query"→plain "filter"/"Query" label→"Filter"; **grep sweep also caught "Schema drift detected"→"Database version mismatch"** in SettingsView health scan (CL-5); (6) visible `title`+`context` at every `Dialog` call site — AuthorDetailView (edit grouping / "Edit tags & metadata" / more-like-this), ChapterJournalDialog, BulkTagDialog, NowPlayingPanel (title only, no context by design, CL-6); (7) tags-vs-metadata-vs-collections point-of-use `.field-hint` microcopy above the Author Detail TagEditor + MetadataEditor (CL-9). **Re-audit corrections confirmed (no rework):** CL-7 (`window.prompt("Save search as:")`) already shipped in M22 → grep-verified gone, dropped (2 *unrelated* `window.prompt`s remain in `App.tsx` — "Add note:"/"Bookmark label" quick-capture from the player transport, out of CL-7 scope, left as-is — candidate for a future inline-form pass); CL-3 nav-gating already handled by `standalone` render → no dead nav-disable code added; CL-8 eyebrow audit deferred to M25. Gates: `tsc` clean · **395 FE** (+1 `HomeView.test.tsx` CL-2 stats-grid regression test; 4 stale copy assertions updated in Journal/Library tests) · `cargo test` **152 green & unchanged** (regression-only) · CI `build-and-test` pass 3m30s. Verify: frozen `cargo tauri build --debug` → `home`(2)/`settings`(1)/`m12`(15)/`m21`(5) walkthroughs → Sonnet subagent text verdict **PASS** on CL-1/2/4/5/6/9 + no regression. **CL-3 NA-then-source-confirmed gotcha:** the `settings` walkthrough fixture has a library already configured, so NO screenshot reaches the `firstRun` Settings copy — the subagent correctly marked CL-3 **NA** (not FAIL); controller closed the gap by grepping the source (`firstRun &&` block present in `SettingsView.tsx` ~L226-238). **(Durable: first-run-only copy is unreachable by the standard walkthroughs whose fixture is pre-configured — verify such copy from source, or add an empty-fixture capture pass; don't read NA as a defect.)** **NEXT: plan M24 — The Listening Loop & Player** (the one v6 milestone likely needing additive schema on the M16 runner; playback speed, kill the chapter-end dead-end, owner decision pending on folding in parked per-second mid-chapter resume).
- **M22 (✅ shipped, PR #54) — Navigation & IA Coherence, the v6 foundation, all 6 impl tasks (6 commits) via Sonnet subagents:** invariants HELD — **FE-only** (`git diff --stat main` of `src-tauri`/`Cargo.*`/`package*.json` EMPTY), no schema/dep, read-only-on-disk, fixtures 43/44/47. Shipped: (1) sidebar grouped into **Browse** (Home/Library/Discover) + **My listening** (Journal/Insights/Collections) via `.sidebar__group`/`.sidebar__group-label`, 9→6 primary items; (2) **Ctrl+K** surfaced as a `.sidebar__search` button + `Ctrl K` `<kbd>` badge (new `onOpenPalette` prop; existing palette state/listener reused); (3) **Narrators merged into Discover** — deleted `NarratorsView`+`.test`, removed the `narrators` Route/state/handlers, Discovery facet props changed `*Options:string[]`→`*Terms:MetaTerm[]` to render per-facet labelled rows (`.facet-row`) with `· chapterCount`; (4) **Rename + Import-metadata demoted** to a **Settings → Library tools** Card (new `onOpenRename`/`onOpenMetadata` props; App passes `onBack={openSettings}` to both views, which had `onBack?` but rendered NO back button before — added via `PageHeader actions`); (5) **Settings sectioned** under Library/Display/Curation/Maintenance `.settings-group` dividers (headers inserted at Card boundaries, no Cards moved); (6) **scoped-search discoverable** — raw `tag:cozy …` code hint → plain-language + collapsible "Search tips" (`{!searching}`-gated), `window.prompt("Save search as:")` → inline form. Gates: `tsc` clean · **394 FE** (was 389; NarratorsView's 2 removed, Discovery/Settings/Library tests added) · `cargo test` green & unchanged · CI `build-and-test` 4m54s. **Durable notes/gotchas:** (1) **Git-Bash mangles `cmd /c`** — MSYS rewrites `/c` to `C:/`, launching an interactive shell (only the cmd banner prints, command ignored); use **`cmd //c`** in the Bash tool, or run via the PowerShell tool. `tools\dev-env.cmd` runs `%*` after `vcvars64.bat`, so `cmd //c "tools\dev-env.cmd cargo test --manifest-path src-tauri\Cargo.toml"`. (2) `verify.ps1` (no `-SkipBuild`) does its OWN frozen `cargo tauri build --debug --no-bundle` + gen-fixture; run `m12` first (builds), then `m21 -SkipBuild` (reuses the fresh exe — safe only if no `cargo test`/`tauri dev` runs between, which re-overwrites the exe in dev mode). (3) **Search-tips affordance is `{!searching}`-gated by design** (shows at the resting search box, hides during active results, matching the prior hint pattern + unit-test-proven) — the screenshot verifier flagged its absence in the *active-search* shot (`m12/06-search`) as a FAIL, but it IS present in `m12/05-library`; this is a designated-shot false-negative, not a defect (verify-don't-perform: did NOT expand scope to persist tips over results). (4) Removing AppShell nav props tripped `noUnusedLocals` on `openMetadata`/`openNarrators` mid-refactor — Task 1 kept them alive by wiring harness callbacks; Task 3/4 then consumed/removed them (sequence the demotions so handlers never dangle). (5) Pre-existing expanded-player avatar/title overlap confirmed again → owned by **M25 (Visual Polish)**, not M22. **NEXT: plan M23 — Clarity & Onboarding** (FE-only; first-run flow, empty states, plain-language copy sweep, dialog titles — some copy depends on M22's relocations, now settled).
- **v6 arc defined + M22 planned (2026-06-13 — `📝 Plan ready`, execute next):** the v5 backlog is **fully consumed** — an Explore digest confirmed all 37 five-lens ideas shipped across M16–M21, leaving only three parked items (listen-later, daily reminder, per-second resume+multi-root). **Owner redirected** the next few versions to **UX/UI coherence** ("make sense, easy to navigate, not confusing, something people want to use") and asked for a **multi-agent UI/UX review → actionable log**. Ran a **five-lens review** (IA/nav · visual/consistency · listening-loop/player · onboarding/clarity · discoverability/cognitive-load), each lens grounded in the **current** `.shots/m12` (15) + `.shots/m21` (5) PNGs (post-M21, 2026-06-13 14:50 — no re-capture needed) + FE source; ~55 findings deduped into the [v6 UX/UI backlog](docs/superpowers/specs/2026-06-13-audioshelf-v6-ux-ui-backlog.md). **Navigation/IA overload was flagged by ALL FIVE lenses** → owner picked **M22 = Navigation & IA Coherence** (AskUserQuestion). Proposed v6 arc: M22 nav/IA → M23 clarity/onboarding → M24 listening-loop/player (likely schema) → M25 visual polish → M26 optional. **Two re-audit corrections baked into the M22 plan:** (1) the "Import tags" nav item opened the **M16 embedded-metadata importer** (`MetadataView`), NOT the vocab manager (`MetadataManagerView`, already in Settings) — so it's an admin-tool *demotion*, not a mislabel fix; (2) the multi-select **Select** button **already exists** in scoped mode → dropped from scope. **M22 is FE-only** (no Rust/schema/dep; `git diff --stat` of `src-tauri`/`Cargo.*`/`package*.json` must be empty), 7 serial tasks (shared `AppShell.tsx`/`App.tsx`/`SettingsView.tsx`/CSS): sidebar grouping + admin-demote, Ctrl+K affordance, Narrators→Discover merge (delete `NarratorsView`+test, preserve counts, repoint m21 step keeping its NAME so `runner.test.ts` stays green), Settings→Library-tools, Settings sectioning, discoverable scoped-search + inline save. Verify via FE-only diff gate + `m12`/`m21` against a **frozen** `cargo tauri build --debug` + subagent text verdict. Plan: [`docs/superpowers/plans/2026-06-13-audioshelf-m22-navigation-ia.md`](docs/superpowers/plans/2026-06-13-audioshelf-m22-navigation-ia.md). **NEXT: execute M22.**
- **M21 (✅ shipped, PR #51) — Metadata & Discovery (narrator + per-audio), all 15 plan tasks in 16 commits (8 backend strictly serial → 6 FE → 1 walkthrough):** delivered both pillars broad and faithful to the plan. Schema **v8** additive on the M16 `run_step`/`user_version` runner (`metadata_terms(facet,value UNIQUE)` + `chapter_metadata`/`author_metadata`; `LATEST` 7→8; `SCHEMA_V1` untouched; bumping `LATEST` forced 4 `commands.rs` `user_version==7` test asserts → 8, a legit version-bump consequence). New `metadata.rs` (fixed FACETS + injection-safe `scope_table` mapping, never interpolated). Works aggregate = deduped union of their chapters' terms (built in `query_author_detail` after the per-chapter read). One DSL: `parse_query` gained `narrator:`/`language:`/`mood:` (single-token like `tag:`; empty value falls through to free text), `run_scoped_query` adds metadata EXISTS clauses, `discovery_for_metadata` powers "more by this narrator". FE adds a vocabulary manager (Settings), reusable `MetadataEditor` (the per-chapter editor lives inside the **Edit-tags Dialog**, opened via a new harness-only `openTagsForChapterId` prop), first-class Narrators browse, Discover facet picker, `chip--meta` chips. **Invariants verified:** no new dep (diff-stat empty), read-only-on-disk (every write SQLite — no export this milestone; M16 `lofty` ingestion untouched per owner "ignore tags on the files"), fixtures 43/44/47 (`fixture_scan.rs` untouched; all metadata seeded at runtime). 389 FE + 152 Rust green; CI `build-and-test` pass 3m2s. **Two durable gotchas (both verification-caught):** (1) **Tauri dev-mode exe = "localhost refused to connect."** A plain `cargo build`/`cargo test`/`tauri dev` compiles the app to load `tauri.conf.json build.devUrl` (`http://localhost:1420`); launched without Vite it shows the WebView2 "can't reach this page / localhost refused" error — even though it's a local app. Only `cargo tauri build --debug` embeds the frozen `dist`. The gate `cargo test -p audioshelf` **re-overwrites `target/debug/audioshelf.exe` in dev mode after** a good frozen build, so screenshots via `verify.ps1 -SkipBuild` can capture localhost-error (or stale) pages — always re-do the frozen build (or drop `-SkipBuild`) before capturing. (See memory `reference-tauri-devmode-localhost`.) (2) **`discovery_for_metadata` is unplayed-only by design** (mirrors `discovery_for_tags` — "what to listen to next"), so the Narrators browse seed must land the narrator on a work that still has an unplayed chapter; the original seed used `works[0]` (the standalone "Another Standalone Tale", which m12 marks played in the shared persisted DB) → empty page. Fixed by seeding onto a multi-chapter work's unplayed chapter (commit `4ba9bf6`). **NEXT: validate + plan M22 / next v5 milestone (M20–M21 were the last provisional v5 rows — re-scope the remaining backlog before planning).**
- **M21 plan + scope validation (2026-06-13 — `📝 Plan ready`, execute next):** ran the mandated audit-before-build first (two Explore subagents): confirmed **narrator exists nowhere today** (zero schema/UI/search) and that M16 embedded ingestion (`read_embedded_meta`→`lofty primary_tag`, `preview_metadata`/`apply_metadata`, `MetadataView.tsx`) + the M9/M16 tag system + the M19 `parse_query`/`scoped.rs run_scoped_query` DSL (unknown `key:` tokens already fall through) + the additive `run_step`/`user_version` runner (`LATEST=7`) are the reuse targets. **Four owner product forks (one batched AskUserQuestion) reshaped the milestone:** (1) **Scope = both pillars, broad.** (2) **Data source = manual-only, NO embedded ingestion** — *"ignore any tags on the actual files; only care about our tags… don't care about ingestion of these metadata at all other than duration and size of the file."* So M21 does NOT extend M16's preview→apply; M16 stays untouched. (3) **Field set = narrator + language + mood**, each a **user-defined value vocabulary** (not hardcoded enums): *"I should be able to create a new mood and then apply it to anything."* → a typed-vocabulary model, not fixed columns. (4) **Granularity = per-audio (chapter/file), works aggregate** — and per the data-source answer, also applied to **creators (authors)**. **Architecture decided:** additive **migration v8** (`metadata_terms(facet,value UNIQUE)` vocabulary + `chapter_metadata`/`author_metadata` attach tables; `LATEST` 7→8; first schema touch since M19's v7; no FK-off rebuild, `SCHEMA_V1` untouched). New `metadata.rs` (fixed `FACETS=[narrator,language,mood]` + injection-safe `scope_table` mapping + `facet_label`). `MetaTag`/`MetaTerm` models; `metadata: Vec<MetaTag>` added to `ChapterRow`/`WorkRow`/`AuthorDetail` (struct-literal fan-out: `query_author_detail` + `load_chapter_row` + TS test literals — works aggregate = union of their chapters' terms, deduped). Vocabulary CRUD (`create`/`list`/`rename`/`delete`/`merge` terms) + attach/detach (`add_metadata_value`/`remove_metadata_value` over a `scope` keyword). Discovery reuses the DSL: `parse_query` gains `narrator:`/`language:`/`mood:` (single-token values, like `tag:` — multi-word narrators handled by the browse/picker, not the DSL — documented limitation), `run_scoped_query` adds metadata EXISTS clauses, new `discovery_for_metadata` powers "more by this narrator". FE: vocabulary manager in Settings (mirrors `TagManagerView`), reusable `MetadataEditor` on Author Detail (author header + per-chapter Dialog), first-class **Narrators** browse route (new `voice` icon + nav), Discover facet picker, read-only metadata chips. **15 serial tasks** (8 backend strictly serial on shared `db.rs`/`model.rs`/`commands.rs`/`query.rs`/`scoped.rs`/`lib.rs`, then FE, then `m21` walkthrough). **Invariants (hard gate):** **no new crate/npm dep** (`git diff --stat` of `Cargo.*`/`package*.json` must be empty — pure SQLite + FE, `lofty` untouched), **read-only-on-disk** (every write is SQLite; M21 adds NO filesystem write — no export), **fixtures 43/44/47** (all metadata seeded at runtime in the new `m21` walkthrough; `fixture_scan.rs` untouched). **File-size capture explicitly DEFERRED/out-of-scope** (owner's "duration and size" was a contrast against ingestion, not a request; not a discovery facet; revisit later). Plan: [`docs/superpowers/plans/2026-06-13-audioshelf-m21-metadata-discovery.md`](docs/superpowers/plans/2026-06-13-audioshelf-m21-metadata-discovery.md). **NEXT: execute M21.**
- **M21 reshaped → "Metadata & Discovery (narrator + per-audio)" (2026-06-13, owner decisions, two rounds):** Round 1 cut M21 "Spoken-Audio Rituals" to **narrator-only** (the other 3 candidates — "What fits?" suggestions, wind-down/bedtime fade, "haven't returned in N days" nudges — are **SCRAPPED**). Round 2 (this entry) ADDED a second pillar: **per-audio metadata**. M21 is now TWO metadata systems that BOTH drive discovery across the app: **(1) narrator metadata + "more by this narrator"** (narrator as a first-class browse/discovery dimension, today everything is author-centric); **(2) per-audio metadata** — each audio (per-chapter/per-file; works aggregate) carries rich, queryable metadata — **"if it doesn't already exist."** **CRITICAL PLANNING STEP — audit-before-build:** much of system (2)'s plumbing already exists — **M16 embedded-metadata ingestion** (`lofty primary_tag()` read at scan + `preview_metadata`/`apply_metadata` diff-preview→apply mirroring Rename + "Import metadata" view) and the **M9/M16 tag systems** (author/work/chapter tag tables + `get_all_tags` + tag taxonomy). The plan MUST inventory what M16/M9 already provide and REUSE them; only add the genuinely-missing fields/flows (avoid rebuilding M16). Discovery for BOTH pillars reuses existing plumbing — `discovery_for_tags` and the **M19 `parse_query`/`scoped.rs run_scoped_query` DSL** (add `narrator:`/metadata filters; surface in browse + search + Discover). **Open questions for the planning grill:** (a) DATA SOURCE — files may carry no embedded tags, so narrator + per-audio fields need embedded-tag ingestion (preview→apply à la M16, never auto-at-scan) AND/OR manual editors; (b) the exact metadata FIELD SET (narrator, genre/mood, year, description, …) kept queryable for discovery. Likely the **first schema migration since M19** (additive `narrator`/metadata columns or tag tables on the M16 `run_step`/`user_version` runner). Read-only-on-disk + no-new-dep invariants still hold (`lofty` already a dep). **NEXT: plan M21 (Metadata & Discovery — narrator + per-audio) — first audit existing M16/M9 metadata+tag coverage, validate the data-source + field-set approach, then write the self-contained plan.**
- **M20 (✅ shipped, PR #46) — Accessibility & Platform Integration, all 11 sub-features in one broad milestone (16 commits, 12 serial tasks: 11 FE + 1 Rust window command):** invariants HELD — **no schema migration** (`LATEST` stays 7), **no new dep** (`Cargo.*`/`package*.json` `git diff --stat main` empty; only `src-tauri/gen/schemas/capabilities.json` regenerated for the new `["main","miniplayer"]` window capability), **read-only-on-disk** (only new write = the `a11y_prefs` setting), **fixtures 43/44/47** (all M20 state toggled at runtime in the new `m20` walkthrough). Architecture as planned: `src/lib/a11y.ts` `parseA11yPrefs`/`a11yDataAttrs` → one `a11y_prefs` JSON setting on `.app-shell` data-attrs; `main.tsx` branches on `?miniplayer=1` to mount `MiniPlayerRemote` (NOT an App early-return — hooks rule); Rust `open/close_mini_player`; Media Session pure-TS; prev/next-chapter logic extracted into shared `playPrevChapterRef`/`playNextChapterRef` so the Media-Session handlers AND the mini-player listener call ONE implementation (DRY). **Durable gotchas this run (mostly verification-caught):** (1) **React does NOT remove a stale `data-*` attribute when a spread omits the key** — `a11yDataAttrs` returning `{}` for defaults left a stale `data-theme` when toggling light→dark (a REAL user-facing bug, not just a harness artifact: theme would stick until reload). Fix: **always emit all keys, `undefined` for defaults** (React only removes an attr on an explicit `undefined` value). (2) **A `[data-theme]` override on `.app-shell` does NOT flip the page backdrop if `body { background: var(--color-bg) }`** — `body` reads `:root` (an ancestor), so the override never reaches it. Fix: `.app-shell` must paint its OWN `background: var(--color-bg); color: var(--color-text)` so the theme flips edge-to-edge. (3) **`getComputedStyle().fontFamily` returns the SPECIFIED stack, not the rendered face** — it canNOT prove a system font (Verdana) is installed/rendering; only `letter-spacing`/`line-height` computed values are real used-values. The dyslexia font is a deliberately **asset-free system-Verdana stack** + looser spacing; on a prose-sparse media app a system-font swap is too subtle to dramatize in a downscaled PNG → **verified by instrumentation + unit tests, not pixel diff** (do NOT block PASS on it; same class as the OS-surface SMTC card/2nd-window that the main-HWND `captureWindow` can't capture). (4) **Walkthrough step sequencing** — a step that opens a modal/panel (the colorblind-status shot opened the expanded Now Playing) must be CLEANED UP by following steps (`setPlayerExpanded(false)`), else the overlay BLEEDS into the next shots (broke skip-link + sr-tree captures). Each `m20` step force-resets transient UI + a11y state (M19 persisted-setting-leak lesson) before its capture. (5) **Color-blind status must let the ICON be the indicator** — wrapping a small check/ring icon inside a solid colored dot makes played/unplayed differ only by dot color (defeats the purpose); dropped the disc so the shape (✓ vs ○) IS the signal. (6) **Space must not be hijacked from focused buttons/links** — the global play/pause Space shortcut excludes `BUTTON`/`A`/interactive-role targets so native activation still works (a11y keyboard correctness). (7) **`noUnusedLocals` forces deferral** — `updateA11y` (Task 1) couldn't be defined until its consumer (Settings, Task 5) existed, or tsc errors TS6133. Gates: tsc clean · 383 FE · 139 Rust unit+integration · `cargo tauri build --debug` full · `m20` 11-shot + `m12` 15-shot subagent text-verdict PASS (sole intended cross-screen changes: new "Pop out player" button + icon-augmented status shapes; expected default-font metric shift Inter→Segoe-UI stack since Inter was never bundled). **NEXT: validate M21 (Spoken-Audio Rituals) vs the v5 backlog before planning.**
- **M20 plan + scope validation (2026-06-13 — `📝 Plan ready`, execute next):** validated the provisional "Accessibility & Platform Integration" against the [v5 backlog](docs/superpowers/specs/2026-06-12-audioshelf-v5-backlog.md) before planning. Backlog frames the cluster as **curb-cuts that help everyone**, reframes the deliberately-dropped light/dark toggle **as accessibility** ("forced-colors first… full light theme later"), flags **media-key/SMTC as the #1 Windows-audio expectation** (converged across all five lenses), and scopes i18n as **"readiness"** (logical properties + `dir="auto"`), NOT a translation framework. Non-goals still hold (no queue/autoplay/speed/social/multi-root/per-second-resume). **Two key technical findings reshaped the plan:** (1) **media keys + the OS now-playing/SMTC card need NO new dep** — WebView2 (Chromium) supports the **Media Session API** (`navigator.mediaSession.metadata` + `setActionHandler` + `setPositionState`) and **auto-bridges it to the Windows SMTC card and hardware media keys**; pure TypeScript. (2) the **only** item breaking the FE-only/no-dep/no-schema profile is the **focus mini-player** (a second always-on-top Tauri window) — the user chose to include it anyway (broad scope). **Three product forks decided (AskUserQuestion + one clarifying round on "what is the SMTC card"):** (1) **Scope = ALL 11 sub-features** incl. the mini-player window (mirrors M16–M19 broad pattern). (2) **Media keys = Media Session API** (no new dep), NOT native WinRT SMTC via the `windows` crate. (3) **i18n = readiness only** (logical props + `dir=auto`, strings stay English). **Plan-level decisions baked in to keep invariants:** reduced-motion is **CSS-only** (a subagent digest wrongly suggested adding Motion One — explicitly forbidden; the app has no animation dep); dyslexia font uses a **system stack** (`Verdana, Tahoma, "Segoe UI"` + spacing), **not** a bundled OpenDyslexic file, so the build stays offline-safe + asset-free; all a11y prefs persist as **one `a11y_prefs` JSON `settings` row** applied as `data-theme`/`data-text-size`/`data-font`/`data-reduced-motion` on `.app-shell` (mirrors M19 `data-density` via a new `src/lib/a11y.ts` with `parseA11yPrefs`/`a11yDataAttrs` failsafe). **Mini-player architecture:** Rust `open_mini_player`/`close_mini_player` commands (`WebviewWindowBuilder` "miniplayer" label, `always_on_top`/`decorations(false)`/`skip_taskbar`, loads `index.html?miniplayer=1`); **`main.tsx` branches at root render** on the `?miniplayer=1` query → mounts a separate `MiniPlayerRemote` (NOT an early-return inside `App` — hooks rule); state-sync via Tauri events (`emit("playback:state")` from main, throttled ~1/sec; `MiniPlayerRemote` `listen`s it and `emit("miniplayer:command", {action})` back; main `listen`s and applies to `audioRef`); capability = add `"miniplayer"` to `default.json` `windows` (events are in `core:default`, window created from Rust → **no new permission/dep**). **Verification strategy for OS surfaces:** the SMTC card + the real second window can't be captured by the existing main-HWND `captureWindow` → verified by unit tests (`mediaSession.test.ts` with a mock `ms`; `MiniPlayer` render + emit-on-click) + an **inline `<MiniPlayer/>` harness render** for the `mini-player` screenshot (like M18's inline recap) + optional controller manual/computer-use check; do NOT block PASS on an app-harness shot of the OS card/window. **Invariants (the hard "done" gate):** **NO schema migration (`LATEST` stays 7, `db.rs migrate()` untouched — returns to the M18 no-migration approach), NO new crate/npm dep (`Cargo.*`/`package*.json` byte-clean — `git diff --stat` must be empty), read-only-on-disk (only new write = the `a11y_prefs` setting; mini-player creates no files; Rename stays sole audio mutator), fixtures 43/44/47** (all M20 state toggled at runtime in the new `m20` walkthrough). **12 serial tasks** (mostly FE; only Rust is the window command) — shared files (`tokens.css`/`base.css`/`components.css`/`App.tsx`/`AppShell.tsx`/`SettingsView.tsx`/`ui.tsx`) force serialization. **Fixture artifact to expect (not a defect):** ~5s synthetic clips → duration labels read small; themes/status/structure render correctly. **Known plan caveats flagged for the executor:** text-size won't scale components using literal `px` font-size (accept partial coverage, no sweeping rem refactor); browse `role=tree` falls back to nested-list+headings if the markup resists clean tree semantics (report, don't force a risky refactor); Task 2 includes a hardcoded-hex→token sweep so colors flip under `[data-theme]`. Plan: [`docs/superpowers/plans/2026-06-13-audioshelf-m20-accessibility-platform.md`](docs/superpowers/plans/2026-06-13-audioshelf-m20-accessibility-platform.md). **M21 (Spoken-Audio Rituals) remains provisional** — re-validate at its turn. **NEXT: execute M20.**
- **M19 (✅ shipped, PR #43) — Power & Scale, all 8 sub-features in one broad milestone (17 serialized phases: 9 backend → 7 FE → 1 harness/gates):** schema **v7** additive on the M16 `run_step`/`user_version` runner (`saved_searches`/`smart_collections` tables + `works.chapter_sort` col; `LATEST` 6→7 and **moved to a module-scope `pub(crate) const`** so `migrate()` + the health scan share one source of truth; `open_at_version` extended; no FK-off rebuild). **One DSL, one resolver (DRY):** pure `query.rs::parse_query` (`tag:` repeatable AND, `duration:<op><n>{s,m,h}` default op `≤`, `status:{unstarted,inprogress,done}` + aliases, free-text remainder, unknown `key:` falls through) → `scoped.rs::run_scoped_query` (candidate works via LIKE + EXISTS-per-tag over `work_tags ∪ author_tags`, then per-work duration/status filter in Rust, capped at `SEARCH_CAP=50`) — consumed by advanced search, saved searches, AND smart collections. `WorkRow` gained `chapter_sort` — **the feared struct-literal fan-out was a single site** (`query_author_detail`; home/discovery use `DiscoveryWork`/`RecommendationWork`, not `WorkRow`). **Export/import BOTH modes:** `build_curation_export` → identity-keyed JSON (keyed by `folder_name`/`base_title`/`raw_filename`, NOT DB ids) and `export_db_snapshot` via **`VACUUM INTO ?1`** (bound-param form works on the bundled SQLite — no string-interp fallback needed); import = **strictly additive merge** (tags union via INSERT-OR-IGNORE, played/favorite OR-only, journal scalars **fill-if-empty** = keep-existing-wins, notes/bookmarks add-missing deduped, collections/searches/aliases add-if-name-absent, unmatched skipped+counted — **never deletes**, proven by `import_merges_additively_without_deleting`). **Crash-safe DB restore (destructive-op discipline, controller-verified):** `stage_db_restore` opens the source READ-ONLY, requires `PRAGMA integrity_check == ok` and `user_version ≤ 7` BEFORE touching anything, then copies it to `restore_pending.db` beside the live DB — **never touches the live DB**; `apply_pending_restore` (prepended as ONE line at the top of `db::open()`) backs the live DB up to `audioshelf.db.bak-<ts>` via rename FIRST, and **only on backup success** renames pending→live (a failed backup ABORTS — original never destroyed), recoverable at every crash point (proven by `pending_restore_backs_up_then_swaps`). Live DB path surfaced by extracting `resolve_db_path(app)` from `init_db` and `.manage(DbPathState(path))`. **`build.rs` got `cargo:rerun-if-changed=../dist`** so the embedded frontend (Rust `generate_context!`) isn't stale after a `npm run build` — important because **`tools\verify.ps1` runs the DEBUG-BUILT exe**, so any FE change needs `npm run build` + `cargo tauri build --debug` before re-capturing. **FE:** global Ctrl+K `CommandPalette` (pure, debounced `search_library` reuse), scoped-search mode in Library (`hasScopedTokens` switch → `advanced_search`; save-name via `window.prompt` matching the existing note/bookmark pattern), new **Collections** route+nav (the sole new sidebar item → only `m12` cross-screen change), bulk-select + `BulkTagDialog` (reuses the M13 `Dialog` primitive), `density.ts` failsafe + `data-density` on `.app-shell`, per-work chapter-sort `<select>`, Settings "Backup & maintenance" section. **Invariants verified:** no new crate/npm dep (`Cargo.*`/`package*.json` byte-clean; only `dialog:allow-open` capability + regenerated `gen/schemas`); **read-only-on-disk** (diff-audited: every fs write targets SQLite / staged-restore+backup DB / a user-chosen JSON-or-DB path / test temp dirs — never audio); **fixtures 43/44/47** (all M19 data seeded at runtime in the new 9-step `m19` walkthrough). **Verification gotchas (the long tail this run):** the `m19` screenshot walkthrough initially FAILED 6/9 shots — **5 were capture bugs, not feature bugs** (stale debug exe; wrong view captured; scroll-targets needed real classNames `.backup-maintenance`/`.health-report`; overlay/state not opened) + **1 real CSS gap** (Collections rows had no styling — Task 13 omitted `.collection-row*`). **Density-spacious took 3 attempts and an empirical badge** (rendered `density` state + `getComputedStyle('.card-grid').gap` + `data-density` attr into the shot): all three links were intact (spacious/spacious/32px) — the real causes were (a) 32px vs 16px gap too subtle to read in a frame, and (b) **the persisted `library_density` setting leaked across harness runs**, so the "comfortable" baseline shot was ALSO spacious. Fix: spacious now drops to **3 wide columns** (40px gap / 28px padding via `grid-template-columns: repeat(auto-fill, minmax(220px,1fr))`) vs comfortable's 5 tight columns, and step 2 of the walkthrough force-resets density to comfortable. **(Durable lesson: when a screenshot "looks unchanged," instrument the live value into the frame before theorizing; and beware persisted settings leaking between harness runs.)** Independent subagent re-verified 9/9 `m19` + 15/15 `m12` PASS. Gates: tsc clean · 310 FE · 138 Rust · `cargo tauri build --debug` full (exe + bundles). **NEXT: validate M20 (Accessibility & Platform Integration) vs the v5 backlog before planning.**
- **M19 plan + scope validation (2026-06-13 — `📝 Plan ready`, execute next):** validated the provisional "Power & Scale" against the [v5 backlog](docs/superpowers/specs/2026-06-12-audioshelf-v5-backlog.md) before planning. Backlog confirms all 8 items are the **power-user / heavy-library-curation lens**; explicit guardrails carried into the plan: read-only-on-disk (all writes → SQLite or a user-chosen non-audio path; Rename stays the sole audio mutator), **chapter-sort override must be non-destructive** ("Rename stays the disk path"), backlog literally flags `smart_collections` as needing a table + chapter-sort as "small schema", and the standing non-goals hold (no queue/autoplay/speed/social/multi-root/per-second-resume). **Three product forks decided by the user (AskUserQuestion):** (1) **Scope = ALL 8 sub-features** in one broad milestone (mirrors M16/M17/M18), not a focused-core split — command palette (Ctrl+K), advanced/scoped search (`tag:`/`duration:`/`status:` + saved searches), multi-select + bulk tagging, saved smart collections, density toggle, per-work chapter-sort, SQLite export/import, library-health scan. (2) **Persistence = dedicated schema tables** (v7 migration), not JSON settings — `saved_searches` + `smart_collections` tables + `works.chapter_sort` column; **v7 is fully ADDITIVE** on the M16 `run_step`/`user_version` runner (bump `LATEST` 5→… actually 6→7, extend `open_at_version`, no FK-off rebuild). (3) **Export/import = BOTH modes** — a portable identity-keyed JSON dump (`build_curation_export`, keyed by `folder_name`/`base_title`/`raw_filename`, NOT DB ids) with a **strictly additive non-destructive merge** import (tags union, played/favorite OR, journal scalars **fill-if-empty** = keep-existing-wins since we lack per-field timestamps, notes/bookmarks add-missing, collections/searches add-if-name-absent, unmatched skipped+counted) **AND** a full `VACUUM INTO` DB snapshot with a **crash-safe staged restore** applied at bootstrap (`apply_pending_restore` at top of `db::open`: validate source via read-only `PRAGMA integrity_check`+`user_version≤7` BEFORE touching the live DB, back up current DB to `audioshelf.db.bak-<ts>` first, then place-then-rename `restore_pending.db` → live — never destroys the original; honors the user's destructive-op discipline). **Architecture:** one DSL powers advanced search + saved searches + collections — pure `query.rs::parse_query` (`tag:`/`duration:<op><n>{s,m,h}` default op `≤`/`status:{unstarted,inprogress,done}` + free text) → `scoped.rs::run_scoped_query` (candidate works via LIKE+EXISTS-per-tag, then per-work duration/status filter in Rust). New modules `query.rs`/`scoped.rs`/`backup.rs`; `WorkRow` gains `chapter_sort` (struct-literal fan-out to fix — watch `query_author_detail` + any home/discovery `WorkRow` builder, mirrors the M17 fan-out lesson). FE: global Ctrl+K overlay (`CommandPalette`, debounced `search_library` reuse — no existing keydown handler to collide with), scoped-search mode in Library (`hasScopedTokens` switch → `advanced_search`), new **Collections** route+nav (the sole new sidebar item → only cross-screen change in the `m12` regression), bulk-select mode + `BulkTagDialog`, `density.ts` failsafe setting + `data-density` CSS, per-work chapter-sort `<select>`, Settings "Backup & maintenance" section. **Capability:** add `dialog:allow-open` (import/restore need an open-file dialog; `dialog:allow-save` already added in M17). **Invariants:** **no new crate/npm dep** (`serde_json`/`@tauri-apps/plugin-dialog` already present); **fixtures 43/44/47** (all M19 data seeded at runtime in a new 9-step `m19` walkthrough); read-only-on-disk. **17 phases** (9 backend → 7 FE → 1 harness/gates/regression-verify), serialized (every backend phase edits shared `db.rs`/`commands.rs`/`lib.rs`/`model.rs`). **Fixture artifact to expect (not a defect):** ~5s synthetic clips → duration-scoped filters & total-time labels read small; counts/health/structure populate correctly (same class as M11/M14/M15/M18). Plan: [`docs/superpowers/plans/2026-06-13-audioshelf-m19-power-scale.md`](docs/superpowers/plans/2026-06-13-audioshelf-m19-power-scale.md). **M20–M21 remain provisional** — re-validate each at its turn. **NEXT: execute M19.**
- **M18 (✅ shipped, PR #40) — Insight & "Your Year in Listening", a return to the pre-M16 no-migration approach:** all 5 sub-features delivered in one broad milestone across 8 serialized phases (3 backend → 4 frontend → 1 harness/gates). **NO schema migration** (`LATEST` stays 6; `db.rs`/`SCHEMA_V1` untouched), **no new crate/npm dep** (`Cargo.lock`/`Cargo.toml`/`package-lock.json` byte-clean, verified `git diff --stat`), **read-only-on-disk** (only new write = the user-chosen recap PNG via the existing `dialog:allow-save`, no new capability; Rename stays the sole audio mutator), **fixtures 43/44/47** (all insight data seeded at runtime via the harness-only, not-UI-wired `seed_play_events`). **Architecture:** new `src-tauri/src/insights.rs` splits **pure `build_insights(events, author_names, works, now, tz)` (unit-tested without a DB) from thin `compute_insights(conn,…)`** (one events join + author names + per-work tag∪/completion, then delegates). Std-only date math reused/extended from `home_stats`: tz-offset local-day bucket `(ms - tz*60000).div_euclid(86_400_000)`, `civil_from_days` (Howard Hinnant), `weekday_of` (Sun=0 anchored at day 3), `longest_run`, `local_hour`, and the existing `pub(crate) streak_len` (NOT duplicated). Per-tag breakdown is **work-level** (`work_tags ∪ author_tags`, chapter tags excluded mirroring M9/M10): owned = works carrying the tag, finished = those fully played. Insights "Time listened" counts replays (activity measure, distinct from Home's played-flag total). Read-only command `query_insights(now_ms, tz_offset_minutes)`; `export_recap_png(path, Vec<u8>)`. **Recap PNG = no-dep client raster:** `buildRecapSvg` builds a **self-contained** dark SVG (literal hex, NO external `<image>` → no canvas taint) → `Image` → `<canvas>` → `toBlob` → `Uint8Array` → Rust `std::fs::write`. **FE:** pure prop-driven `InsightsView` with hand-rolled SVG/CSS viz (no charting lib); `src/lib/insights.ts` (`heatLevel`/`heatColumns`/`weekdayOfDay`/`maxCount`) + `src/lib/recap.ts` unit-tested; canvas rasterizer lives in `App.tsx` (jsdom can't test it → screenshot-verified). New bar-chart `Icon` + `insights` sidebar route. **Three deviations from the verbatim plan (all forced by reality, reported & fixed):** (1) **TS name collision** — `TagStat` already exists in `api.ts` from M16 (tag-management `{tag, workCount, chapterCount, authorCount}`); the new insights interface was renamed **`InsightTagStat`** (`{tag, owned, finished}`), confined entirely to `api.ts` — the Rust struct stays `TagStat` and the serde camelCase wire shape is unchanged. (2) **`Icon.tsx`** uses a single `<path d=…>` per glyph (`stroke="currentColor" fill="none"`), not inline `<rect>` JSX → added the bar-chart glyph as a `<path>` per the plan's own fallback instruction. (3) **Two test fixes**: `AppShell.test.tsx` needed the new mandatory `onInsights` prop; `InsightsView.test.tsx` switched 3 `getByText`→`getAllByText` because the headline values (`2h 5m`, `Jane Doe`, `mystery`) legitimately render in BOTH a breakdown/stat card AND the recap SVG. **Two verification-caught bugs:** (a) **screenshot-capture gap (not a feature bug)** — the Insights page is a tall 9-section stack but the window is fixed 1280×860 and `captureWindow` grabs only the viewport, so the populated/recap shots cut off at "When you listen"; fixed by **splitting the `insights` walkthrough into 4 scrolled bands** (empty / overview / trends / recap) that scroll `.app-main` (`scrollTo`/`scrollIntoView`) to the target section before each capture (`runner.test.ts` updated to 4 step names). (b) **REAL CSS bug** — the three trend bar charts rendered BLANK (axis labels but zero bars): `.bar-chart` used `align-items: flex-end`, so `.bar-chart__bar` was content-sized with no definite height, so the inline `.bar-chart__fill { height: X% }` resolved its percentage against an auto height → collapsed to 0 (the breakdown bars worked because they have a definite `height: 8px`). Fixed by `align-items: stretch` so bars fill the 140px row. **(Durable gotcha: a percentage-height child needs a definite-height parent — `align-items:flex-end` does NOT give flex children a definite cross-axis height.)** Independent screenshot subagent re-verified all 4 insights shots PASS + the full `m12` 15-shot regression matrix PASS (sole cross-screen change = the new Insights nav item). Gates: tsc clean · 303 FE (prior 292→303) · 114 Rust unit + 5 integration green · `cargo tauri build --debug` OK. **NEXT: validate M19 (Power & Scale) before planning — re-confirm scope vs the v5 backlog (command palette / advanced search / bulk-tag / smart collections / export-import / health scan).**
- **M18 plan + scope validation (2026-06-12 — `📝 Plan ready`, execute next):** validated the provisional "Insight & Year in Listening" against the [v5 backlog](docs/superpowers/specs/2026-06-12-audioshelf-v5-backlog.md) before planning. Backlog confirms M18 = **pure visualization over `play_events` + the M17 Journal, minimal/no new schema** (3-lens convergence: power-user + engagement/insight + domain) and the guardrail **"self-knowledge, not gamification; streak framed as 'days in a row' or dropped."** **Two product forks decided by the user:** (1) **Scope = ALL 5 sub-features** (broad, mirrors M16/M17) — 52-week heatmap, trends (month-vs-month + time-of-day + day-of-week), per-creator/per-tag breakdowns, weekly rhythm, annual recap. (2) **Recap export = PNG snapshot** (the backlog's literal "exportable PNG snapshot" — the recap's value is a shareable image), done with **NO new dependency**: the recap is a **self-contained SVG** (literal hex, no external `<image>` → no canvas taint) rasterized client-side in WebView2 (`buildRecapSvg`→`Image`→`<canvas>`→`toBlob`→`Uint8Array`) and written by a new Rust `export_recap_png(path, Vec<u8>)` via `std::fs::write` to a user-chosen path (reuses the M17 `save()` dialog + existing `dialog:allow-save` — **no new capability**). **Schema = NONE** (returns to the pre-M16 read-only/no-migration approach; `LATEST` stays 6, `db.rs migrate()` untouched). **Backend digest confirmed the precedent to reuse:** `home_stats`' tz-offset local-day-bucket math (`(ms - tz*60000).div_euclid(86_400_000)` + `BTreeSet` + the existing `pub(crate) streak_len`) — extended in a **new `src-tauri/src/insights.rs`** module: pure `build_insights(events, author_names, works, now, tz)` + std-only helpers (`civil_from_days` = Howard Hinnant, `weekday_of` Sun=0 anchored at day 3, `longest_run`, `local_hour`) all unit-tested without a DB; thin `compute_insights(conn,…)` does the SQL (one events join + author names + per-work tag∪/completion) then delegates. One read-only command `query_insights(now_ms, tz_offset_minutes) -> InsightsData`. **Per-tag breakdown = work-level** (work_tags ∪ author_tags, chapter tags excluded mirroring M9/M10): owned = works carrying the tag, finished = those fully played. **Insights "Time listened" total counts replays** (activity measure — distinct from Home's played-flag total; documented). **Invariants:** fixtures **43/44/47** (all data seeded at runtime via a **harness-only, not-UI-wired `seed_play_events(Vec<SeedPlayEvent>)`** — mirrors `reset_play_history`; the `insights` walkthrough seeds ~120 deterministic events off a **fixed UTC anchor** `Date.UTC(2026,5,12,18,0,0)` so the heatmap is identical every run); **no new crate/npm dep → `Cargo.lock`/`Cargo.toml`/`package-lock.json` byte-clean**; read-only-on-disk (only new write = user-chosen recap PNG). **FE:** new `insights` route + sidebar nav (`Insights`, new bar-chart Icon), pure `InsightsView` (prop-driven, no `invoke`) with hand-rolled **SVG/CSS viz, no charting lib** (heatmap grid, weekday/hour/rhythm bars, breakdown bars, recap card via `dangerouslySetInnerHTML` of our own escaped SVG); pure helpers `src/lib/insights.ts` (`heatLevel`/`heatColumns`/`weekdayOfDay`) + `src/lib/recap.ts` (`buildRecapSvg`) unit-tested; canvas rasterization lives in `App.tsx` (jsdom can't test it → screenshot-verified). **8 serialized phases** (backend module+command → recap PNG command → harness seeder → FE api → recap/insights helpers → icon+route+shell wiring → InsightsView+CSS → harness `insights` walkthrough+gates+`m12` regression subagent verify). **Fixture artifact to expect (not a defect):** ~5s synthetic clips → "Time listened" reads small `Xm`/`0m`; counts/heatmap are event-driven so they populate correctly (same artifact class as M11/M14/M15). Plan: [`docs/superpowers/plans/2026-06-12-audioshelf-m18-insights.md`](docs/superpowers/plans/2026-06-12-audioshelf-m18-insights.md). **M19–M21 remain provisional** — re-validate each at its turn. **NEXT: execute M18.**
- **M17 (✅ shipped, PR #37) — The Listening Journal, v5's second migration (schema v6):** all 7 sub-features delivered in one broad milestone across 7 serialized Sonnet-subagent phases (every backend phase edits the shared `db.rs`/`model.rs`/`commands.rs`/`lib.rs`, so no parallelism — same as M16). **Migration v6 is fully ADDITIVE on the M16 runner** — `migration_v6_journal` adds 2 tables (`chapter_notes`/`chapter_bookmarks`) + 5 `ALTER TABLE ADD COLUMN` in one `execute_batch`; `LATEST` 5→6; `open_at_version` gets an `if version >= 6 { run_step(&conn, 6, …) }` guard (the existing fn already uses `run_step` for *every* version — the plan's bare-call text was imprecise; `run_step` is correct so `PRAGMA user_version` reaches 6). `SCHEMA_V1` untouched; legacy v1/`user_version=0` DBs upgrade clean (tested). **Struct-literal fan-out handled:** adding scalar fields to `ChapterRow` (`user_summary`/`takeaway`/`is_favorite`) + `WorkRow` (`re_entry_note`/`completion_rating`) broke 3 Rust constructors — `query_author_detail`'s work + chapter SELECTs **and `load_chapter_row`** (the M11 home helper the plan didn't enumerate — caught by grep) — plus 22 TS test literals across 7 files. **`collect_journal` reused the EXISTING conventions, not new ones** (the plan's two STOP-if-unclear spots): author name = `COALESCE(a.display_name, a.folder_name)` (as `query_authors`), chapter title = `Path::file_stem` of `raw_filename` (factored into a `strip_ext` helper matching `query_author_detail`/`load_chapter_row`/`home_stats`); work title col = `base_title`. `query_journal` filters the collected vec Rust-side (case-insensitive `contains`) — no 7 parameterized LIKE queries. `build_journal_markdown`/`build_journal_json` (`serde_json::to_string_pretty`) are pure+unit-tested; `export_journal` is the **only new on-disk write** → user-chosen non-audio path via the save dialog (`std::fs::write`); **Rename stays the sole audio mutator, read-only-on-disk preserved.** Added `dialog:allow-save` to `capabilities/default.json` (auto-regenerates `gen/schemas/capabilities.json`). **Jump-to-bookmark = the app's first sub-chapter seek** — `pendingSeekRef` applied in the audio element's `onLoadedMetadata`; same-chapter jumps seek directly, cross-chapter jumps load-then-seek via the M14 `getAuthorDetail`→`playChapter` pattern (`playChapterById` resolves the author by scanning `getAuthors()`+`getAuthorDetail` — acceptable for an explicit-action lookup). **NOT auto-resume → the "no per-second mid-chapter resume" non-goal stays intact.** **Invariants held:** fixtures **43/44/47** (all journal data seeded at runtime in the new `journal` walkthrough; `fixture_scan.rs`/on-disk fixtures untouched); **`serde_json` was already a direct dep → `git diff --stat Cargo.lock`/`Cargo.toml` byte-clean** (no new subtree). **Screenshot-verify caught a harness-capture bug (NOT a feature bug — the feature works, proven by the browse shot showing every artifact):** (1) `journal-chapter-edit` captured the plain author page because `ChapterJournalDialog` needs BOTH App's `openJournal` AND `AuthorDetailView`'s component-local `editState.mode==="journal"` (normally set by the chapter overflow-menu click) — fixed by adding an `openJournalForChapterId` prop + a `useEffect` that sets `editState`; (2) `journal-work-meta` showed empty fields because `WorkReEntryField`/`WorkRatingField` are controlled components that seed local `useState` from props **only on mount** — re-fetching `detail` re-rendered but didn't remount them; fixed by navigating to library then back to author (forces remount with the saved values). **Re-verified PASS** by a fresh subagent. **Known test-env artifact (accepted, NOT a defect):** the harness shares `com.audioshelf.app/audioshelf.db` and journal tables aren't reset between runs (only `reset_play_history` clears play-events), so repeated `verify.ps1 -Walkthrough journal` runs accumulate duplicate seeded notes/bookmarks in the shots — cosmetic; a future `reset_journal` harness command would make it deterministic. **One stray file** (`C:Tempbuild_out.txt`, a botched-redirect terminal-banner capture) was swept into a subagent's `git add -A` and removed in a cleanup commit before merge — watch subagent `git add -A` for stray cwd artifacts. **Gates:** 108 Rust (prior 95→108) + 292 FE (prior 248→292) green; tsc clean; `cargo tauri build --debug` OK; `journal` 6-shot + `m16`/`m12` regression subagent-verified PASS (sole cross-screen change = new Journal sidebar nav item). **NEXT: validate M18 (Insight & "Your Year in Listening") before planning — re-confirm scope vs the v5 backlog; it's mostly visualization over `play_events` + the new Journal data.**
- **M17 plan + scope validation (2026-06-12 — `📝 Plan ready`, execute next):** validated the provisional "Listening Journal" against the [v5 backlog](docs/superpowers/specs/2026-06-12-audioshelf-v5-backlog.md) before planning. **Three product forks decided by the user:** (1) **Scope = ALL 7 sub-features** in one broad milestone (mirrors M16's broad choice), not a trimmed core. (2) **Bookmarks are POSITIONAL** — store integer seconds captured live from the `<audio>` element's `currentTime`; user **manually jumps** (seek applied only after `loadedmetadata`, only on explicit action) → introduces the app's first sub-chapter *seek* but **NOT** auto-resume, so the standing **"no per-second mid-chapter resume" non-goal stays intact**. (3) **Export = Markdown + JSON** (MD to read/share, JSON to back up) — net-new (no export/save-file code existed); save path via `@tauri-apps/plugin-dialog` `save()` (already a dep; may need `dialog:allow-save` capability), file written in **Rust** (`std::fs::write`) — the only new on-disk write, to a **user-chosen non-audio path** (Rename stays the sole audio mutator). **Schema = migration v6, fully ADDITIVE** on the M16 runner (`run_step` + atomic `PRAGMA user_version`, bump `LATEST` 5→6, extend `open_at_version`): **2 new tables** `chapter_notes`/`chapter_bookmarks` (each `chapter_id, position_secs, …, created_at`) **+ 5 `ADD COLUMN`** (`chapters.user_summary`/`takeaway`/`is_favorite`, `works.re_entry_note`/`completion_rating`) — **no FK-off table-rebuild, don't touch `SCHEMA_V1`**. Backend digest confirmed: ChapterRow/WorkRow gain scalar fields (populated in existing `query_author_detail` SELECTs); notes/bookmarks fetched on demand via `get_chapter_journal`; unified `query_journal` collects all 7 kinds into a flat `JournalEntry` vec + Rust-side `LIKE`-style filter; `collect_journal`/`build_journal_markdown`/`build_journal_json` are pure+unit-testable. **Invariants held:** fixtures stay 43/44/47 (all journal data seeded at runtime in a new `journal` walkthrough, like M9/M11 — `fixture_scan.rs` untouched); `serde_json` reuses the tauri-locked version (no new subtree). **7 phases:** migration+upgrade-tests → scalar fields+setters → notes/bookmarks CRUD → unified query+MD/JSON export → FE api+chapter/work editing UI → Journal route+player capture+jump-to → harness `journal` walkthrough+gates+subagent screenshot verify (journal shots + `m16`/`m12` regression). Plan: [`docs/superpowers/plans/2026-06-12-audioshelf-m17-listening-journal.md`](docs/superpowers/plans/2026-06-12-audioshelf-m17-listening-journal.md). **M18–M21 remain provisional** — re-validate each at its turn. **NEXT: execute M17.**
- **M16 (✅ shipped, PR #34) — v5's first migration & first backend milestone after the M8→M15 frontend streak:** all 8 sub-features delivered in one broad milestone across 13 tasks. **Execution: 6 Sonnet subagents (one per phase vertical slice: backend migration+commands+tests → API+view+FE tests), serialized because every backend task edits the same `db.rs migrate()` / `lib.rs` handler list / `commands.rs` (a shared working tree — no parallelism).** Controller spec-reviewed each migration's `db.rs` wiring + handler registration directly. **The migration framework is the durable artifact:** `migrate()` reads `PRAGMA user_version`, runs `if current < N { run_step(conn, N, body) }` per step; `run_step` wraps the body in BEGIN/COMMIT/ROLLBACK and bumps `PRAGMA user_version = N` atomically inside the txn (crash mid-migration → DB sits at last fully-applied version); `const LATEST` + an `INSERT OR REPLACE` `schema_version` sentinel; `pub fn open_at_version(v)` mirrors the steps guarded by `if version >= N` for upgrade tests (re-exported in `lib.rs pub mod testing`). **`PRAGMA user_version = N` can't be parameterized** → `format!` with the validated integer is correct/safe. Used `execute_batch("BEGIN"/"COMMIT"/"ROLLBACK")` on a borrowed `&Connection` (NOT `&mut`/`unchecked_transaction`) so callers keep passing `&conn`. Migrations v2–v5 are all ADDITIVE (new tables + `ADD COLUMN` with constant default) → **the MangaReader FK-off table-rebuild was never needed.** Legacy real DBs (v1 tables but `user_version=0` from the old `CREATE TABLE IF NOT EXISTS migrate()`) upgrade cleanly — step 1 re-runs `SCHEMA_V1` idempotently and sets `user_version=1` (tested). **Gotchas / decisions that mattered:** (1) **`lofty 0.21` already a dep** (scan duration + covers) → embedded-metadata ingestion added **NO crate**; `Cargo.lock`/`Cargo.toml` stayed byte-identical (verified via `git diff --stat`). (2) **Fixture generator (`gen-fixture`) uses `hound` (WAV PCM) and CANNOT write ID3/Vorbis tags**, so `apply_metadata`/diff logic is unit-tested with a seeded in-memory DB + constructed proposals, not real tagged fixtures — noted limitation, not a defect. (3) **`detect_series` has no multi-work numeric series in the fixtures** ("Cool Story" is one multi-chapter WORK; titles are non-numeric), so it's tested with a seeded author ("Cool Story"/"Cool Story 2"/"Cool Story 3"); in the live `m16` shot the series spine correctly renders nothing (conditional section). Series UX = **auto-detect-and-apply on author open** (idempotent via `UNIQUE(author_id,title)` + work_id PK; read-only-until-`apply_series`). (4) **Transcripts use `LIKE`, not FTS5** (plan's recommendation — adequate for a local library, zero new dep); `.srt`/`.vtt` are non-audio exts so sidecar ingestion never moves the 43/44/47 counts. (5) **A real gap the controller caught:** Phase 5's transcript components had optional props but **App.tsx was never wired** to call `searchTranscripts`/`getChapterTranscript` (subagent deemed it "not required") → a follow-up fix wired both (search bucket + Now-Playing panel) so the feature is live, not dead. **Lesson: when a subagent leaves "optional props" unpopulated, verify the App-level wiring exists or the feature won't render.** (6) `undo_metadata` deferred (proposals are ephemeral/re-derivable; re-scan+re-preview suffices). **Gate evolution:** cargo gate is now "all green + fixture 43/44/47" (the prior 47 grew to 95 unit + integration as new Rust tests landed); FE 210→248. Screenshot verify: new `m16` walkthrough (6 steps: manage-tags, metadata-diff, series-spine, transcript-search, forgotten-shelf, discover-reasons) + full `m12` regression matrix — Sonnet subagent text verdict **PASS** (sole cross-screen change is the new "Import tags" sidebar nav item). **NEXT: validate M17 (The Listening Journal) before planning — it's the next signature/schema milestone; re-confirm scope vs the v5 backlog.**
- **v5 validation + M16 plan (2026-06-12, docs PR pending):** v4 complete → validated v5 before planning. **Decisions:** (1) **Ordering kept** — M16 Library Intelligence stays first (later milestones consume its data); user confirmed M16 is next. (2) **M16 scope = BROAD** (user choice) — all 8 backlog sub-features in one milestone, not trimmed. (3) **First-migration approach** — anchor the new migration runner on the *smallest, dependency-free* schema change (tag taxonomy) first, then layer the heavier schema; this de-risks the project's first-ever migration. **A thorough backend digest massively de-risked M16:** (a) **migrations genuinely don't exist** — `db.rs:77 migrate()` just runs `SCHEMA_V1` (`CREATE TABLE IF NOT EXISTS`) + a `schema_version` sentinel, no `PRAGMA user_version`; the plan refactors it into a `user_version`-gated, transactional runner with `SCHEMA_V1` as step 1 + an `open_at_version(v)` test helper; (b) **`lofty 0.21` is ALREADY a dependency** (used in `scan.rs probe_duration_secs` + `covers.rs primary_tag()`) → **embedded-metadata ingestion needs NO new crate** (the biggest feared risk evaporated — env is online cargo anyway, `Cargo.lock` committed, no vendored/offline config); (c) **all M16 migrations are ADDITIVE** (new tables + `ALTER TABLE ADD COLUMN`) → the MangaReader FK-off table-rebuild pattern is documented as a helper but NOT exercised by M16 (crash-safety = per-step transaction + atomic `user_version` bump); (d) **most features have backbones:** series reuses `grouping::parse_stem`/`group_author`, "more like this"+"calmer Discover reasons" reuse `discovery_for_tags`+`recommendation_reason` (currently drops the reason — expose it via an additive `DiscoveryWork.reason` field), dormancy = one new query over `play_events.played_at` ms; (e) **tags are denormalized strings** (`author_tags`/`work_tags`/`chapter_tags`, composite PK, no normalized table) → rename/merge = INSERT-OR-IGNORE-then-DELETE across the 3 tables (no FK hazard), only the alias map needs a table; (f) **transcripts** are the one genuinely-new subsystem (new `transcripts` table + sidecar `.srt`/`.vtt` parser + search — use `LIKE` if bundled rusqlite lacks FTS5, no new dep). **Hard invariants for M16:** read-only-on-disk (metadata/series/transcripts only READ; Rename stays the only disk-writer); metadata ingestion is **preview→apply (mirrors `preview_renames`/`apply_renames`/`undo_renames`), NOT auto-at-scan**, so `tests/fixture_scan.rs` stays **43/44/47**; the cargo-test gate becomes "all green + fixture counts 43/44/47" (no longer "stays 47" — new Rust tests are expected). Plan: [`docs/superpowers/plans/2026-06-12-audioshelf-m16-library-intelligence.md`](docs/superpowers/plans/2026-06-12-audioshelf-m16-library-intelligence.md), 13 tasks across 7 phases (runner → tag taxonomy → metadata → series → transcripts → no-schema intelligence → harness/gates). **M17–M21 remain provisional** — re-validate each at its turn. **NEXT: execute M16.**
- **M15 (✅ shipped, PR #31) — v4 COMPLETE:** Home & Browsing Polish — **frontend-only**, no Rust/schema/dep change (`cargo test` stayed 47; fixtures 43/44/47; **no new backend command** — the no-migration streak holds). **Planning digest finding: 3 of 4 roadmap items were mostly already built** (AuthorDetailView already computed total duration + per-work `ProgressBar`; HomeView already rendered a "Recently listened" list; the `PlayedStatus` filter already existed as a `<select>`), so those were polish; the one substantial feature was configurable Home shelves. **(1) Configurable Home shelves:** new `src/lib/shelves.ts` holds `HomeShelf{id,title,kind,tag?/authorId?/status?}`, failsafe `parseHomeShelves`/`serializeHomeShelves` (mirrors `parseBrowsePrefs`), and `loadShelfItems(shelf)` mapping each kind to an **existing** command — `tag`→`getDiscoveryByTags([tag])`, `creator`→`getAuthorDetail(id).works`, `status`→`getAuthors()`+`filterAuthors` (status shelves render **creator** cards, since a cross-author works-by-status list would need new Rust — deliberately avoided). Persisted as one `home_shelves` JSON setting via a `persistShelves` helper + startup `parseHomeShelves(getSetting("home_shelves"))`. **Architecture decision: App fetches shelf data in a `useEffect` keyed on `homeShelves` and passes `shelves`/`shelfItems` down, so HomeView and the new pure `Shelf` component never call `invoke`** (keeps them test-friendly; the kind→command map is unit-tested in `shelves.test.ts` with `vi.mock("./api")`). New `.card-row` horizontal-scroll CSS (the pre-existing `.card-grid` is a wrapping grid). **Default `home_shelves` is EMPTY** → Home renders exactly as before until the user adds shelves (no empty-Home regression, no fixture dependency). **(2) Shelf management in SettingsView** (additive — 225 insertions/0 deletions, existing Settings untouched): list with ▲/▼ reorder + Remove, and an Add-shelf form (kind `<select>` → dependent tag/creator/status target + title); App mints ids `s{counter}_{len}`. **(3) Library tabs:** promoted the status filter out of `SortFilterBar`'s `<select>` into a top-level `.tabs` bar (`role="tablist"`) in LibraryView; the select was removed from SortFilterBar (Sort+Tag remain); tabs hidden during search. **(4) Creator-hub hours:** AuthorDetail header total + per-work totals switched `formatDuration`(m:ss)→`formatLong`("2h 5m"); per-work `ProgressBar` kept. **(5)** Home recent-listened rows made clickable via `CreatorIdentity onOpen`→`onOpenAuthor`. New harness `home-shelves` step (seeds a `cozy` tag shelf + an `unstarted` status shelf, items seeded via `loadShelfItems` for determinism) + `runner.test.ts`. **Token substitutions vs plan (reported by implementers):** `SectionHeading` takes a `title` prop (not children); `--radius-pill` doesn't exist → used `999px` (matches `.chip`/`.badge`). **Screenshot-verify nuance:** the Author-Detail total reads `0m` in the shot **only because the fixtures use ~5-second synthetic clips** (sub-minute author totals) — `formatLong` is unit-tested (7500s→"2h 5m") and is correct for the app's hours-long spoken-audio domain; same artifact class as M14's all-unplayed dots, NOT a code defect. Verified: tsc clean, **210 FE** (+49) + 47 Rust green, before/after `m12` matrix PASS (shelves as horizontal rows, Library tabs with select removed, Settings management section, regressions intact). **v4 (M13–M15) is now fully ✅ Merged.** **NEXT: validate v5 (M16–M21, provisional) before planning M16** — re-confirm scope/order and the first-schema-migration decision per the v5 backlog spec.
- **M14 (✅ shipped, PR #28):** Player & Now Playing Polish — **frontend-only**, no Rust/schema/dep change (`cargo test` stayed 47; fixtures 43/44/47). (1) **Cycling time label:** the left seek timestamp became a `<button className="time-label">` (transparent reset, inherits font/color) cycling **elapsed → remaining (`-m:ss`) → percent**; the cycle mode lives in `App.tsx` (`timeLabelMode` + `cycleTimeLabel`) and is passed to BOTH `<PlayerBar>` and `<NowPlayingPanel>` so the compact bar and the expanded panel stay in sync. New pure helpers in `src/player/playback.ts`: `formatTimeLeft`, `formatPercent`, and a shared `timeLabel(mode,current,duration)` + exported `TimeLabelMode` union (home in `playback.ts` so both player components import it). New props are **optional/defaulted** (`timeLabelMode?`/`onCycleTimeLabel?`) so the button is inert and existing tests that omit them still pass. (2) **Chapters-in-this-work panel:** solved **FE-only, no new Rust command** — `App.tsx` holds `currentWorkChapters` populated by a `useEffect` keyed on `current?.workId/authorId` that calls the existing `getAuthorDetail` and picks the matching work's `chapters` (keying on workId+authorId avoids refetch on mere time updates). `jumpToChapter(chapter)` rebuilds a `PlaybackContext` (using `currentRef.current` for author/work identity, recomputing `workTotalChapters`/`workPlayedChapters` from the fetched list) and calls existing `playChapter` — **no per-second offset** (jumps start the chapter at 0, consistent with the app's chapter-at-a-time model). `NowPlayingPanel` renders `.now-playing__chapters` ("In this work") only when `chapters.length > 1`: each row a `.chapter-jump` button with a played/unplayed dot (`--color-accent` vs `--color-border-strong`), `Ch N — Title`, a "played" tag, current row highlighted (`.chapter-jump--current` accent-soft + `aria-current`); the list scrolls (`max-height:220px`) so long works don't blow out the M13 `Dialog`. (3) Layout polish (CSS-only). **Harness:** new `player-chapters` step after `player-expanded` (+ `runner.test.ts` order). **Gotcha caught by screenshot-verify:** the step first played `creator.works[0]`, but works are **title-ordered** so `works[0]` is the single-chapter "Another Standalone Tale" → the panel never rendered; fixed by selecting the work with the **most chapters** (a `reduce`), which is "Cool Story" (multi-chapter) — re-capture then showed the "In this work" list + `-0:02` remaining label. (Played dots all read unplayed in the shot because no chapter in that work is `played` in the harness state — correct; the played/unplayed dot is unit-tested.) **The chapters list reflects played-state at panel-open and intentionally does NOT live-update on `onTimeUpdate`/`handleEnded`** (kept off the hot path). Verified: tsc clean, **161 FE** (+20) + 47 Rust green, before/after `m12`+`player` subagent-verified PASS, `player` walkthrough unregressed. **NEXT: plan M15** (Home & Browsing Polish).
- **M13 (✅ shipped, PR #25):** UI Consistency & UX Refinement — **frontend-only**, no Rust/schema/dep change (`cargo test` stayed 47; the no-migration streak is irrelevant here since no DB touched). Built shared primitives in `src/components/ui.tsx` — `PageHeader`, `SectionHeading`, `TagGroup`, and a reusable `Dialog` (top-right close, Escape, `data-autofocus`) — and adopted them across every view, killing the inline header/section/pill duplication. **Play-next-chapter from any work card** was solved FE-only (App helper `playNextChapterOfWork(workId,authorId)` = `getAuthorDetail`→first `!played` chapter else `chapters[0]`→build `PlaybackContext`→existing `playChapter`) — **no new Rust command needed** (the digest confirmed `ContinueItem.nextChapter` only exists for the one in-progress work, and `DiscoveryWork`/`RecommendationWork`/`ChapterHit` carry no chapter rows, so the card fetches author detail on click). `WorkCard` gained optional `onPlay` (primary ▶ Play, demoting "View creator" to ghost) — kept optional so the featured Keep-listening card and inline-prop view tests didn't break. **Author-detail editing isolated:** chapter rows are now browse-only (play, played toggle, title, read-only `TagGroup`, a per-chapter `more` menu with "Edit grouping"/"Edit tags"); the editors open in the new `Dialog` (local `EditState={chapterId,mode}`), fixing the worst collision (m12 shot 06); removed the redundant per-row `CreatorIdentity`; toolbar shows "Works (N)". **`TagEditor` datalist id collision** (hardcoded `id="all-tags"` broke when multiple editors mount) fixed with `useId()`. **Now Playing** reuses `Dialog` (close moved top-left→**top-right**), shows "Chapter X of Y" + "Plays this chapter, then stops."/"Last chapter…" + a tappable work title (fills the empty lower third); PlayerBar shows chapter position + a divided utility cluster. **Discovery** "Pick a tag" moved above "For You" with native checkboxes → `chip--toggle` pill buttons (`aria-pressed`). First-run/empty Home = a real welcome (mental-model + Settings CTA) that **suppresses the false "You May Like" recs** at zero history. **Digest gotchas that mattered:** the `…` was already an `<IconButton icon="more">` (a contrast matter, not raw text — so no rewrite); keep new props OPTIONAL (AuthorDetail/Discovery/Library tests build props inline). **Screenshot-verify caught one harness gap:** the `m12` `context-menu` step landed on the new empty Home because step 7 (`showDiscoveryByTag`) calls `resetPlayHistory()` and later steps never re-seeded → fixed by re-seeding 2 finished chapters in `showContextMenu` before `loadHome()` (so the featured card + its overflow menu render). Verified: tsc clean, 141 FE + 47 Rust green, before/after `m12` matrix all-PASS, 7 walkthrough regressions clean. **NEXT: plan M14** (Player & Now Playing Polish — builds on M13's `Dialog`/tooltip primitives).
- **v5 scope (drafted 2026-06-12 — PROVISIONAL, validate before implementing):** "Depth & Reach" = six provisional milestones synthesized from an intensive **five-lens** subagent UX review (power-user, accessibility, engagement/insight, library-intelligence, spoken-audio domain) over the `m12` screens, captured in [`docs/superpowers/specs/2026-06-12-audioshelf-v5-backlog.md`](docs/superpowers/specs/2026-06-12-audioshelf-v5-backlog.md). **M16 Library Intelligence** (metadata ingestion / series detection / tag taxonomy / dormancy / transcripts) → **M17 The Listening Journal** (notes/bookmarks/summaries/favorites/completion ritual — the signature) → **M18 Insight & Year in Listening** (heatmap/trends/recap) → **M19 Power & Scale** (command palette / advanced search / bulk-tag / smart collections / export-import / health scan) → **M20 Accessibility & Platform Integration** (keyboard/SR/SMTC media keys/light+high-contrast/i18n) → **M21 Spoken-Audio Rituals** ("what fits?"/wind-down/narrator/return-nudges). **Key decisions to re-confirm at v5 start:** (1) v5 **intentionally ends the M8→M12 no-migration streak** — M16/M17 need the app's first schema migrations (reuse the crash-safe FK-off table-rebuild pattern from the MangaReader sibling); (2) read-only-on-disk preserved (metadata ingestion only *reads* file tags); (3) non-goals still hold (no queue/autoplay/speed/social). **Parked pending a product decision:** listen-later "queues of intent" (brushes no-queue), opt-in daily reminders (outbound signal); per-second resume + multi-root remain deferred (per-second resume gets cheaper if M17 bookmarks land position storage). Convergence signal (3+ lenses agreed): tag taxonomy, stats/heatmap, transcripts/search-within-audio, "what fits?", dormancy/forgotten works, media-key/SMTC. **This is a backlog, not a commitment** — v4 ships first; re-validate scope/order after v4. NEXT (unchanged): **plan M13** (the actual next work).
- **v4 scope (decided 2026-06-12; M13 added after a UI/UX review):** "Player & Home Polish" = **M13 UI Consistency & UX Refinement** → **M14 Player & Now Playing Polish** → **M15 Home & Browsing Polish**. Originally two feature milestones (player→home, user-chosen split) scoped from a token-lean web survey of audio apps; then a **two-lens expert review** (senior UI engineer + senior UX designer over the 13-shot `m12` matrix — captured in [`docs/superpowers/specs/2026-06-12-audioshelf-ui-ux-review.md`](docs/superpowers/specs/2026-06-12-audioshelf-ui-ux-review.md)) surfaced enough **cross-cutting** consistency + core-loop issues to warrant a **foundation milestone first**: M13 builds the shared primitives (`<SectionHeading>`, `<TagGroup>`, `<Dialog>`/`<Modal>`, `<IconButton>` overflow, icon tooltips, responsive card grid) and fixes global defects + the core-loop ("Play next chapter" on work cards vs the current away-navigating "View creator"; true first-run empty Home; isolate the author-detail editing collision; make the chapter-at-a-time model explicit; reorder Discover). The relative player→home order is preserved (M14 before M15). **Feature additions stay in M14/M15** (chapters-in-work jump panel; configurable Home shelves; creator hours/progress) — they build on M13's primitives, avoiding rework. **M14** = cycling time label (Libby) + chapters-in-this-work jump panel in the expanded Now Playing + expanded-panel feature refresh. **M15** = configurable Home shelves (MangaReader-style, persisted JSON setting) + played-status Library tabs (promote the M10 filter) + richer creator hub (hours + per-work progress) + recent-history surfacing. **All read-only / additive — keeps the M8→M12 no-migration streak.** **Explicitly NOT in v4** (would fight the design or break the streak): per-second mid-chapter resume (needs a `chapters.playback_position_secs` migration — kept deferred), sleep-timer-in-chapters (marginal: the app already stops after each chapter), and the poor-fit survey patterns (queue/Up-Next, autoplay/radio, playback-speed, social feeds). NEXT: **plan M13** (Opus planning session).
- **Grouping:** `original_stem` is threaded for on-disk file lookup; canonical `stem` drops trailing words. Lone numbered file demoted to standalone ("Area 51" not split into "Area"/51).
- **Asset protocol:** static scope `[]` + runtime `allow_directory(root, true)` in `scan_library` (library root only).
- **Harness:** `settle()` (double rAF + 60ms) before every screenshot; a walkthrough step that reads React state set by the immediately-prior step must be self-contained (re-fetch). Screenshots render on the user's iOS app via the Read tool.
- **Subagents** must run cargo in the FOREGROUND (background builds can't be resumed).
- **Windows:** a colon in a filename creates an NTFS Alternate Data Stream, not a real file (bit M4 tests).
- **Rename↔grouping (v1 limit):** `grouping_overrides` is keyed on `file_path`, so renaming an overridden file via M4 orphans its override row. Acceptable for v1.
- **Settings (M6):** library root persisted in the `settings` table under key `library_root`. The `--library` flag is intentionally **not** persisted (keeps the harness hermetic); only user-picked roots survive restarts. Native picker needs a **capability file** (`capabilities/default.json` granting `dialog:default`) — plugin commands are gated, unlike custom app commands.
- **Tauri debug rebuild gotcha:** `cargo tauri build --debug` is a no-op cache hit if no *Rust* source changed — even after `npm run build` updated the embedded `dist/`. A stale binary runs old JS and the harness hangs. Force a relink (`cargo clean`, or touch a Rust file) after a frontend-only change before re-running the screenshot harness.
- **v2 scope (decided 2026-06-12):** "Library Experience" = M8 covers, M9 work/chapter tags, M10 richer browsing UI. **Multi-root libraries explicitly deferred** (not chosen). Playback-side features (speed, per-second resume, auto-advance) deferred to a possible later v3.
- **M8 design (cover art):** Resolved **on-the-fly by command** (`get_work_cover` embedded-first, `get_author_cover` folder-image-first) — **NO schema change/migration**. The library is flat (`Author/file.wav`), and chapter `file_path`s are absolute, so an author dir = `parent(file_path)`; this sidesteps the harness-unset `library_root` setting. Thumbnails (`image` crate, max 256px) cached under `<app_data>/covers/` keyed by source path+mtime hash, served via the **existing asset protocol** (`allow_directory(covers, true)` granted in `setup`; `convertFileSrc` on the FE) — no `base64` crate needed. `<Cover>` lazy-loads w/ a module-level client cache and falls back to the color+initials `<Swatch>` (no layout shift). **Harness gotcha:** `settle()` (2 rAF + 60ms) does NOT wait for `<img>` decode → added `imagesSettled()` before cover screenshots. Fixtures use **folder `cover.png`** images (PNG via `image` crate) on Jane Doe + Sam Smith — non-audio, so `fixture_scan.rs` counts stay 43/44/47; embedded extraction covered by unit tests (`lofty` picture read).
- **M8 (✅ shipped, PR #8):** Covers resolved **on-the-fly by command** (`get_work_cover` embedded-first, `get_author_cover` folder-first) — **NO schema change/migration**, as planned. Author dir = `parent(file_path)` (chapter paths are absolute), deliberately avoiding the harness-unset `library_root`. Thumbnails (`image` 0.25, ≤256px, aspect preserved) cached under `<app_data>/covers/` keyed by hash(source path + mtime), reused if present; served via the **existing asset protocol** (`allow_directory(covers, true)` granted in `setup`, `convertFileSrc`/`fileUrl` on the FE) — **no `base64` crate**. `<Cover>` (in `src/components/Cover.tsx`, which now also owns the shared `<Swatch>` moved out of `LibraryView`) lazy-loads with a **module-level `Map` cache** (so virtualized rows remounting on scroll don't refetch) and falls back to the initials `<Swatch>` — no layout shift, no broken-image icon. **Harness gotcha:** `settle()` (2 rAF + 60ms) does NOT wait for `<img>` decode, so added `imagesSettled()` (resolves on each image's `load`/`error`) and capture does `settle()→imagesSettled()→settle()`. Fixtures drop a non-audio `cover.png` into Jane Doe (red) + Sam Smith (blue), so `fixture_scan` counts stay 43/44/47 (do not edit that test). The `image=0.25` dep added to `tools/gen-fixture` pulls ~975 transitive `Cargo.lock` lines — must be committed (a later cargo build can regenerate it post-commit; re-check `git status` before PR). New harness output dir `.shots-baseline/` is gitignored. Non-atomic cache write (write-not-rename) accepted for a local thumbnail cache. PR also carried a small `chore(harness)` commit: an informational tile-max pixel-diff helper (`tools/Compare-Screenshots.ps1` + `verify.ps1` manifest) — CHANGED/NEW shots never fail a run.
- **M9 design (work/chapter tags — planned 2026-06-12):** Two new tables `work_tags(work_id,tag)` / `chapter_tags(chapter_id,tag)` appended to `SCHEMA_V1` (idempotent `IF NOT EXISTS`, no version bump/runner change). A generic `replace_tags(conn, table, key_col, id, tags)` helper backs author/work/chapter writes (table/col are **compile-time constants**, never user input → `format!` injection-safe); `set_tags` kept as the author alias so existing tests pass. `discovery_for_tags` **refactored author-centric → work-centric**: iterate active works w/ ≥1 unplayed chapter, match on the **union** of author-tags ∪ work-tags (BTreeSet → sorted `sharedTags`); backward-compatible (author-only works surface identically — existing `discovery_by_tags_ranks…` test stays green). **Chapter tags deliberately excluded from Discover** (too granular). `discovery_for_you` also seeds from recent authors' works' work-tags. Search gains tag matching via `OR EXISTS (SELECT 1 FROM work_tags/chapter_tags … LIKE ?1 ESCAPE '\')` folded into the existing works/chapters buckets (no new bucket types). `get_all_tags` = `UNION` across all 3 tables. `WorkRow`/`ChapterRow` gain `tags: Vec<String>`, populated in `query_author_detail`. UI: always-visible work `<TagEditor>` under each work heading; **per-chapter `<ChapterTags>` toggle** (🏷 Tags, open-by-default when `tags.length>0`). Tags are **seeded at runtime** in the `tags` walkthrough (mirrors M3 discovery seeding) → **fixture_scan counts stay 43/44/47, no on-disk fixture/`fixture_scan.rs` change**. No new crate deps (no `Cargo.lock` churn). New `tags` walkthrough (seed → detail → search-by-tag); regression-verify `discovery` + `m7` (the refactor touched both). Read-only-on-disk intact (only writes are SQLite tag rows).
- **M9 (✅ shipped, PR #10):** Work/chapter tagging. Two idempotent tables `work_tags(work_id,tag)` / `chapter_tags(chapter_id,tag)` appended to `SCHEMA_V1` (`IF NOT EXISTS`, no version bump). Generic `replace_tags(conn, table, key_col, id, tags)` helper backs all three levels — `table`/`key_col` are **compile-time `&'static str` constants** (never user input → `format!` injection-safe); `set_tags` kept as the author alias (NOT `id_eq(...)` — that helper never existed; the plan's own correction). `discovery_for_tags` **refactored author-centric → work-centric**: iterate active works w/ ≥1 unplayed chapter, match on the **union** of author-tags ∪ work-tags (BTreeSet → sorted `sharedTags`); backward-compatible (author-only works surface identically — `discovery_by_tags_ranks_shared_then_unplayed` stayed green). **Chapter tags deliberately excluded from Discover** (too granular). `discovery_for_you` also seeds recent authors' work-tags. Search matches tags via `OR EXISTS (SELECT 1 FROM work_tags/chapter_tags … LIKE ?1 ESCAPE '\')` folded into the existing works/chapters buckets (no new bucket types, `?1`/`?2` bindings unchanged). `get_all_tags` = `UNION` across all 3 tables. UI: always-visible work `<TagEditor>` under each work heading; per-chapter `<ChapterTags>` **toggle** (🏷 Tags, open-by-default when `tags.length>0`). Tags **seeded at runtime** in the `tags` walkthrough (mirrors M3) → **fixture_scan counts stayed 43/44/47, no on-disk fixture change**. **No new crate deps → no `Cargo.lock` churn.** Verified: 61 FE + 45 Rust tests green; `tags` walkthrough screenshot shows all three tag levels (author "cozy" · work "mystery" · chapter "intro" open) + search "mystery" surfaces the work by tag not title; `discovery` healthy post-refactor, `m7` pixel-identical. Read-only-on-disk intact (only writes are SQLite tag rows).
- **M10 design (richer browsing UI — planned 2026-06-12):** **No schema migration** (preserves the M8/M9 streak). The user dropped **"recently-added" sort** precisely because the schema has no `added_at`/`created_at`/`scanned_at` column and we won't add one for it; sorts are **A–Z / Length / Played %**. Filters = **tag** (author's own tags ∪ its works' tags) + **played status** (All / Has unplayed / Fully played / Not started). Backend delta is minimal: `AuthorRow` gains `total_secs` (`SUM(duration_secs)` over active chapters) + `tags` (author_tags ∪ work_tags, chapter tags excluded mirroring M9 Discover), both populated in `query_authors` — **no DDL change**. **Sorting/filtering is client-side** on the already-in-memory, virtualized author list (M7 fetched all up front); work-level length/played% derive FE-side from `WorkRow.chapters`. Author detail gains a work-sort `<select>` + collapse/expand-all (per-work + all); **collapse state is session-only** (not persisted), only the chapter `<ul>` collapses (work tags stay visible). Preferences (authorSort, filterTag, filterStatus, workSort) persist as **one `browse_prefs` JSON** in the M6 settings table; `parseBrowsePrefs` fails safe to defaults on malformed/absent value. New pure module `src/lib/browse.ts` (sort/filter/parse, unit-tested) + `SortFilterBar.tsx`; `summarizeAuthor` enriched with `% played` + total length (shared `formatDuration` extracted to `src/lib/time.ts` if currently local). The existing **`browse` walkthrough** is extended (seed tags+played at runtime → sorted → filtered → detail) so **fixtures stay 43/44/47**, no on-disk fixture/`fixture_scan.rs` change, no `Cargo.lock` churn. Read-only-on-disk intact (only write is the `browse_prefs` setting). **Length-sort ordering is verified by unit tests** (fixture WAV durations may be ~0/equal, so screenshots only confirm the UI wired up).
- **M10 (✅ shipped, PR #12):** Richer browsing UI. **No schema change** (M8/M9/M10 no-migration streak intact) — "recently-added" sort was dropped precisely to avoid adding an `added_at` column. Backend delta only: `AuthorRow` gained `total_secs` (`COALESCE(SUM(c.duration_secs),0)` over active chapters) + `tags` (author_tags ∪ that author's active work_tags, chapter tags excluded mirroring M9 Discover), both populated in `query_authors` (two grouped passes into a `HashMap<i64,BTreeSet>` → sorted/deduped). **Sorting & filtering are client-side** on the already-in-memory virtualized author list (M7 fetched all up front); work length/played% derive FE-side from `WorkRow.chapters`. New pure module `src/lib/browse.ts` (`sortAuthors`/`filterAuthors`/`sortWorks`/`authorPlayedFraction`/`workPlayedFraction`/`parseBrowsePrefs`, all unit-tested) + `SortFilterBar.tsx`; shared `formatDuration` extracted from `AuthorDetailView` to `src/lib/time.ts` (now shared by `library.ts`/`browse`). Author detail: work-sort `<select>` + per-work ▾/▸ collapse + Collapse/Expand-all — only the chapter `<ul>` collapses (work `<TagEditor>` stays visible); **collapse state is session-only** (not persisted). Prefs (authorSort/filterTag/filterStatus/workSort) persist as **one `browse_prefs` JSON** in the M6 settings table, seeded on startup via `parseBrowsePrefs(getSetting("browse_prefs"))` after scan (not on first-run onboarding); malformed/absent → defaults (fail safe). **Gotcha caught in screenshot verify:** the persist handlers originally each spread a stale `browsePrefs` closure, so two synchronous setters (`setFilterTag` then `setFilterStatus`) made the second overwrite the first (`filterTag` lost) — fixed by converting to **functional state updaters** (`persistPrefs((prev)=>…)`) that compose and persist the actually-applied value. `browse` walkthrough extended (seed cozy tags on 3 authors + mark one chapter finished at runtime → sorted-by-length → tag+status filter → author detail) so **fixtures stayed 43/44/47, no on-disk fixture change**. **No new crate deps → no `Cargo.lock` churn.** Frontend-only fix required a forced Rust relink (`cargo clean -p audioshelf`) before re-running the harness (the debug-rebuild cache-hit gotcha). Verified: 104 FE + 47 Rust tests green; browse screenshots show all three sort/filter selects + enriched summary + Tag=cozy/Has-unplayed narrowing + author-detail work-sort/collapse; m7/tags/covers unregressed. Read-only-on-disk intact (only write is the `browse_prefs` setting).
- **M7 (✅ shipped):** Search is a **backend** `search_library` SQL command (indexed `LIKE` w/ ESCAPE, per-bucket cap 50), debounced 150ms client-side — *not* client filtering (would defeat the 10k scale target). `LibraryView` is **fully controlled** by App (query + results props), staying free of `invoke`. Author list virtualized with **`react-window` `FixedSizeList`** (explicit height/itemSize → renders a bounded window even in jsdom, enabling a deterministic 1000-author bound test). Cover placeholders are **inline-styled** color+initials (app ships no stylesheet, so a CSS class would be inert). Fixture gained **40 `Zz Sample Author NN`** authors named to sort **after** the real three so first-author walkthroughs (player, grouping) are unaffected — regression-verified. **Gotcha:** the fixture-count integration test (`src-tauri/tests/fixture_scan.rs`) hardcodes author/work/chapter counts (was 3/4/7, now 43/44/47) — any future fixture change must update it. Substring search means querying `sam` also matches `Zz Sample Author NN` (the substring "sam" is in "Sample") — expected, not a bug. **`react-window` v1 only** (v2 has a breaking API). CI uses `npm ci`, so `package-lock.json` ships synced. **With M7 the v1 spec (design doc §4/§6/§13) is fully implemented.**
- **M11 design (Personal Home — planned 2026-06-12):** One new read-only command `query_home(now_ms, tz_offset_minutes) -> HomeData` (registered after `get_more_from_author`). **NO schema migration / no new crate deps** — all derived from `play_events` / `chapters.played` / `chapters.duration_secs` (std-only math, no `chrono`). Two backend sections: (1) `home_continue` — author/creator-centric "Jump back in" reusing `recent_authors()`; per recent author resume at the next unplayed chapter (prefer most-recently-played work, else first active work by `sort_key`; **chapter granularity, no per-second offset**); fully-played authors omitted; `next_chapter` is a full `ChapterRow` so the FE plays it directly. (2) `home_stats` — totals from the `played` flag (replays **not** double-counted), `chapters_finished`, recent-10 history, and a `streak_days` from distinct **local** day-buckets of `play_events.played_at` (local day = `(ms - tzOffsetMin*60000).div_euclid(86_400_000)`; `getTimezoneOffset()` = UTC−local; streak live only if last active day is today/yesterday — pure `streak_len` helper, unit-tested). New structs in `model.rs` (`HomeData`/`ContinueItem`/`ListeningStats`/`RecentItem`). FE: new default `home` route added to the `Route` union; `App` holds `home`/`homeNow` + `loadHome()`/`openHome()`; lands on Home after scan/onboarding (replacing the old `library` landing); new `HomeView.tsx` (pure renderer, inline styles, reuses `<Cover kind="work">`, `formatLong`/`formatRelative` added to `time.ts`). Library **demoted**: gains an `onOpenHome` prop + 🏠 Home button (LibraryView test baseProps updated). New `home` walkthrough seeds **two finished chapters across two days at runtime** (empty-home then populated-home shots) → **fixtures stay 43/44/47, no on-disk/`fixture_scan.rs`/`db.rs` change**. Plan: [M11](docs/superpowers/plans/2026-06-12-audioshelf-m11-personal-home.md).
- **M11 (✅ shipped, PR #16):** Personal Home. New default `home` route; Library demoted to a 🏠-reachable nav button (and Home reachable from Library). Backend `query_home(now_ms, tz_offset_minutes) -> HomeData` (read-only) with `home_continue` (author-centric "Jump back in": reuses `recent_authors()`, over-fetches 2× since fully-played authors are skipped; resume = lowest-`chapter_no` unplayed in the author's **most-recently-played work**, else first active work by `sort_key` with an unplayed chapter; omits authors with nothing unplayed; `next_chapter` is a full `ChapterRow` so the FE plays it directly), `home_stats` (totals from the **`played` flag** so replays aren't double-counted; **streak via tz-offset local-day bucketing** — `local_day = (ms - tzOffsetMin*60000).div_euclid(86_400_000)`, live only if the last active day is today/yesterday; pure `streak_len` helper, unit-tested; recent-10 history), and `load_chapter_row`. New structs `HomeData`/`ContinueItem`/`ListeningStats`/`RecentItem`. **No schema migration** (M8→M11 no-migration streak intact), **no new crate deps → no `Cargo.lock` churn** (std-only date math, no `chrono`), read-only on disk (only writes stay existing `play_events`/`played` rows). FE: `HomeView.tsx` (pure renderer, inline styles, reuses `<Cover>`), `queryHome` wrapper, `formatLong`/`formatRelative` in `time.ts` (+ tests); App holds `home`/`homeNow` + `loadHome()`/`openHome()`, lands on Home after scan/onboarding. **Harness gotcha (caught in screenshot verify):** the harness shares the app-data DB path (`com.audioshelf.app/audioshelf.db`) with the real app and is never cleared, so a prior run's seeded play-events leaked into the empty-home shot. Fix = a **harness-only, not-UI-wired** `reset_play_history` command (clears only the regenerable `play_events` + `played` flag) invoked by `showEmptyHome` before capture — deliberately surgical: chosen over deleting the DB file, which would also wipe the persisted `library_root`/settings and force re-onboarding (worse). Revisit at M12 if a cleaner harness-isolated app-data dir is introduced. The `home` walkthrough seeds two finished chapters across two days **at runtime** → fixtures stayed 43/44/47, `fixture_scan.rs`/`db.rs` untouched. Verified: 110 FE + 50 Rust tests green; `01-home-empty.png` (zero tiles + "Nothing played yet", no Jump-back-in) & `02-home.png` (Jump-back-in row + ▶ Play, non-zero tiles, Recent list) both PASS; browse/player unregressed.
- **M12 design (Design System & Theming — planned 2026-06-12):** Approved direction is **dark-only**, replacing the roadmap's earlier light/dark-toggle draft: no `settings.theme`; near-black canvas, dark-blue/gray surfaces, electric-blue accent, balanced density, soft-modern radii/elevation, and expressive but bounded transform/opacity motion with `prefers-reduced-motion`. Add the app's first CSS token/component layer and local SVG icons (no UI dependency), a persistent manually collapsible left sidebar (expanded by default; only new setting is `sidebar_collapsed`), and restyle every view. Creator context is mandatory wherever audio appears: square work art + circular creator portrait/initials fallback, with context-sensitive tags/progress/metadata. Home becomes one **Keep listening to [Creator]** feature (latest resumable creator, preserving M11's deterministic next-unplayed logic) followed by a responsive six-card **You May Like** grid with factual reasons, then recent history and stats. A narrow read-only Rust/API extension ranks recommendations from existing author/work tags + play history, excludes the featured work, favors mostly-unplayed and creator diversity, and falls back deterministically; **no schema migration/new dependency/audio-file mutation**. PlayerBar becomes creator-rich and expands into an accessible in-app Now Playing panel that reuses the single audio element/state (no route/queue/autoplay/speed). Verification requires durable pre-change and post-change screenshots for every view/state plus all existing walkthrough regressions. Plan: [M12](docs/superpowers/plans/2026-06-12-audioshelf-m12-design-system.md); spec: [M12 design](docs/superpowers/specs/2026-06-12-audioshelf-m12-design-system.md).
- **M12 (✅ shipped, PR #19):** Design System & Theming — the app's first stylesheet, **dark-only** (the earlier light/dark-toggle draft was dropped; no `settings.theme`). Token/component layer: `src/styles/{tokens,base,components,layout}.css` (imported in `main.tsx`) + `src/components/` (`ui.tsx` primitives, `Icon.tsx` local SVGs — **no UI dep added**, `AppShell.tsx`, `Menu.tsx`, `WorkCard.tsx`, `CreatorIdentity.tsx`, `Cover.tsx` now exposing `WorkArtwork`/`CreatorAvatar`). Persistent collapsible left sidebar; **only new persisted setting is `sidebar_collapsed`** via the M6 settings table (`getSetting==="true"`, fails safe to expanded — no schema change). Every view restyled onto tokens. Home redesigned: **Keep listening to [Creator]** feature (preserves M11's deterministic next-unplayed resume) + six-card **You May Like** grid; new read-only backend `home_keep_listening`/`home_recommendations` (in `commands.rs`, helpers in `src/lib/home.ts`) rank from existing tags + play history, **exclude the featured work** (by work, not author), favor mostly-unplayed + creator diversity (two-pass: one work/author then fill), deterministic sparse/empty-history fallback (author-name tiebreak); 3 new Rust tests. Creator-rich PlayerBar expands into an in-app **Now Playing panel** (`NowPlayingPanel.tsx`, `role="dialog"`, focus-trap, Escape) reusing the single audio element/state — no route/queue/autoplay/speed. **No schema migration, no new dep → no `Cargo.lock` churn, read-only on disk, fixtures stayed 43/44/47** (`db.rs`/`fixture_scan.rs` untouched). **Carry-over note:** most of M12 was authored by a prior **Codex** session and left uncommitted on `m12-design-system`; this run preserved it (WIP checkpoint commit), fixed 3 failing FE tests (CreatorIdentity a11y name — avatar inside `CreatorIdentity` is now `aria-hidden` decorative so the button is named by the creator alone; de-duplicated Settings first-run copy; corrected a LibraryView empty-filter test string), fixed 2 walkthrough-artifact issues (m12 `home-empty` resets play history → no Keep-listening card in the no-history state; `discovery-by-tag` step made self-contained so the tag-filtered view is visible/distinct — the by-tag path itself was NOT regressed), and applied one defensive hardening (`MAX(played_at)` → fail-safe `Option<i64>`/skip). **Harness gotcha reconfirmed:** the shared app-data DB (`com.audioshelf.app/audioshelf.db`) persists play-events across runs → the M11 harness-only `reset_play_history` (not UI-wired) is reused for clean empty-state shots. Also hit a stale `.git/index.lock` from the crashed Codex session (44 min old, no live git process → safe to remove). Verified: 122 FE + 52 Rust tests green; 13-shot `m12` matrix screenshot-verified (dark theme consistent, sidebar collapse, Keep-listening + You-May-Like, search, author detail, discovery + by-tag, rename, settings, compact + expanded player, context menu) + all 10 prior walkthroughs completed OK (pixel diffs CHANGED as expected for a full restyle — not regressions). **With M12, v3 (Personal Home & Look) is COMPLETE and the whole roadmap M1–M12 is ✅ Merged.**
- **v3 scope (decided 2026-06-12):** "Personal Home & Look" = **M11 Personal Home** then **M12 Design System & Theming**, in that order (home first, design last — user-chosen, so the design pass re-skins the new home in one go). M11 **merges continue-listening + stats/history into ONE personal landing surface** (user: "they should persist together"); continue-listening is **author/creator-centric, not per-audio** — recently-played *authors* via the existing `recent_authors()` helper (GROUP BY author, MAX(played_at)), resuming at the **next unplayed chapter** (**chapter granularity, no per-second offset** — fits the app's "a chapter at a time / stop after each" model). v3 keeps the **M8→M10 no-migration streak**: both milestones are read-only-on-disk, deriving everything from existing `play_events` / `chapters.played` / `settings` (the only writes stay the SQLite settings + play_events rows). Design system is **greenfield** (no CSS today — all inline `style={}`), so M12 is additive, not a re-theme; the approved implementation is dark-only with persisted `sidebar_collapsed`, not the earlier `theme` setting draft. **Deferred / not chosen for v3:** per-second mid-chapter resume (would need a `chapters.playback_position_secs` column — from the set-aside playback-mechanics bucket), multi-root libraries, playback speed/auto-advance.
