# AudioShelf — ROADMAP

> Source of truth for what to build next. A fresh session reads this, finds the topmost milestone that is **not ✅ Merged**, and acts. Follows the `/roadmap` workflow (Opus plans/researches · Sonnet implements · ping at every phase handoff).

**Legend:** ✅ Merged · 📝 Plan ready (execute next) · 🔬 Researching/Planning · [ ] Not started (plan first)

## Definition

Windows desktop app for listening to a large library of short-form spoken audio (audiobooks ~5min–1hr, a chapter at a time). Tauri 2 + React 18 + TypeScript + SQLite (rusqlite). **Read-only on audio files by default**; the rename tool is the sole sanctioned mutation.

- Repo: https://github.com/yovanmc/AudioShelf (default branch `main`)
- Design spec: [`docs/superpowers/specs/2026-06-11-audioshelf-design.md`](docs/superpowers/specs/2026-06-11-audioshelf-design.md)
- Detailed runbook (how-to, env, CI): [`docs/superpowers/WORKFLOW-execution.md`](docs/superpowers/WORKFLOW-execution.md)

## Conventions

- Cargo via `cmd /c "tools\dev-env.cmd cargo ..."` in the FOREGROUND (large timeout); `npm run build` before any `cargo tauri build`.
- Gates: `npx tsc --noEmit` · `npm test` · `cargo test` · `tools\verify.ps1 -Walkthrough <name>` (screenshot self-verification).
- Commits: use the repo's configured git identity (`yovanmc <yovanmc@users.noreply.github.com>`) — never pass `-c user.email=...` overrides. Add trailer `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`. **No Codex trailer.**
- CI: `build-and-test` on windows-latest. Merge PRs `--merge --delete-branch` from main; FOREGROUND `gh pr checks <PR#> --watch`.
- App ships **no stylesheet** in v1 (intentionally unstyled).

## Milestones

| # | Title | Status | Plan | PR | Notes |
|---|-------|--------|------|----|----|
| 1 | Foundation & Library Browsing | ✅ Merged | [M1](docs/superpowers/plans/2026-06-11-audioshelf-foundation.md) | [#1](https://github.com/yovanmc/AudioShelf/pull/1) | Scan `Author/` dirs → filename grouping (`original_stem` for file lookup) → browse author→works→chapters + played toggle. Harness + WAV fixture + CI. Read-only verified. |
| 2 | Playback & Progress | ✅ Merged | [M2](docs/superpowers/plans/2026-06-11-audioshelf-m2-playback.md) | [#2](https://github.com/yovanmc/AudioShelf/pull/2) | Now-playing PlayerBar (play/pause, seek, skip ±15/30s, volume, sleep timer); auto-mark-played on finish + `play_events`; stop after each. Asset scope hardened to `[]` + runtime grant of library root. |
| 3 | Tags & Discovery | ✅ Merged | [M3](docs/superpowers/plans/2026-06-11-audioshelf-m3-discovery.md) | [#3](https://github.com/yovanmc/AudioShelf/pull/3) | Author TagEditor; Discover panel (For-you / pick-a-tag / more-from-author). No schema change. DiscoveryView `picked` lifted to App (controlled). |
| 4 | Opt-in Rename Tool | ✅ Merged | [M4](docs/superpowers/plans/2026-06-11-audioshelf-m4-rename.md) | [#4](https://github.com/yovanmc/AudioShelf/pull/4) | Defensive, crash-safe, reversible batch rename to canonical names (`rename.rs`: build_plan ok/noop/conflict; manifest-before-rename + TOCTOU; tolerant idempotent undo). The only audio-file mutation. |
| 5 | Grouping-Override Review UI | ✅ Merged | [M5](docs/superpowers/plans/2026-06-11-audioshelf-m5-grouping.md) | [#5](https://github.com/yovanmc/AudioShelf/pull/5) | Per-chapter Work/Ch# edit on Author Detail; DB-only `regroup_author` overlays `grouping_overrides` on the heuristic, re-applied on scan, never to disk. Merge/split/reassign/reset. 41 FE + 32 Rust tests (incl. `grouping_roundtrip`). Screenshot-verified merge→reset round-trip. |
| 6 | Settings & Library-Root Picker | ✅ Merged | [M6](docs/superpowers/plans/2026-06-11-audioshelf-m6-settings.md) | [#6](https://github.com/yovanmc/AudioShelf/pull/6) | App is now standalone-usable: generic `get_setting`/`set_setting` over the `settings` table + `tauri-plugin-dialog` folder picker. Bootstrap precedence `--library` flag → persisted `library_root` → first-run onboarding; a gone/unreadable saved root **fails safe** to Settings with the error. Prop-driven `SettingsView` (choose-folder/re-scan/onboarding), Settings button on LibraryView, `settings` walkthrough. 47 FE + 33 Rust tests. No new audio-file mutation (only the SQLite settings write). |
| 7 | Scale & Search Polish | ✅ Merged | [M7](docs/superpowers/plans/2026-06-11-audioshelf-m7-scale-search.md) | [#7](https://github.com/yovanmc/AudioShelf/pull/7) | Virtualized author list (`react-window` FixedSizeList, 300+/10k); backend `search_library` SQL search across authors/works/chapters (debounced 150ms, per-bucket cap 50, `LibraryView` fully controlled); inline color+initials cover placeholders; +40 `Zz Sample Author NN` filler fixtures (sort last) + `m7` walkthrough. **Finishes the v1 spec.** 53 FE + 34 Rust tests; m7 walkthrough verified (list+swatches+scroll, cool→works/chapters, sam→author) + player/grouping regression-verified (Jane Doe still first). |

### v2 — Library Experience

> Deepens browsing & organization (the user picked these three; multi-root explicitly deferred). Order: covers (visual, self-contained) → tags (schema, extends discovery) → browsing UI (frontend polish that builds on the prior two).

| # | Title | Status | Plan | PR | Notes |
|---|-------|--------|------|----|----|
| 8 | Embedded Cover Art | ✅ Merged | [M8](docs/superpowers/plans/2026-06-12-audioshelf-m8-cover-art.md) | [#8](https://github.com/yovanmc/AudioShelf/pull/8) | On-the-fly `get_work_cover` (embedded-first) / `get_author_cover` (folder-first) resolve a cover from audio-tag pictures (`lofty`) **or** a folder image (`cover/folder/front.{jpg,jpeg,png}`); thumbnailed (`image`, ≤256px) and cached under `<app_data>/covers/`, served via the existing asset protocol — **NO schema change**, no `base64`. Author dir = `parent(file_path)` (sidesteps harness-unset `library_root`). `<Cover>` lazy-loads with a module-level client cache and falls back to the shared color+initials `<Swatch>` (no layout shift). Folder-`cover.png` fixtures on Jane Doe (red) + Sam Smith (blue); `covers` walkthrough + `imagesSettled()` before cover shots. Read-only intact (only writes are app-private thumbnails). 6 unit + 2 integration Rust tests, 53 FE tests; covers/browse/m7 screenshot-verified. |
| 9 | Work & Chapter Tags | ✅ Merged | [M9](docs/superpowers/plans/2026-06-12-audioshelf-m9-work-chapter-tags.md) | [#10](https://github.com/yovanmc/AudioShelf/pull/10) | New idempotent `work_tags`/`chapter_tags` tables; generic injection-safe `replace_tags(table,key_col,…)` helper (+`set_work_tags`/`set_chapter_tags` commands); `WorkRow`/`ChapterRow` gain `tags`. Discovery refactored author-centric → **work-centric union** (author ∪ work tags; chapter tags excluded by design; backward-compatible). Search matches work/chapter tags via `EXISTS` subqueries; `get_all_tags` unions all 3 tables. UI: always-visible work TagEditor + per-chapter toggle (open-when-tagged). `tags` walkthrough. **No audio-file mutation; fixture counts stayed 43/44/47** (tags seeded at runtime); no `Cargo.lock` churn. 61 FE + 45 Rust tests; tags/discovery/m7 screenshot-verified. |
| 10 | Richer Browsing UI | ✅ Merged | [M10](docs/superpowers/plans/2026-06-12-audioshelf-m10-browsing-ui.md) | [#12](https://github.com/yovanmc/AudioShelf/pull/12) | Author list: sort A–Z/Length/Played% + filter by tag & played-status + enriched per-row counts (`… NN% played · <duration>`); author detail: sort works + per-work collapse/expand-all. Prefs persist as one `browse_prefs` JSON (M6 settings). `AuthorRow.{totalSecs,tags}` added to `query_authors` — **no schema change**. Sort/filter client-side on the in-memory virtualized list. Fixed a stale-closure pref bug (functional updaters). 104 FE + 47 Rust tests; browse walkthrough + m7/tags/covers screenshot-verified. Read-only; fixtures 43/44/47; no `Cargo.lock` churn. |

## Decision log & gotchas

- **Grouping:** `original_stem` is threaded for on-disk file lookup; canonical `stem` drops trailing words. Lone numbered file demoted to standalone ("Area 51" not split into "Area"/51).
- **Asset protocol:** static scope `[]` + runtime `allow_directory(root, true)` in `scan_library` (library root only).
- **Harness:** `settle()` (double rAF + 60ms) before every screenshot; a walkthrough step that reads React state set by the immediately-prior step must be self-contained (re-fetch). Screenshots render on the user's iOS app via the Read tool.
- **Subagents** must run cargo in the FOREGROUND (background builds can't be resumed).
- **Windows:** a colon in a filename creates an NTFS Alternate Data Stream, not a real file (bit M4 tests).
- **Rename↔grouping (v1 limit):** `grouping_overrides` is keyed on `file_path`, so renaming an overridden file via M4 orphans its override row. Acceptable for v1.
- **Settings (M6):** library root persisted in the `settings` table under key `library_root`. The `--library` flag is intentionally **not** persisted (keeps the harness hermetic); only user-picked roots survive restarts. Native picker needs a **capability file** (`capabilities/default.json` granting `dialog:default`) — plugin commands are gated, unlike custom app commands.
- **Tauri debug rebuild gotcha:** `cargo tauri build --debug` is a no-op cache hit if no *Rust* source changed — even after `npm run build` updated the embedded `dist/`. A stale binary runs old JS and the harness hangs. Force a relink (`cargo clean`, or touch a Rust file) after a frontend-only change before re-running the screenshot harness.
- **v2 scope (decided 2026-06-12):** "Library Experience" = M8 covers, M9 work/chapter tags, M10 richer browsing UI. **Multi-root libraries explicitly deferred** (not chosen). Playback-side features (speed, per-second resume, auto-advance) deferred to a possible later v3.
- **M8 design (cover art):** Resolved **on-the-fly by command** (`get_work_cover` embedded-first, `get_author_cover` folder-image-first) — **NO schema change/migration**. The library is flat (`Author/file.wav`), and chapter `file_path`s are absolute, so an author dir = `parent(file_path)`; this sidesteps the harness-unset `library_root` setting. Thumbnails (`image` crate, max 256px) cached under `<app_data>/covers/` keyed by source path+mtime hash, served via the **existing asset protocol** (`allow_directory(covers, true)` granted in `setup`; `convertFileSrc` on the FE) — no `base64` crate needed. `<Cover>` lazy-loads w/ a module-level client cache and falls back to the color+initials `<Swatch>` (no layout shift). **Harness gotcha:** `settle()` (2 rAF + 60ms) does NOT wait for `<img>` decode → added `imagesSettled()` before cover screenshots. Fixtures use **folder `cover.png`** images (PNG via `image` crate) on Jane Doe + Sam Smith — non-audio, so `fixture_scan.rs` counts stay 43/44/47; embedded extraction covered by unit tests (`lofty` picture read).
- **M8 (✅ shipped, PR #8):** Covers resolved **on-the-fly by command** (`get_work_cover` embedded-first, `get_author_cover` folder-first) — **NO schema change/migration**, as planned. Author dir = `parent(file_path)` (chapter paths are absolute), deliberately avoiding the harness-unset `library_root`. Thumbnails (`image` 0.25, ≤256px, aspect preserved) cached under `<app_data>/covers/` keyed by hash(source path + mtime), reused if present; served via the **existing asset protocol** (`allow_directory(covers, true)` granted in `setup`, `convertFileSrc`/`fileUrl` on the FE) — **no `base64` crate**. `<Cover>` (in `src/components/Cover.tsx`, which now also owns the shared `<Swatch>` moved out of `LibraryView`) lazy-loads with a **module-level `Map` cache** (so virtualized rows remounting on scroll don't refetch) and falls back to the initials `<Swatch>` — no layout shift, no broken-image icon. **Harness gotcha:** `settle()` (2 rAF + 60ms) does NOT wait for `<img>` decode, so added `imagesSettled()` (resolves on each image's `load`/`error`) and capture does `settle()→imagesSettled()→settle()`. Fixtures drop a non-audio `cover.png` into Jane Doe (red) + Sam Smith (blue), so `fixture_scan` counts stay 43/44/47 (do not edit that test). The `image=0.25` dep added to `tools/gen-fixture` pulls ~975 transitive `Cargo.lock` lines — must be committed (a later cargo build can regenerate it post-commit; re-check `git status` before PR). New harness output dir `.shots-baseline/` is gitignored. Non-atomic cache write (write-not-rename) accepted for a local thumbnail cache. PR also carried a small `chore(harness)` commit: an informational tile-max pixel-diff helper (`tools/Compare-Screenshots.ps1` + `verify.ps1` manifest) — CHANGED/NEW shots never fail a run.
- **M9 design (work/chapter tags — planned 2026-06-12):** Two new tables `work_tags(work_id,tag)` / `chapter_tags(chapter_id,tag)` appended to `SCHEMA_V1` (idempotent `IF NOT EXISTS`, no version bump/runner change). A generic `replace_tags(conn, table, key_col, id, tags)` helper backs author/work/chapter writes (table/col are **compile-time constants**, never user input → `format!` injection-safe); `set_tags` kept as the author alias so existing tests pass. `discovery_for_tags` **refactored author-centric → work-centric**: iterate active works w/ ≥1 unplayed chapter, match on the **union** of author-tags ∪ work-tags (BTreeSet → sorted `sharedTags`); backward-compatible (author-only works surface identically — existing `discovery_by_tags_ranks…` test stays green). **Chapter tags deliberately excluded from Discover** (too granular). `discovery_for_you` also seeds from recent authors' works' work-tags. Search gains tag matching via `OR EXISTS (SELECT 1 FROM work_tags/chapter_tags … LIKE ?1 ESCAPE '\')` folded into the existing works/chapters buckets (no new bucket types). `get_all_tags` = `UNION` across all 3 tables. `WorkRow`/`ChapterRow` gain `tags: Vec<String>`, populated in `query_author_detail`. UI: always-visible work `<TagEditor>` under each work heading; **per-chapter `<ChapterTags>` toggle** (🏷 Tags, open-by-default when `tags.length>0`). Tags are **seeded at runtime** in the `tags` walkthrough (mirrors M3 discovery seeding) → **fixture_scan counts stay 43/44/47, no on-disk fixture/`fixture_scan.rs` change**. No new crate deps (no `Cargo.lock` churn). New `tags` walkthrough (seed → detail → search-by-tag); regression-verify `discovery` + `m7` (the refactor touched both). Read-only-on-disk intact (only writes are SQLite tag rows).
- **M9 (✅ shipped, PR #10):** Work/chapter tagging. Two idempotent tables `work_tags(work_id,tag)` / `chapter_tags(chapter_id,tag)` appended to `SCHEMA_V1` (`IF NOT EXISTS`, no version bump). Generic `replace_tags(conn, table, key_col, id, tags)` helper backs all three levels — `table`/`key_col` are **compile-time `&'static str` constants** (never user input → `format!` injection-safe); `set_tags` kept as the author alias (NOT `id_eq(...)` — that helper never existed; the plan's own correction). `discovery_for_tags` **refactored author-centric → work-centric**: iterate active works w/ ≥1 unplayed chapter, match on the **union** of author-tags ∪ work-tags (BTreeSet → sorted `sharedTags`); backward-compatible (author-only works surface identically — `discovery_by_tags_ranks_shared_then_unplayed` stayed green). **Chapter tags deliberately excluded from Discover** (too granular). `discovery_for_you` also seeds recent authors' work-tags. Search matches tags via `OR EXISTS (SELECT 1 FROM work_tags/chapter_tags … LIKE ?1 ESCAPE '\')` folded into the existing works/chapters buckets (no new bucket types, `?1`/`?2` bindings unchanged). `get_all_tags` = `UNION` across all 3 tables. UI: always-visible work `<TagEditor>` under each work heading; per-chapter `<ChapterTags>` **toggle** (🏷 Tags, open-by-default when `tags.length>0`). Tags **seeded at runtime** in the `tags` walkthrough (mirrors M3) → **fixture_scan counts stayed 43/44/47, no on-disk fixture change**. **No new crate deps → no `Cargo.lock` churn.** Verified: 61 FE + 45 Rust tests green; `tags` walkthrough screenshot shows all three tag levels (author "cozy" · work "mystery" · chapter "intro" open) + search "mystery" surfaces the work by tag not title; `discovery` healthy post-refactor, `m7` pixel-identical. Read-only-on-disk intact (only writes are SQLite tag rows).
- **M10 design (richer browsing UI — planned 2026-06-12):** **No schema migration** (preserves the M8/M9 streak). The user dropped **"recently-added" sort** precisely because the schema has no `added_at`/`created_at`/`scanned_at` column and we won't add one for it; sorts are **A–Z / Length / Played %**. Filters = **tag** (author's own tags ∪ its works' tags) + **played status** (All / Has unplayed / Fully played / Not started). Backend delta is minimal: `AuthorRow` gains `total_secs` (`SUM(duration_secs)` over active chapters) + `tags` (author_tags ∪ work_tags, chapter tags excluded mirroring M9 Discover), both populated in `query_authors` — **no DDL change**. **Sorting/filtering is client-side** on the already-in-memory, virtualized author list (M7 fetched all up front); work-level length/played% derive FE-side from `WorkRow.chapters`. Author detail gains a work-sort `<select>` + collapse/expand-all (per-work + all); **collapse state is session-only** (not persisted), only the chapter `<ul>` collapses (work tags stay visible). Preferences (authorSort, filterTag, filterStatus, workSort) persist as **one `browse_prefs` JSON** in the M6 settings table; `parseBrowsePrefs` fails safe to defaults on malformed/absent value. New pure module `src/lib/browse.ts` (sort/filter/parse, unit-tested) + `SortFilterBar.tsx`; `summarizeAuthor` enriched with `% played` + total length (shared `formatDuration` extracted to `src/lib/time.ts` if currently local). The existing **`browse` walkthrough** is extended (seed tags+played at runtime → sorted → filtered → detail) so **fixtures stay 43/44/47**, no on-disk fixture/`fixture_scan.rs` change, no `Cargo.lock` churn. Read-only-on-disk intact (only write is the `browse_prefs` setting). **Length-sort ordering is verified by unit tests** (fixture WAV durations may be ~0/equal, so screenshots only confirm the UI wired up).
- **M10 (✅ shipped, PR #12):** Richer browsing UI. **No schema change** (M8/M9/M10 no-migration streak intact) — "recently-added" sort was dropped precisely to avoid adding an `added_at` column. Backend delta only: `AuthorRow` gained `total_secs` (`COALESCE(SUM(c.duration_secs),0)` over active chapters) + `tags` (author_tags ∪ that author's active work_tags, chapter tags excluded mirroring M9 Discover), both populated in `query_authors` (two grouped passes into a `HashMap<i64,BTreeSet>` → sorted/deduped). **Sorting & filtering are client-side** on the already-in-memory virtualized author list (M7 fetched all up front); work length/played% derive FE-side from `WorkRow.chapters`. New pure module `src/lib/browse.ts` (`sortAuthors`/`filterAuthors`/`sortWorks`/`authorPlayedFraction`/`workPlayedFraction`/`parseBrowsePrefs`, all unit-tested) + `SortFilterBar.tsx`; shared `formatDuration` extracted from `AuthorDetailView` to `src/lib/time.ts` (now shared by `library.ts`/`browse`). Author detail: work-sort `<select>` + per-work ▾/▸ collapse + Collapse/Expand-all — only the chapter `<ul>` collapses (work `<TagEditor>` stays visible); **collapse state is session-only** (not persisted). Prefs (authorSort/filterTag/filterStatus/workSort) persist as **one `browse_prefs` JSON** in the M6 settings table, seeded on startup via `parseBrowsePrefs(getSetting("browse_prefs"))` after scan (not on first-run onboarding); malformed/absent → defaults (fail safe). **Gotcha caught in screenshot verify:** the persist handlers originally each spread a stale `browsePrefs` closure, so two synchronous setters (`setFilterTag` then `setFilterStatus`) made the second overwrite the first (`filterTag` lost) — fixed by converting to **functional state updaters** (`persistPrefs((prev)=>…)`) that compose and persist the actually-applied value. `browse` walkthrough extended (seed cozy tags on 3 authors + mark one chapter finished at runtime → sorted-by-length → tag+status filter → author detail) so **fixtures stayed 43/44/47, no on-disk fixture change**. **No new crate deps → no `Cargo.lock` churn.** Frontend-only fix required a forced Rust relink (`cargo clean -p audioshelf`) before re-running the harness (the debug-rebuild cache-hit gotcha). Verified: 104 FE + 47 Rust tests green; browse screenshots show all three sort/filter selects + enriched summary + Tag=cozy/Has-unplayed narrowing + author-detail work-sort/collapse; m7/tags/covers unregressed. Read-only-on-disk intact (only write is the `browse_prefs` setting).
- **M7 (✅ shipped):** Search is a **backend** `search_library` SQL command (indexed `LIKE` w/ ESCAPE, per-bucket cap 50), debounced 150ms client-side — *not* client filtering (would defeat the 10k scale target). `LibraryView` is **fully controlled** by App (query + results props), staying free of `invoke`. Author list virtualized with **`react-window` `FixedSizeList`** (explicit height/itemSize → renders a bounded window even in jsdom, enabling a deterministic 1000-author bound test). Cover placeholders are **inline-styled** color+initials (app ships no stylesheet, so a CSS class would be inert). Fixture gained **40 `Zz Sample Author NN`** authors named to sort **after** the real three so first-author walkthroughs (player, grouping) are unaffected — regression-verified. **Gotcha:** the fixture-count integration test (`src-tauri/tests/fixture_scan.rs`) hardcodes author/work/chapter counts (was 3/4/7, now 43/44/47) — any future fixture change must update it. Substring search means querying `sam` also matches `Zz Sample Author NN` (the substring "sam" is in "Sample") — expected, not a bug. **`react-window` v1 only** (v2 has a breaking API). CI uses `npm ci`, so `package-lock.json` ships synced. **With M7 the v1 spec (design doc §4/§6/§13) is fully implemented.**
